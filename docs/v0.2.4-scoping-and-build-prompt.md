> **Archivist's note (5 Sep 2026, the v0.2.4 build session).** This is the PM's brief, saved
> verbatim below as delivered in the chat message — the paper trail, not the spec of record
> (that is `docs/v0.2.4-proposal.md`). Where the repo disagreed with it, the repo won, and the
> disagreements are recorded here rather than edited into the text:
>
> - **The Node version — two machines, not one.** The brief read `node v22.23.2` and presented
>   it as Beni's Mac (§6.1). It was not the Mac: that number is `node -v` inside the mounted
>   Linux VM the PM session runs in. The session that built v0.2.4 ran `node -v` on the Mac →
>   `v24.15.0` (nvm default `24.15`). Both satisfy jsdom 30.0.1's engine range
>   (`^22.22.2 || ^24.15.0 || >=26.0.0`); decision 6.1 is answered in the proposal. *(Wording
>   corrected 5 Sep 2026 by the review of PR #21, Beni's item: the first draft of this note read
>   as "the brief was wrong about the Mac", when it was right about a different machine.)*
> - **PR #19's hold reasons** were marked ROUTED; they are now CONFIRMED —
>   `gh pr view 19 --json title,labels,mergedAt,mergedBy`: title
>   `⚠ npm run sync 09/04/2026 5:34 PM ET — something inside 72h moved`, label `hold: human`,
>   merged by BeniCheni 2026-09-04T22:00:18Z. The squash commit on `main` (`895b37d`) carries
>   the plain title, which is why the commit log alone could not settle it.
> - **The local checkout** was not two behind: the worktree this session ran in was cut at
>   `810165c`, already `origin/main`. The brief's reading came from a different checkout.
> - **The inventory** — 39 occurrences of `beni-pr-review` in 12 files — was exact.
> - **§2.6's grep acceptance** ("nothing live") is met with two deliberate exceptions, both
>   pointers carrying the new name beside the old: one `CLAUDE.md` ladder line and one
>   `docs/README.md` archive-table row. Named in the proposal's verification matrix.

---

# Kickoff v0.2.4 — "tests reach the wiring"

**Scoping + build prompt for a Claude Code session in the Kickoff repo.**
Written 5 Sep 2026 (Brooklyn) by the Cowork PM/TPM session running `/beni-betting-pipeline`,
Track B. You are the CTO for this release. Route this at Fable 5 **High**; the review pass that
follows is **Extra**, run by a human — and §9A renames that command inside this very release, so
which name he types depends on which tree his session starts in (§11 step 5).

Everything factual below traces to a file read or command run on 5 Sep 2026 against a repo whose
local `main` was two commits behind `origin/main` — so **re-read it all yourself before you
build**. This prompt is a brief, not a source. Repo ground truth beats it, including where it is
wrong.

---

## 0. Before anything

1. `TZ=America/New_York date` before you write any date anywhere.
2. `git fetch origin` (note: this failed from a mounted VM with "Host key verification failed" on
   5 Sep — if it fails for you too, say so and read with `git show origin/main:<path>` rather than
   trusting the working tree), then `git log --oneline -12 origin/main` and `git status -sb`.
   As of 5 Sep 2026 `origin/main` was `810165c` — `Docs: the front door gets its voice back (#20)`
   — and the local checkout was at `5a23143`, two behind. **Pull before you branch.**
3. Read in this order, whole: `CLAUDE.md`, `AGENTS.md`, `docs/v0.3.0-ideas.md` (rows **and** the
   `## Process notes` tail — they bind you), `docs/v0.2.1-proposal.md` §"The train" *including its
   two dated superseding notes*, `docs/v0.2.2-proposal.md`'s numbering note, `CHANGELOG.md`
   `[Unreleased]` and `[0.2.3]`, `.claude/skills/beni-pr-review/SKILL.md`.
4. Record the baseline yourself: `npm run typecheck`, `npm test`, `node -v`. **Do not carry a test
   count out of any document, this one included** — every prompt that has carried one was stale
   within days (`SKILL.md` §2).
5. Never run `npm run sync` in this cycle. `npm run sync -- --check` is the only form allowed.
   Leave the bot's `sync/scheduled` PR alone; if one is open and held, it is Beni's, not yours.

---

## 1. What v0.2.4 is

**v0.2.4 — "tests reach the wiring".** A **patch**: test infrastructure, no app behaviour, nothing
under `src/` changes except what a test needs in order to be importable.

Numbering is settled, not open: `CHANGELOG.md` `[0.2.3]`'s "Deliberately not done" fixes the rig
at **v0.2.4** and the resilience patch at **v0.2.5** (free to split into v0.2.5 + v0.2.6), v0.3.0
unchanged — Beni's call, 4 Sep 2026, and it is written into `README.md`, `CLAUDE.md`,
`docs/v0.3.0-ideas.md` row 5 and both proposals' numbering notes. **Do not renumber anything.** If
you believe a number must move, stop and ask Beni; a session that picks one silently has
renumbered a public roadmap.

The row is `docs/v0.3.0-ideas.md` **row 5** ("A component test rig", size M), which is
`docs/v0.2.0-ideas.md` row 5 — read that file's text too, it is the fuller description. The scope
as `docs/v0.2.1-proposal.md`'s train table wrote it (there listed as v0.2.2, before two slides):

> `test.projects`, jsdom + Testing Library, `tsconfig.test-dom.json`, a `matchMedia` stub, first
> `.tsx` tests for `useUrlState` popstate, the re-anchor effect on a `today` change, App's lens →
> theme effect under StrictMode.

**This prompt amends that scope in four places (§2 items 4–7),** the last of which carries Beni's
5 Sep decision to rename the review command. The amendments are the PM's call; how you implement
or split them is yours.

**Write `docs/v0.2.4-proposal.md` first.** There is no spec of record for this release yet. Audit
the repo, state the directions you considered and the one you took, and carry the review
resolutions back into it later — v0.2.5's session will read it as the spec, not this prompt.

---

## 2. Scope — in

1. **Two vitest projects under one `npm test`.** `test.projects` (Vitest 4 — `vitest@^4.1.11` is
   what `package.json` pins; confirm the resolved version): the existing node project keeps
   `environment: 'node'` and its `tests/**/*.test.ts` include, and a new jsdom project takes the
   DOM tests. Today the whole vitest config is inline in `vite.config.ts` under `test:`; if
   adding `projects` there stops typechecking cleanly, moving to a `vitest.config.ts` with
   `defineConfig` from `vitest/config` is a legitimate answer — say which you did and why.
2. **A DOM tsconfig, and `npm run typecheck` still green.** `npm run typecheck` is `tsc -b
   --noEmit` over the solution in `tsconfig.json`, which references `tsconfig.app.json` (include:
   `src`) and `tsconfig.node.json` (include: `scripts`, `tests`, `vite.config.ts`; `lib:
   ["ES2023"]`, `types: ["node"]`, no DOM). A `.tsx` test under `tests/` breaks the node project
   as it stands, so: add `tsconfig.test-dom.json`, reference it from `tsconfig.json`, and exclude
   the DOM tests from `tsconfig.node.json`. **`tsc -p tsconfig.node.json` alone must stay green**
   — that is the train table's own acceptance criterion. Where the DOM tests live on disk
   (`tests/dom/`, `tests/*.test.tsx`, `src/**/__tests__/`) is yours to decide; name the choice.
3. **Testing Library, jsdom, and the smallest dependency set that works.** `SKILL.md` §5: no new
   dependency without saying why in the spec of record — so justify each one in
   `docs/v0.2.4-proposal.md`, one line each. Check peer ranges against what is actually installed
   (React 19.2.8, Vitest 4.1.11, Vite 8.2.2, TypeScript 7.0.2 per `package.json` on 5 Sep) and
   **report the resolved versions**; do not assume a major is compatible because it is current.
   Whether `@testing-library/jest-dom` earns its place alongside vitest's own `expect` is a real
   question — answer it rather than installing it by reflex.
4. **(Amendment) The five affordances of §4, each with one test that proves it.** This is the
   handoff to v0.2.5 and it is the point of the release.
5. **(Amendment) The two historical regressions of §5.** A rig that cannot catch the two bugs
   that motivated it has not earned a release note.
6. **(Amendment) The deliberate red, quoted.** Break one component's behaviour, show the jsdom
   project failing, revert, show it green. Paste both outputs into the PR comment. The train
   table lists this under "Verified by"; it is promoted here to a scope item so it is not
   skipped, because "the tests pass" is exactly the claim a new rig cannot make about itself.
7. **(Amendment) The review-skill work of §9** — the **rename to `/kickoff-pr-review`** (§9A) and
   the **gate amendments** (§9B) — as **two separate commits, rename first**. A `git mv` combined
   with heavy content edits in one commit can drop below git's rename-similarity threshold and
   record as delete+add, which costs `git log --follow` on the file. Move it, then edit it.
8. **The bookkeeping of §8.** Including folding `[Unreleased]`'s PR-#20 copy-pass entry into
   `[0.2.4]` — it says in its own text that it "rides the next one".

---

## 3. Scope — out, and it goes in "Deliberately not done"

- **No app behaviour changes.** If the rig surfaces a real bug — and it may; that is what it is
  for — **do not fix it here.** Write it up as a row appended to `docs/v0.3.0-ideas.md` with a
  reproduction, and let v0.2.5 or v0.3.0 take it. Test infrastructure inside a behaviour diff is
  harder to review adversarially; that was v0.2.0's stated reason for giving the rig its own
  release, and fixing a bug inside the rig's own PR reintroduces exactly the mixture.
  *The one exception:* a change under `src/` needed purely to make a unit importable or a pure
  function reachable — extract, don't rewrite, and name it in the PR comment.
- **The browser matrix is not retired.** A jsdom rig covers wiring, not layout, not contrast, not
  `scrollWidth === innerWidth`, not what a real engine does with the marquee. `CLAUDE.md`'s
  verification discipline and `SKILL.md` §4 stand unchanged. Say so in the CHANGELOG in one
  sentence, so no later session reads the rig as permission to stop capturing.
- **No Playwright, no `scripts/verify-matrix.mjs`.** Settled in `docs/v0.2.1-proposal.md`'s
  "The skill's three open questions, answered" (3): the matrix lives as snippets in
  `.claude/skills/beni-pr-review/browser-matrix.md`. If Playwright ever arrives it is its own
  proposal.
- **The archived `docs/` files are not edited, and released `CHANGELOG.md` sections are not
  edited.** This bites on the rename (§9A) and it is the repo's own settled convention: v0.2.1's
  Decisions refused to edit `[0.2.0]`'s superseded sentence ("historically true for what 0.2.0
  shipped … Keep a Changelog convention leaves released sections alone"), and both numbering
  slides were handled with **dated notes at the head of the affected section**, never rewrites
  ("The table below is left as written — it is v0.2.1's spec of record, and it is not
  retconned"). A global find-and-replace across `docs/` would retcon the paper trail.
- **`benicheni` is the GitHub username, not the skill name.** It appears in
  `.github/workflows/pages.yml` and in `README.md` as `https://benicheni.github.io/kickoff/`.
  Any pattern loose enough to catch `beni-pr-review` and also `benicheni` breaks the live demo
  URL. Match the full token `beni-pr-review`, and diff before you commit.
- **Not this release:** rows 6, 7, 8, 11, 12, 13a–d (v0.2.5, the resilience patch); rows 3, 4, 10
  and two thirds of 11 (the design cycle — a Claude Design brief, not an implementation guess);
  rows 1, 2 (v0.3.0); rows 9, 14, 15, 16, 17, 18, 19 (unscheduled). Numbers are
  `docs/v0.3.0-ideas.md`'s.

---

## 4. The five affordances v0.2.5 inherits — the clean handoff

`docs/v0.2.1-proposal.md`'s train table says the resilience patch "depends on" this release "for
the component tests it will carry." That dependency is currently a sentence. Make it a fact: the
rig must demonstrate, with one passing test each, the five things v0.2.5's rows need. If it ships
without them, v0.2.5's fixes get browser-only proof again and this release bought nothing.

| # | Affordance | Proves it for | Suggested first test |
|---|---|---|---|
| 1 | **Drive the clock** — the app's `src/lib/clock.ts` store and `useNow`, not a fresh instance; fake timers or an injected offset; a visibility/focus catch-up | rows 7, 11, and every "does it still tick" claim | the `FixturesPage` re-anchor effect on a `today` change (the train's own named test) |
| 2 | **Drive URL state without navigating** — `history.replaceState` + a synthetic `PopStateEvent`, the method the process notes already prescribe for the browser | rows 7 and 9; the `?only=` / `&date=` contract the betting pipeline's Step 0 reads | `useUrlState`'s popstate handler (the train's named test) |
| 3 | **StrictMode double-subscribe / double-effect** | rows 7 and 12 — both are subscription bugs waiting to happen | App's lens → theme effect under StrictMode (the train's named test) |
| 4 | **`matchMedia` and a `localStorage` that throws** | row 12's `prefers-color-scheme` listener and the theme boot | `theme.ts` under a stubbed `matchMedia` and a storage that throws on read |
| 5 | **A deliberate throw under a boundary, with the console noise contained** | row 6, the error boundary — the cheapest insurance in the codebase, and untestable without this | a throwing child asserted to be caught, with React's error logging suppressed for that test only |

Affordance 5 has no error boundary to test yet (row 6 is v0.2.5). Prove the *rig capability* — a
throw under a test-local boundary is caught, asserted, and does not pollute the run — so v0.2.5
inherits a working pattern rather than inventing one.

---

## 5. The two regressions the rig must reproduce

`docs/v0.3.0-ideas.md` row 5 names two bugs as the entire evidentiary case for this release:

- the **`?date=` crash** (found in the v0.1.0 browser review), and
- the **pinned-date rollover bug** (found in the v0.2.0 browser review).

Both lived in component wiring; both were found only in a browser. Reconstruct each from the
git history (`git log -S`, the v0.1.0 and v0.2.0 review commits, `docs/v0.0.3-review-prompt.md`
and `docs/v0.2.0-review-prompt.md`) and write a jsdom test for each that **fails against the
pre-fix logic and passes against today's**. Demonstrate the failure — a stash, a temporary
revert, a re-introduced condition — and quote it.

If one of them turns out not to be reproducible in jsdom, that is a finding, not a failure: say
so, say why (what it needed that jsdom does not give), and record it in the proposal. A rig with
an honest boundary is worth more than one with a claimed one. Do not weaken a test to manufacture
the receipt.

---

## 6. Open decisions — yours, with the evidence attached

The PM decides scope and sequence; you decide implementation. Four of these five are yours; the
fifth is a fact only you hold. Each comes with evidence rather than an instruction:

1. **The Node floor.** `.github/workflows/ci.yml` pins `node-version: 24`. Beni's machine ran
   `node v22.23.2` on 5 Sep 2026. The train table's acceptance line reads "`ci.yml` on Node 24
   proves jsdom 30's engine floor" — which says nothing about the local checkout. If the jsdom
   major you land requires Node ≥ 24, `npm ci` breaks on Beni's Mac and he finds out at the worst
   moment. Decide: pin a jsdom that supports Node 22, add an `engines` field and say what it
   means, or tell Beni he needs to move. **Whichever you choose, state it in the CHANGELOG and
   in `CONTRIBUTING.md`'s run instructions** — a floor nobody wrote down is a trap.
2. **Where the DOM tests live and how the tsconfigs split** (§2.2).
3. **Whether the vitest config stays in `vite.config.ts`** (§2.1).
4. **Whether this ships as one release or two.** `CLAUDE.md`: a patch train is legitimate when
   the pieces are independently verifiable, and the implementing session decides where the splits
   fall because it holds the diff. The PM's read is that this is **one release** — the rig, the
   affordances and the regressions share a single verification loop and split badly — but if the
   diff argues otherwise, split it and say so. **A split takes v0.2.4 + v0.2.5 and pushes the
   resilience patch to v0.2.6; that is a numbering fork, so ask Beni before doing it**, per
   `CLAUDE.md`'s "Escalate numbering forks to Beni".
5. **One report, not a decision: does v0.2.5 split?** Beni decided on 5 Sep 2026 that the build
   model moves from Fable 5.1 to **Opus 5 High** once the current train is done; the review pass
   stays **Fable 5.1 Extra**. He proposed v0.3.0 as the switch point. The PM's amendment, which
   he has not yet ruled on, is that **v0.3.0 is the wrong release to make the first Opus run** —
   it is the minor, it touches `diff.ts`, and Track A's Step 0 leans on that engine for every
   open position; switching there also inverts this repo's own doctrine of landing the
   asynchronously-verified item first and reviewing the largest diff last. The cheap alternative
   is a patch: `docs/v0.2.1-proposal.md` leaves v0.2.5 free to split into v0.2.5 + v0.2.6, and
   the second half of a split is the natural low-stakes first Opus run.
   **You are not being asked to pick the switch point — that is Beni's.** You are being asked
   for the one fact the decision needs, which only the session holding the diff has: *does the
   resilience patch actually split?* Answer it in `docs/v0.2.4-proposal.md`'s "What v0.2.5
   inherits" section, in one line, either way.

---

## 7. Verification matrix — name it, then re-run it

Write this into `docs/v0.2.4-proposal.md` so the review re-runs it rather than trusting the PR
body's account of it (`SKILL.md` §4).

- **Both projects green under one `npm test`.** Report the real numbers, per project.
- **`tsc -p tsconfig.node.json` alone stays green** with the DOM tests excluded, and
  `npm run typecheck` (the whole solution) is green.
- **`npm run build` green**, and `npm run build:single` too — the single-file mode is a shipped
  script and a config change is exactly what breaks it quietly.
- **The deliberate red, quoted** (§2.6): red output and green output, both in the PR comment.
- **The five affordances**, one named test each, listed by test name in the comment.
- **The two regressions** (§5), each with the demonstration of its pre-fix failure — or an honest
  statement of why one could not be reproduced.
- **Smoke pass, not the full matrix.** The diff is tooling and docs, so `SKILL.md` §4's smoke
  pass applies: build, load at 390 and ~1000 px, confirm the header's version string reads
  `0.2.4`, assert `document.documentElement.scrollWidth === window.innerWidth`. Set the viewport
  *before* any capture. Do not spend 48 captures proving a test rig did not break the renderer —
  but do not skip the version-string check, because §8 moves it.
- **The rename (§9A), three checks.** `git log --follow` on the moved `SKILL.md` still reaches
  v0.2.1; `git grep -n 'beni-pr-review'` returns **only** the archived `docs/` files, the released
  `CHANGELOG.md` sections and (if kept) the `README.md` Lineage line — nothing live; and
  `git grep -n 'benicheni'` returns exactly what it returned before the diff.
- **The rename's one human acceptance step.** A skill's name can only be tested by invoking it,
  and a `.claude/skills/` entry that did not exist when a session started is not watched — that
  is the v0.2.1 review's own "Unknown skill" lesson, in `SKILL.md`'s "Who can invoke it" and in
  `docs/v0.3.0-ideas.md`'s process notes. **You cannot verify this yourself.** Put it in the
  handoff block: Beni opens a worktree on the v0.2.4 branch, starts a *fresh* session there, and
  confirms `/kickoff-pr-review` appears in the `/` menu and loads. Say plainly in the PR comment
  that this one item is unverified until he does it.
- **No workflow changed**, so no `dry_run` dispatch — say "not applicable" rather than leaving
  the line out.
- **CI on Node 24 must be green on the PR.** That run is the jsdom engine-floor proof; if it is
  red for an engine reason, that is decision 6.1 answering itself.

---

## 8. Bookkeeping

- **The version string moves in seven places** (`CLAUDE.md`, "Release management"):
  `package.json`; `package-lock.json` (**two** lines — `npm version 0.2.4 --no-git-tag-version`
  moves both files, a hand edit does not); `CHANGELOG.md`; `README.md`'s version badge; its
  `## ⏱️ What it does (v0.2.3)` heading; its `## 🏆 Lineage` list; and the annotated tag (§11 —
  Beni's, not yours).
- **`CHANGELOG.md`.** Fold `[Unreleased]`'s existing entry — "The front door gets its voice
  back", the PR #20 copy pass, which says in its own text that it rides the next release — into
  the new `[0.2.4]` section, and leave `[Unreleased]` empty. The section leads with **one
  sentence naming the release's subject**, then Added / Changed / Fixed, then **"Deliberately not
  done"** (this repo's signature heading — it stays). `### Changed` carries **two** review-skill
  lines: the rename (§9A — say why, in Beni's terms, and say that the archived record keeps the
  old name on purpose) and the gate amendments (§9B). Date it in Brooklyn time, on the day you
  expect Beni to merge; if the merge slips past Brooklyn midnight, `grep -rn '<day> Sep 2026'`
  and move every one before the tag.
- **`README.md`'s `## 🧭 Roadmap`** currently leads with "v0.2.4 — a component test rig". Remove
  the shipped bullet, keep v0.2.5, the design cycle and v0.3.0, and add a Lineage entry in the
  established voice.
- **`docs/README.md`'s archive table** gains rows for `v0.2.4-proposal.md` (rung: proposal) and
  for this prompt archived as **`docs/v0.2.4-scoping-and-build-prompt.md`** (rung: scoping) —
  save it verbatim; it is the paper trail, and the chat message was the delivery.
- **`docs/v0.3.0-ideas.md`:** strike through **row 5** in place (do not delete it — the
  strike-through history is the roadmap's audit trail), append any new rows the rig found, and
  add to the `## Process notes` tail everything this cycle taught about verifying — that tail is
  method, not scope, and the next reviewer reads it cold.
- **`CLAUDE.md`** gains a short paragraph on the two-project test setup: what each project
  covers, what the rig does *not* cover (the browser matrix stands), and the Node floor from
  decision 6.1.
- **Commits** authored `Claude <noreply@anthropic.com>` via per-commit
  `git -c user.name=Claude -c user.email=noreply@anthropic.com commit` — never `git config`, the
  repo config is shared across worktrees. End each message with the `Co-Authored-By` trailer the
  harness gives you for the model in your session; do not hard-code one.

---

## 9A. Rename `/beni-pr-review` → `/kickoff-pr-review` — Beni's call, 5 Sep 2026

His reasons, in his words: drop the personal nickname, and let the command name map back to the
repo. Ship it in v0.2.4 — it is tooling, no app behaviour, the same class as the rest of this
release, and it touches the same file as §9B.

**The move.** `git mv .claude/skills/beni-pr-review .claude/skills/kickoff-pr-review`, then in
`SKILL.md`: the frontmatter `name:` (this is what becomes the slash command), the trigger phrases
in `description:`, and the `/beni-pr-review <N>` in the "Who can invoke it" paragraph.
`argument-hint`, `disable-model-invocation: true`, `browser-matrix.md` and `pr-comment.md` all
stay as they are; the `${CLAUDE_SKILL_DIR}` references inside `SKILL.md` are path-relative and
need no edit — **verify that rather than trusting this sentence.**

**The inventory, as of `origin/main` on 5 Sep 2026: 39 occurrences of `beni-pr-review` in 12
files.** Re-run `git grep -n 'beni-pr-review' -- . ':!.claude/worktrees'` yourself; the split is
the point, not the count.

*Live instructions — rename:*

| File | Occurrences | Note |
|---|---|---|
| `.claude/skills/beni-pr-review/SKILL.md` | 3 | frontmatter `name`, `description`, "Who can invoke it" |
| `CLAUDE.md` | 3 | the prompt-ladder step 4 line and the two roadmap-pointer lines |
| `CONTRIBUTING.md` | 1 | reader-facing |
| `docs/README.md` | 2 | line ~17 is live method prose — rename it. The archive-table row for `v0.2.1-pr-review-skill-plan.md` describes a historical document; **"the deep-dive plan for the review command (then `/beni-pr-review`)" reads true both ways** — your call, say which you took |
| `docs/v0.3.0-ideas.md` | 1 | **row 14**, and see below |

*Historical record — leave the text alone:* `CHANGELOG.md` ×3 (all inside released `[0.2.1]` and
`[0.2.2]`), `README.md` ×1 (the v0.2.1 Lineage line), `docs/v0.2.1-pr-review-skill-plan.md` ×7,
`docs/v0.2.1-proposal.md` ×7, `docs/v0.2.2-proposal.md` ×3,
`docs/v0.2.x-one-shot-ultracode-prompt.md` ×4, `docs/v0.2.x-patch-train-scoping-prompt.md` ×4.

**Two dated notes, and no more.** The repo's established way of superseding an archived
statement is a dated note at the head of the affected section — that is exactly how both train
renumberings were handled. Add one to the head of `docs/v0.2.1-pr-review-skill-plan.md` and one
at `docs/v0.2.1-proposal.md`'s skill section: *"Renamed to `/kickoff-pr-review` in v0.2.4 (5 Sep
2026, Beni's call); the text below is left as written."* That is the whole reconciliation. Do not
touch the other five archived files.

**`README.md`'s Lineage line** ("v0.2.1 … the review becomes a command: `/beni-pr-review` as a
repo skill") is historical and stays historical. A parenthetical — *(renamed `/kickoff-pr-review`
in v0.2.4)* — is a pointer, not a retcon, and is allowed if you judge the bare old name confusing
next to a `CONTRIBUTING.md` that now says the new one. Substitution is not.

**Row 14 gets sharper, not just renamed.** `docs/v0.3.0-ideas.md` row 14 is the *portable,
account-level, repo-agnostic* review command — and it can no longer be called "a portable
`/beni-pr-review`" now that the repo skill owns a Kickoff-specific name. Rewrite the row to name
the repo skill as `/kickoff-pr-review` and to say the portable one needs a name of its own that
is neither personal nor repo-bound. **Do not pick that name** — naming is Beni's, and row 14 is
his item; leave it as an open question in the row.

---

## 9B. The gate amendments — same PR, second commit

From a 5 Sep 2026 retro on why PRs #18 and #19 needed Beni's hand. Both were **correct** — #18
was a release (`SKILL.md` §7 step 4 stops there by design) and #19 was the bot's held
`sync/scheduled` PR (§0 disclaims it entirely). Nothing about the gate's substance changes. What
changes is *when it announces itself* and *what it hands over*:

**(a) `SKILL.md` §0 — detect the release in the first read.** Add a bullet:

> **Detect a release before you start.** A version bump in `package.json`, a new `CHANGELOG.md`
> section, or a tag to follow makes this a release PR — §7 step 4 will stop you short of the
> merge. Say so in your first message and plan the handoff from the beginning; do not discover
> the gate at the end of a long pass.

**(b) `SKILL.md` §7 step 5 — name the tagger.** It currently reads "Whoever tags:". The tag
record shows that ownership moved without a rule ever changing it: `v0.2.0` and `v0.2.1` are
tagged `Claude <noreply@anthropic.com>`, `v0.2.2` and `v0.2.3` `Benjamin Chen
<benjaminlchen@gmail.com>` — the flip lands exactly at the first release after this skill
shipped. Make it explicit: **Beni merges and tags every release; the session's last act is the
handoff block.**

**(c) `SKILL.md` §7 — a new final step, "The handoff block."** On a release, end with a fenced,
paste-ready block: the merge to click and its squash message, the `git tag -a vX.Y.Z -m "vX.Y.Z —
<subject>"` line with the subject already written, `git push origin vX.Y.Z`, `git pull`, and the
three commands that must come back green. Beni tagged v0.2.3 fifty-eight seconds after merging
it, which means he was composing that himself; the skill should not rely on his speed.

**(d) `SKILL.md` §0 — say what the sync PR *is*, not just that it is not yours.** Keep "leave it
alone", and add: a held `sync/scheduled` PR carries the `hold: human` label, is a Track A Step 0
re-verification trigger for every open position, and is cleared by Beni alone — by merging it or
by removing the label. This skill neither reviews nor merges it.

Land these as one commit — the second, after §9A's rename — listed in the findings table like any
other diff (`SKILL.md` §9 asks for exactly this when the PR is tooling), and listed in the
CHANGELOG under `### Changed`.

**If you judge that §9B muddies a single-subject release,** the alternative is a standalone
micro-patch after v0.2.5. Say which you chose and why. Do not silently drop it. **§9A is not
optional and does not move** — it is Beni's decision, not a candidate.

---

## 10. What v0.2.5 inherits

Write a **"What v0.2.5 inherits"** section at the end of `docs/v0.2.4-proposal.md`, so the next
session reads one page instead of re-deriving the train:

- **v0.2.5 is the resilience patch.** Its scope, from `docs/v0.2.1-proposal.md`'s train table
  (listed there as v0.2.3, before two slides): *sync half* — row 8 (collect-and-exit-2 for
  un-normalizable events, no `<comp>:undefined` ever, `sourceId` `.min(1)`, one doctrine for
  standings) and row 13d (`inWindow` via `brooklynDate`, relocated and pinned). *App half* — row
  6 (keyed class boundary at the App seam, Reload + Reset view), row 7 (stable codecs,
  `useEffectEvent` popstate, hash preserved, `canonicalSearch()` normalize-on-load), row 12
  (inline boot script generated from one source + tested, `prefers-color-scheme` listener), rows
  13a–c, and row 11's NEXT day qualifier. It is free to split into v0.2.5 + v0.2.6.
- **Sequence the sync half first.** v0.2.0's rule, restated in v0.2.1's Decisions: land the item
  whose verification is asynchronous (a `dry_run` dispatch) before the one whose proof is the
  full browser matrix, so the slow loop runs while the large diff is built.
- **The five affordances of §4**, by test name and file, so v0.2.5 extends them instead of
  re-inventing them.
- **Anything the rig found and did not fix** (§3), with reproductions.
- **The Node floor** from decision 6.1.
- **The pending routing switch, and the trap in it.** Beni's decision of 5 Sep 2026 moves the
  build model to Opus 5 High after the train; the review stays Fable 5.1 Extra. **Do not
  pre-apply it.** `Kickoff/CLAUDE.md`'s routing line ("Fable 5 High for design/build, Extra for
  the review pass") stays exactly as it reads until the release that actually runs on Opus, and
  that edit is one line made in that release's own PR — never tidied up early by a passing
  session. The reason is mechanical: the same routing rule is also stated in the
  `/beni-betting-pipeline` skill, which **outranks a folder's `CLAUDE.md` on workflow** under
  that skill's own precedence rule. Moving the repo line alone would leave the higher-precedence
  copy stale and the update pointing backwards — failure mode #15 with the arrow reversed. The
  skill half moves separately in Cowork, by a `propose_skills` card Beni approves; a Claude Code
  session cannot make that edit and should not try (the synced skill copy on disk is a
  read-only cache). Both halves move in one sitting, or neither does.
- **The review command is `/kickoff-pr-review` from v0.2.4 on** (§9A), and the archived `docs/`
  and released `CHANGELOG.md` sections keep the old name on purpose. A v0.2.5 session reading
  the paper trail will meet `/beni-pr-review` in six files and must not "fix" any of them.
- **The design cycle is unblocked and is the long pole.** Rows 3, 4, 10 and two thirds of 11 want
  a Claude Design brief against Fergie Time; the train says nothing in it blocks the brief. The
  PM's recommendation to Beni is to start that brief in parallel with this release rather than
  after v0.2.5 — it is the only item on the board with human latency in it, and it gates a chunk
  of v0.3.x. Note it; the decision is Beni's.

---

## 11. Landing it

1. Branch, build, commit in small reviewable units, push, open the PR. Wait for `verify`.
2. **The version bump lives inside the PR** — this repo has bumped in the PR on every release.
   Only the tag is post-merge.
3. **You do not merge this.** It is a release: a version bump, a new `CHANGELOG.md` section, a
   tag to follow. `SKILL.md` §7 step 4 stops here and Beni clicks it. Say so in your first
   message, not your last.
4. **End with the handoff block** — §9B(c)'s shape, for this release: the squash message, the
   `git tag -a v0.2.4 -m "v0.2.4 — <your subject line>"` line, the tag push, the pull, and the
   green checks. Beni should be able to paste, not compose. **Include the rename's acceptance
   step** (§7): open a worktree on the branch, start a fresh session there, confirm
   `/kickoff-pr-review` appears in the `/` menu and loads.
5. The review pass is a **separate session** at Fable 5 Extra, in a worktree checked out *before*
   the session starts (a `.claude/skills/` that did not exist at session start is not watched).
   **Which command he types depends on which tree the session starts in, and you must say so:**
   from a worktree on the v0.2.4 branch it is `/kickoff-pr-review <N>` and the old name is gone;
   from a checkout still on `main` it is `/beni-pr-review <N>`. Getting this wrong reads as a
   broken skill. If you have findings you could not chase, hand them over as a **sealed
   appendix** — suspicions to re-derive independently, never conclusions.

---

## Verify this brief before you trust it

Everything above was read on 5 Sep 2026 from the Kickoff repo on Beni's Mac and from `origin/main`
where the working tree was behind. Two things could not be verified from the repo and are marked
as such:

- **PR #19's hold reasons.** GitHub's PR pages read back inconsistently through a fetching tool
  (they reported #19 and #20 as open, while `git log origin/main` shows both merged as `(#19)`
  and `(#20)` squash commits). The *mechanism* is confirmed from code — `sync.yml` line 191 adds
  the `⚠ … something inside 72h moved` title prefix only when the run is urgent, and lines
  342–350 refuse to arm auto-merge on a PR that already carries `hold: human` — and #19's merge
  commit title is the plain form. The specific reasons that held it are **ROUTED, not
  CONFIRMED**; `gh pr view 19 --json title,labels,mergedAt,mergedBy` settles it in one command.
- **Anything in `../Sportsbooks/`.** That folder was not connected to the session that wrote
  this, so no claim here depends on it.

If any of it disagrees with what you read: **the repo wins, say so in the PR, and correct the
archived copy of this prompt in the same commit.**
