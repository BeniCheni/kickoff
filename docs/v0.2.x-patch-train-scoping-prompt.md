# Kickoff — patch-train + next-minor scoping prompt (archive copy)

*Generated 3 Sep 2026, 01:17 ET, by the Cowork session running `/beni-betting-pipeline` (Track B,
PM/TPM role), from a fresh read of the repo at `60afeb1` + the uncommitted `CLAUDE.md` edit.
Delivered to Beni in chat, fenced and ready to paste; this file is the archive, never the delivery.
Filename uses `v0.2.x` deliberately — the release numbering is the open decision this prompt hands
to the Claude Code session, so naming the archive after a number would pre-decide it.*

Routing: **Fable 5 Extra**, fresh Claude Code session in the repo. First delivered saying High —
corrected the same session when Beni asked. The skill's build-track routing line (High for
design/build, Extra for the adversarial review) predates this **scoping rung** and does not cover
it; the rung's failure mode is a confident summary that later sessions treat as ground truth, plus
cross-file row reconciliation between two ideas lists, plus adversarially testing a hypothesis the
PM handed over — all Extra-shaped. Appendix C should gain the entry.

**Ultracode considered and rejected for this rung, for the record.** Ultracode is Extra effort *plus
workflows* — the same reasoning tier with multi-agent orchestration on top — and this session has
nothing to fan out to. Its work is a dependent chain (CHANGELOG → ideas files → proposal), and the
13 rows are classified as a set, not independently: row 1 decides whether row 2 is reachable, and
row 7's borderline call depends on where the patch/minor line landed for rows 6, 8 and 12. Split
across subagents that yields locally-sensible classifications that don't cohere as a release plan —
the failure the "one subject line per minor" convention exists to prevent. **Where Ultracode does
earn it in this repo:** the adversarial PR review (review dimensions in parallel, then each finding
verified by its own agent — the shape `docs/v0.3.0-ideas.md`'s process notes describe without
naming), and the browser matrix (3 lenses × 2 themes × 2 tabs × 4 widths = 48 independent captures,
each needing the viewport set and `scrollWidth` asserted before shooting).

**⚠️ SUPERSEDED for the actual run, same session.** Beni asked whether the scoping scope could be
bundled with the adversarial review and the browser matrix into one Ultracode session. It can, and
the bundled version — `docs/v0.2.x-one-shot-ultracode-prompt.md` — is what he ran. This file stays
as the archive of the two-session shape, which remains the right form on Extra without workflows:
without subagents there is no way to give the review a cold context, so build and review must sit
in different sessions. The bundle's whole legitimacy rests on that one capability.

---

# Kickoff — scope the patch train, then the next minor (release-planning deep dive)

You are the CTO on this repo. I'm the PM/TPM (a Cowork session running `/beni-betting-pipeline`);
Beni is the CEO. He has told me plainly that he has **not** read the generated `docs/` logs in
detail and is relying on you and me to be the experts here. Your job this session is to read the
whole release record and come back with a **release plan**, not code.

## 0. Trust nothing in this prompt

Every factual claim below is tagged ESTIMATE and was read at **01:17 ET, Thu 3 Sep 2026** from a
mounted VM session. Re-derive each one yourself before you lean on it. If any claim is wrong, say
so explicitly in your proposal — that's a finding, not a nuisance.

Also, before anything else:

- `git pull` — **local `main` was 1 commit behind `origin/main`** at the time of writing (ESTIMATE).
- `git status` — **`CLAUDE.md` has an uncommitted local edit** (ESTIMATE). It's mine: a new
  "Release management — semver, the CHANGELOG, and who decides what (added 3 Sep 2026)" section,
  plus a roadmap pointer updated from `v0.2.0-ideas.md` to `v0.3.0-ideas.md`. Read it — it is
  input to this task — and treat it as a docs change to fold into whichever release you scope
  first. Don't discard it, and don't treat it as spec-of-record: `docs/` and `CHANGELOG.md` outrank it.
- Run `TZ=America/New_York date` before writing any date anywhere in this session.

## 1. Read first, in this order, completely

1. `CLAUDE.md` and `AGENTS.md` — they outrank this prompt on process.
2. `CHANGELOG.md` — every released section plus `[Unreleased]`.
3. `docs/v0.3.0-ideas.md` — the current ranked candidate list (13 rows) **and** its
   `## Process notes` tail.
4. `docs/v0.2.0-ideas.md` — the older list. Rows carry forward keeping their **original numbering**,
   and the older file holds the fuller description for any row it originated. Row 4 in the v0.3.0
   list *is* row 4 of the v0.2.0 list.
5. `docs/v0.2.0-proposal.md` — the spec of record for the last minor, including its
   "Review resolutions" section and its accepted costs.
6. `docs/v0.2.1-pr-review-skill-plan.md` — a planned repo-local `/beni-pr-review` skill.
7. `README.md` (especially "Lineage" and "Beyond"), `package.json`, `git tag`, `git log --oneline -20`,
   `.github/workflows/ci.yml` and `sync.yml`.

Then, and only then, form a view.

## 2. Where the repo actually is (ESTIMATE — verify all of it)

- `package.json` version: **0.2.0**. Only tag: **v0.2.0**, pointing at `f8bcb14`.
- Two commits sit past that tag: `1249b4a` (PR #10 — a theory the CHANGELOG explicitly **retracts**)
  and `60afeb1` (PR #11 — the fix that actually worked: `sync.yml` approves its own held `verify`
  run through the Actions API).
- `CHANGELOG.md`'s `[Unreleased]` holds exactly one entry, `### Fixed`, describing that sync-approval
  fix. **It has no release number yet. Giving it one is part of your job.**
- **There is no v0.3.0.** `docs/v0.3.0-ideas.md` is the *candidate list* for the next minor, written
  cold by the session that reviewed and landed PR #8. It has not shipped.
- 11 test files under `tests/`, all `.test.ts`, `environment: 'node'`. Report the real
  `npm run typecheck` / `npm test` numbers yourself — I could not run them (macOS-built
  `node_modules` won't execute under the mounted VM).

## 3. What Beni asked for, in his words

> "deep dive into a feasible v0.3.1 first with all miscellaneous patches we want to ship first
> before the v0.4.0 minor bump. I'm okay with smaller pieces of the patches to have v0.3.2, v0.3.3
> instead of just aiming for a single v0.3.1 and then bumping right to v0.4.0, if our Claude Code
> CTO thinks with its best judgement to make that decision."

He has since confirmed to me that **the v0.3.1 / v0.4.0 numbers were his own slip** — he knows the
repo is at v0.2.0. So read his ask for its *shape*, which stands: **a train of miscellaneous
patches first, then one minor bump.** The numbers themselves are yours to decide.

He has also decided one thing already: **the `/beni-pr-review` skill rides as the FIRST patch in
the train.** Reasoning he accepted — it's fully specced, it's tooling-only, and it makes every
subsequent review in the train cheaper. If, after reading `docs/v0.2.1-pr-review-skill-plan.md`,
you think that's wrong, say so with reasons rather than silently reordering.

## 4. The open decision I am explicitly handing you

**Which numbers does this train take?** I refused to guess — semver numbering is a product decision
with a public paper trail, and a session that picks one silently renumbers the roadmap for everyone
downstream. Beni's instruction was to put it to you with the evidence. Three schemes, at least:

| Scheme | Shape | Cost |
|---|---|---|
| **A** | v0.2.1 / v0.2.2 / … patches → v0.3.0 minor | Nothing renumbers. `docs/v0.3.0-ideas.md` and `docs/v0.2.1-pr-review-skill-plan.md` both keep the numbers they already claim. |
| **B** | Ship a minor as v0.3.0 now, then v0.3.x patches → v0.4.0 | Matches Beni's literal numbers but inverts his stated ordering — the minor lands before the patches. |
| **C** | Something else you can argue for | Argue it. |

Pick one, justify it in the proposal, and state what it costs. **Do not silently renumber any
existing `docs/` file** — if a rename is genuinely required, propose it as its own line item with
the reason.

## 5. The scoping task

Produce a release plan covering **every** open item — all 13 rows of `docs/v0.3.0-ideas.md`, the
unreleased sync-approval fix, the `/beni-pr-review` skill, and the uncommitted `CLAUDE.md` docs edit.
Nothing may be silently dropped: an item is either in a named release or in a "deferred, and here's
why" list.

For each item state: **release · patch-or-minor and why · size · what it touches · what verifies it ·
what it depends on.**

Apply the repo's own conventions, which you'll find in `CLAUDE.md` and `docs/v0.2.0-proposal.md`:

- **Patch** = fixes, tooling, docs, housekeeping, security. No new user-visible capability. A data
  refresh isn't a release at all (v0.2.0's proposal dropped PR #5's 0.1.2 bump for this reason).
- **Minor** = a new capability under **one subject line**. Both minors so far shipped as a single
  theme ("one skeleton, three lenses"; "the app learns to tell time"), not a grab bag, and both
  read better for it.
- **Sequence by prerequisite, not by rank.** v0.2.0's own rule: land the item whose verification is
  *asynchronous* first, so that loop runs while the biggest diff is being built; review the biggest
  diff last, with everything else already green.
- **Name what's deferred and why.** "Wants a design brief this release doesn't have" is a reason.
  "Out of scope" is not.

### My grouping hypothesis — test it, don't inherit it

This is the PM's read from one pass over the ideas files. It is a hypothesis for you to confirm,
amend or reject with reasons — you're the one who'll hold the diff.

- **Tooling/docs patch (first):** the `/beni-pr-review` skill · the unreleased sync-approval fix
  (it needs a number) · row 13's dead `Default` ruleset (empty branch include, applies to nothing —
  delete it or point it somewhere) · the `CLAUDE.md` release-management edit. No app behavior.
- **Resilience patch (second):** row 6 (error boundary, S) · row 8 (the last `warn`-and-continue in
  the provider, XS–S) · row 12 (theme-flash hardening, XS–S) · the rest of row 13's relocations and
  label truths (XS). All fixes, no new capability, and they share a doctrine — "every surprise is
  loud, every label is true."
- **Row 7 is the borderline one.** `useUrlState` hygiene is stable-codec cleanup (patch-shaped), but
  normalize-on-load `replaceState` visibly rewrites the user's address bar (capability-shaped). Call
  it, and say which half drove the call. It also has a second reason to land early: v0.2.0's
  re-anchor effect in `FixturesPage` currently relies on a `prevToday` ref to no-op every render —
  correct and browser-verified, but the ideas file calls it fragile.
- **Test-rig patch (third), or its own release:** row 5 (jsdom + Testing Library, M). v0.2.0's
  proposal deliberately deferred it because test infra inside a feature release makes the diff
  harder to review adversarially — that argument says give it its own release, not that it's
  unimportant. Two releases of evidence now say every browser-found bug has lived exactly where the
  unit tests stop.
- **The minor:** rows 1 + 2 together, under one subject line. Row 1 (`RESULT_CHANGED` /
  `TEAM_RENAMED` diff kinds) is the prerequisite for row 2 (auto-merge an *empty* sync report), and
  row 2 pays off v0.2.0's single named accepted cost — the staleness banner measuring time since
  the last *change-bearing* merged sync, going amber then red through an international break even
  though the bot verified nothing moved. **Accepted costs are the highest-signal candidates in an
  ideas file: they're known debt with a named owner.** Something like *"the sync tells the whole
  truth."*
- **Not in this train:** rows 3 + 4 (Poster's "N REMAINING" vs Ledger's Next-up disagreeing live,
  and FixtureRow's stale-LIVE pill). The ideas file says both want a *designed* state, and a pill
  invented in an implementation prompt sits at the bottom of the precedence ladder
  (template > design brief > implementation prompt). These are a Claude Design cycle, and they
  should be resolved **together** — the ideas file says they're the same design question. Row 10
  (palette-level AA debts) is design-system work in Fergie Time, likewise out. Rows 9 and 11 —
  make a call and say why.

**One cross-track note you must weigh:** row 1 is not only a repo change. This app is the Step 0
fixture source for Beni's betting pipeline, and every `DATE_MOVED` / `TIME_CHANGED` /
`HOME_AWAY_INVERTED` / `STATUS_CHANGED` line in a sync PR is a re-verification trigger for a real
open position. A corrected full-time score or a club rename landing silently is therefore a
betting-data honesty gap, not just a missing report line. Say so in the proposal if you agree.

## 6. Constraints

- **Spec precedence: template > design brief > implementation prompt.** Note every resolution.
- `npm run typecheck` and `npm test` green at every commit. **Data-honesty test assertions are never
  weakened to make anything pass.**
- Commits authored `Claude <noreply@anthropic.com>` (per-commit `git -c`).
- No new dependencies without saying why in the proposal.
- Fixture data in `src/data/*.json` is written only by `npm run sync` — never hand-edited.
- Don't prune `.claude/worktrees/` entries; their absolute gitdir paths only resolve on the Mac.
- `../Fergie Time Design System/` is local-only and unreleased. Never commit it here; the `docs/`
  references to it are deliberate teasers, not broken links.

## 7. Deliverable, and where it stops

1. Write **`docs/<version>-proposal.md`** for the first release in your train, in the shape of
   `docs/v0.2.0-proposal.md`: scope (locked), audit findings, decisions with reasoning, the
   verification matrix, and what stays unverified. That file is the spec of record.
2. Write, or update, a **release-plan section** covering the whole train and the deferred list —
   either inside that proposal or as `docs/<version>-release-plan.md`, your call; say which and why.
3. Commit those docs on a branch (do not push to `main`).
4. **Then stop and report to Beni in chat**, in this order:
   - the numbering scheme you chose, in one sentence, and what it costs;
   - the train as a table: release · subject line · items · size;
   - what you deferred and why;
   - anything in section 2 of this prompt that turned out to be wrong;
   - the real `typecheck`/`test` numbers you got.

**Do not write application code in this session unless Beni replies "approved, go" in this same
chat.** If he does, implement **only the first release in the train**, on its own branch, with a PR
— and stop again at the PR rather than merging. The review pass is a separate session.

## 8. Out of scope for this session

Any of rows 3, 4, 10 (design work); merging anything; tagging anything; touching
`src/data/*.json`; touching `../Sportsbooks/`; renaming existing `docs/` files without proposing it
as a line item first.

## 9. The house standard, restated because it's the point

This repo's religion is **generated and diffed, never typed** — never render a kickoff time the
league hasn't set, never invent a matchday number, both clocks derive from one UTC instant. Its
`CHANGELOG.md` is a product surface, not a commit log: the repo is public, a reader of Beni's
LinkedIn posts may clone it, and every released section leads with one sentence naming the
release's subject, then Added / Changed / Fixed, then **"Deliberately not done."** That last
heading is this repo's signature. Keep it.
