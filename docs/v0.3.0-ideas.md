# v0.3.0 — candidate work, from the v0.2.0 review

Written cold, 2 Sep 2026, by the session that reviewed and landed PR #8 (v0.2.0, "the app
learns to tell time"), for whoever scopes the next release. Everything here was judged
*real* during that review and *out of scope* there — either carried open from
`docs/v0.2.0-ideas.md` (rows 2, 4–9, 11, 12 stay open; their text there is still the
fuller description) or found by this pass. Ranked by research value × feasibility; sizes
are rough (XS < an hour, S < half a day, M ≈ a day or two).

Two things about v0.2.0 shape this list. First, the scheduled sync now commits only when
the diff engine reports a change, so on a quiet stretch the app's `synced` stamp and the
staleness banner measure time since the last *change-bearing* sync a human merged — they go
amber at 24 h and red at 72 h through an international break even though the bot verified
nothing moved, twice a day. That is a known, accepted, documented price (proposal, "Review
resolutions"), and rows 1–2 below are how it gets paid off. Second, every browser-found bug
in two releases running has lived exactly where the unit tests stop (the component wiring),
which is why row 5 keeps its place.

## Ranked ideas

| # | Idea | Why it matters | Size |
|---|------|----------------|------|
| 1 | **The diff engine's two blind spots: result corrections and team-name changes.** `diffFixtures` compares kickoff, status, venue, roles and time confidence — not `result`, and not `home.name`/`away.name` when the `sourceId` is unchanged. A corrected full-time score (ESPN does amend own-goals and late VAR reversals) or a club rename lands in the snapshot with no report line. Add `RESULT_CHANGED` and `TEAM_RENAMED` kinds (never urgent for a rename; urgent for a result inside the horizon), with tests. | The diff engine is the whole safety story, and "no changes" must mean no changes before anything is built on it — row 2 depends on it. | S |
| 2 | **Auto-merge an *empty* sync report.** *(Annotated 4 Sep 2026: the change-bearing half of this row — a merge verdict, `gh pr merge --auto`, a sticky hold — shipped in v0.2.2 as the urgency gate; what remains here is exactly the `changed=false` stamp-only commit, still behind row 1.)* Once row 1 lands, a run whose report line is `changed=false` is a verified statement that nothing moved — and the only thing the human review of a data PR protects is the change list. Let the workflow, on `changed=false`, commit the stamp-only refresh and squash-merge it itself (`gh pr merge --squash --auto` once the PR's own `verify` run is green — GitHub holds it and the workflow approves it since PR #11; there is no dispatched check any more, and the repo's "Allow auto-merge" setting is off today; the CI-gate ruleset still applies), so `lastSyncAt` advances and the banner stays quiet through a break. Change-bearing runs keep the human. Argued in the proposal as the seam; this row is the work. | Pays off the v0.2.0 price without touching the no-auto-merge decision for changes. | M |
| 3 | ~~**Poster's Tonight's slate and Ledger's Next-up now disagree *live*, once a minute.** `TonightSlate` filters by status and Brooklyn date and never reads `nowUtcIso`, so a `scheduled` fixture stays in "N REMAINING" after its exact kickoff has passed; `NextUpStrip` (via `upcoming`) drops it at the kickoff minute. Pre-existing, but v0.2.0's ticking clock makes the two heroes contradict each other in real time on every match night. Wants a design decision (the Poster template's "remaining" semantics), then either `upcoming`-style gating or a "kicked off" sub-state — this is the same design question as row 4 below.~~ *Shipped in v0.2.2 (`stillToKickOff` + `planSlate` in `src/lib/fixtures.ts`; remaining = not yet kicked off, per the template). Row 4 stays open.* | The app's whole promise is honest time; two heroes with two clocks is a visible lie. | S–M |
| 4 | **FixtureRow's stale-LIVE treatment.** (v0.2.0 row 4, unchanged.) Rows still wear a LIVE pill from a frozen `in_play` forever; the ticker and glow already stop believing it after four hours. Wants a designed state for "kicked off, outcome unknown to this snapshot" — a design brief, not an implementation guess. Resolve together with row 3. | The last surface where the snapshot claims a liveness it doesn't have. | S–M |
| 5 | **A component test rig.** (v0.2.0 row 5.) vitest runs `environment: 'node'` on `tests/**/*.test.ts`; `.tsx` tests are structurally impossible. Two releases of evidence now: the `?date=` crash (v0.1.0) and the pinned-date rollover bug (v0.2.0) both lived in component wiring — the re-anchor effect, `useSyncExternalStore` glue, StrictMode's double subscribe, `useUrlState`'s popstate — and were found only in a browser. A jsdom project + Testing Library beside the node project. *(Now v0.2.3, not v0.2.2 — renumbered 4 Sep 2026, `docs/v0.2.2-proposal.md`; then v0.2.4, the same day, when the Node 24 actions hotfix took v0.2.3 — `CHANGELOG.md` `[0.2.3]`.)* | Every browser-found bug so far lived one module past where the tests stop. | M |
| 6 | **An error boundary.** (v0.2.0 row 2.) Still none; any render throw is a white screen. One boundary above the page with a terse fallback and a reload link. | Cheapest insurance in the codebase. | S |
| 7 | **`useUrlState` hygiene, now with a second reason.** (v0.2.0 row 7.) `set`'s identity churns every render because the codecs are inline closures; v0.2.0's re-anchor effect in `FixturesPage` lists `setAnchor` in its deps, so the effect body runs every render and relies on a `prevToday` ref to no-op — correct, browser-verified, fragile. Stable codec references + normalize-on-load `replaceState` (so `?date=<today>` and `?lens=BROADCAST` stop lying in the address bar) fix both. | Makes the URL contract total and the re-anchor effect run once per rollover. | S |
| 8 | **The last warn-and-continue in the provider.** v0.2.0 made unmapped statuses, the 100-event cap, an implausible shrink and (this review) schema-rejected rows all exit 2 before any write. One path still `console.warn`s and skips: `normalizeEvent` returning `null` (missing competitors or team names). It is counted honestly — the skipped event is not in `counts`, so a mass failure trips the shrink guard — but a single un-normalizable event vanishes silently. Decide whether the doctrine is "every surprise is loud" (then collect and exit 2, like the others) or "counted is enough". Also the `sourceId: String(event.id)` path: an event with no `id` becomes `"undefined"`, which the schema accepts. | Consistency of the loud-failure doctrine now that nobody watches the console. | XS–S |
| 9 | **Period-nav history and the month anchor.** (v0.2.0 row 8.) All date paging is `replace`, so Back after five weeks of paging exits the site; `?view=month` paging anchors to the 1st, so returning to week view loses your place. This review confirmed the v0.2.0 behaviour is *consistent* (a pinned `?date=` survives every rollover, Back after a rollover lands on the new today) — the open question is still push-vs-replace. Found in the v0.2.1 review: the one `push` instance, the tab, has no same-URL guard, so clicking the *active* tab pushes a duplicate entry (`TabNav.tsx:38`); a fix needs a per-call history override plus that guard, and must keep the popstate handler's `initial` fresh (row 7). | Navigation honesty. | S |
| 10 | **Palette-level AA debts.** (v0.2.0 row 6, unchanged — design-system work.) Light floodlight as small text, `--text-muted` on cream, light `--accent` as a 9px fill, competition hexes on dark. The DS wants text-safe variants per theme, on the `--accent-lead-strong` precedent. | Flagged, not repainted; it should stop being a flag. | M |
| 11 | **Ticker niceties.** (v0.2.0 row 9.) NEXT shows a bare Brooklyn time when the next kickoff is tomorrow; the marquee is a fixed 36 s regardless of width; the pause control is inert under reduced-motion. | Polish on the one sanctioned animation. | S |
| 12 | **Theme-flash hardening.** (v0.2.0 row 11.) An inline `<head>` script for a real no-flash guarantee; a `matchMedia('prefers-color-scheme')` change listener. | Small correctness gap. | XS–S |
| 13 | **Small relocations and label truths.** (v0.2.0 row 12, plus two from this review.) `posterDayTitle` belongs in `lib/time.ts`; `N REMAINING` counts TBC fixtures the FIRST/LAST range excludes; `SplitTime`/`SlateTime` assume a space in the formatted time. New: `sync.ts`'s `inWindow` slices the *UTC* date while ESPN's `dates=` range is read in US Eastern, so a kickoff between 00:00 and 04:59 Z at either window edge is in one set and not the other — impossible for a European kickoff today, worth a comment or a `brooklynDate` before a provider that isn't. And the `Default` ruleset on the repo (signatures, linear history, PR required) has an empty branch include, so it applies to nothing — delete it or point it somewhere. | Tidiness that prevents future confusion. | XS |
| 14 | **A portable review command — its name is an open question.** (From the v0.2.1 review, Beni's item; reworded 5 Sep 2026 when the repo skill became `/kickoff-pr-review` in v0.2.4.) The repo skill runs only with Kickoff checked out, is typed by a human, and hunts Kickoff's classes. Beni's stated goal is one review command usable from Claude Code, Claude Design, Cowork and Chat for generic PR/SDLC work — an account-level skill that carries the method (read order, real-numbers baseline, classes chosen by the diff, the one comment, the sealed appendix, the merge gate) and takes the repo-specific hunt classes as an input, with Kickoff's as the first instance. Not a later version of `.claude/skills/kickoff-pr-review/`; a separate deliverable with its own plan. **Naming, Beni's call:** the repo skill now owns a repo-bound name and the `beni-` prefix was dropped for being personal, so the portable command needs a name that is neither — not chosen here. | The method is proven twice; the second repo is where it earns the name. | M |
| 15 | **A docsite.** (Added 4 Sep 2026 by the v0.2.2 scoping; decided and deferred there so it is not re-litigated.) The named default is VitePress — same Vite/TS toolchain, Markdown in — deployed under a path of the Pages workflow v0.2.2 added. **Trigger:** when the reader-facing set (`README.md`, `CONTRIBUTING.md`, `SECURITY.md`, `docs/HONESTY.md`, `docs/ARCHITECTURE.md`, `docs/README.md`) passes roughly six pages, or when it needs search and navigation to be usable. Not before: a docsite before the docs exist is furniture before walls. | The docs are the front door; a site is only worth it once there is something to navigate. | S–M |
| 16 | **A digest of what merged itself.** (From the v0.2.2 review of PR #15.) Under the merge verdict, every non-urgent line lands unread: the real `--check` on 4 Sep 2026 carried 53 DATE_MOVED, 44 TIME_CHANGED and 96 TIME_CONFIDENCE_CHANGED lines at `merge=auto`, all beyond 72 h — exactly where a position placed early lives. `CLAUDE.md` now tells Step 0 to keep re-reading the app for every open position, which is honest but manual. A small follow-up: the workflow appends each auto-merged run's change list to one rolling issue (or a weekly digest), so the betting track can scan a week of unread moves in one place. Not a hold, not a PR — a record. | Restores the human read the old regime gave every line, at digest cost instead of PR cost. | S |
| 17 | **Disarm before the push, not after it.** (v0.2.2 review.) The held-PR disarm now runs before the held `verify` run is approved, which closes the window because GitHub holds the bot's run — but that hold is a repo policy, not a law. If GitHub ever runs the bot's `pull_request` check without approval, an auto-merge armed by an earlier run has the seconds between the force-push and the disarm to fire on urgent changes. The tidy fix: look up the open sync PR and disarm it *before* `git push --force`, when the verdict is `hold`. | Removes the last ordering assumption from the sticky hold. | S |
| 18 | **A held PR should say every reason it has ever been held for.** (v0.2.2 proposal C3's accepted cost, made concrete by its review.) The body is rewritten each run and carries only the latest run's reasons; the label says "flagged", not by which line. Append a "Held since <run>" section that accumulates each hold's reason list across runs (read it back from the current body before rewriting), so an urgent line that aged past the horizon is still on the page a human reads. | The recourse today is the Actions log; the PR body is where the human actually looks. | S |
| 19 | **The Poster day header has the count-vs-range gap the hero just lost.** (Row 13b's other half.) `slateSubLine` now says `N TBC` when the FIRST/LAST range cannot cover the count; `PosterWeek`'s un-exported `subLine` ("15 MATCHES · 5 LEAGUES · FIRST KICKOFF 9:00 AM") still counts placeholders it cannot time. Extract it to `lensSelectors.ts`, add the same segment, test it. | The same honesty rule, one block lower on the same lens. | XS |

## Process notes — what this review taught about verifying a PR

Added to the v0.2.0 list, which still applies (screenshot the whole matrix; assert, don't
admire; 390 is the judge, 360 the jury; absolute words are review bait; test past the
module boundary; latent paths need synthetic data; the hidden-tab transition gotcha).

- **Read the previous run's log before believing a workflow step is reachable.** The
  "Nothing changed" step was unreachable in the shipped `sync.yml`; the proof was the 1 Sep
  dispatch's own log — "no changes" followed by "961 insertions(+), 961 deletions(-)" and a
  PR. A workflow's branches are verified by reading step outputs from a real run, not by
  reading YAML. A `dry_run` dispatch is the workflow's unit test; use it after every change.
- **A test named for a claim must exercise the claim.** "A visibility tick in the same
  minute is a no-op" passed without ever moving the fake clock, so it tested "no time
  passed", not "same minute". Strengthening it to jump 23 s failed on the unfixed store. When
  a comment makes an assertion, the test should be able to fail it.
- **Every `console.warn`-and-continue in an unattended script is a silent path.** Grep
  `scripts/` for `warn` at review time and ask, for each, what a reader who never sees the
  console would want. Three of four became exit 2 across v0.2.0 and its review.
- **Driving the clock in a real browser, the method that works:** inject a `Date` subclass
  carrying an offset through a `<script>` element (page world), `import()` the app's clock
  module by the exact URL Vite served it under (`performance.getEntriesByType('resource')`;
  check `subscriberCount() > 0` to prove you hold the app's instance, not a fresh one),
  set the offset, dispatch `focus` as the catch-up signal. Exercise URL state with
  `history.replaceState` + a synthetic `PopStateEvent` instead of navigating, so the shim
  survives.
- **The Browser pane can be hidden while your script runs, and `requestAnimationFrame`
  never fires in a hidden document.** A wait built on rAF hangs to the tool's timeout and
  looks like a stuck renderer. Settle React with `MessageChannel` ticks (its own scheduler
  primitive, unthrottled) and measure with `scrollWidth`, which lays out on demand whether
  or not anything paints.
- **The merge box counts only the PR's own event runs.** A `workflow_dispatch` check-run on
  the head SHA — attached, green, associated with the PR in the REST API — never appears in
  `statusCheckRollup` and never unblocks the merge (PR #7, PR #9 twice). The bot's PR does
  trigger `pull_request`, GitHub just holds the run for approval; the fork-PR approval policy
  setting made no difference; approving the held run through the Actions API with the
  workflow's own token works in thirty seconds. Test the merge box itself, hands off, before
  believing any theory about check-runs — the first theory shipped (PR #10) was wrong.
- **A machine-readable line beats parsed prose, and the line's format is an API.** Pin it
  verbatim in a test, validate it with the same regex in the workflow, and fail loudly when
  it is missing — never fall through to a guess.
- **Name the consequence of a fix in the spec of record.** Gating commits on the report
  changed what the staleness banner means; the proposal and `CLAUDE.md` say so, and this
  list carries the follow-up. A fix whose side effect is undocumented is half a fix.
- **A skill cannot review itself from the session that built it, and a prompt cannot invoke
  it either.** `disable-model-invocation: true` hides a skill from the model — a prompt that
  says "invoke `/x`" cannot make the session load it; a human types the command. And a
  `.claude/skills/` directory that did not exist when the session started is not watched, so
  a fresh worktree of a branch that adds the first skill needs the session started *after* the
  checkout. Both were "Unknown skill" in the v0.2.1 review; a probe skill without the flag
  failed the same way. Test invocation the only way it can be tested: a human, a new session.
- **A dated environment fact in a method file is a lie in waiting.** "`vitest` ran inside the
  sandbox on 3 Sep" was false the same evening (the worktree resolves `node_modules` upward
  into the main checkout, and the sandbox's write allowlist did not cover it); "ten runs" was
  twelve by nightfall. Write the condition and the command that tests it; never the count or
  the date.
- **A PR body's SHA must exist on GitHub.** An amended commit's pre-amend hash lives only in
  the local reflog; `git branch --contains <sha>` empty means the reference is dead for every
  reader but the author. Check every SHA a body cites the same way, and recompute the diff
  numbers with `gh pr view --json additions,deletions,changedFiles` — never read them through a
  summarizer.
- **Merge `origin/main` into the branch before the smoke pass; do not rebase.** A rebase
  rewrites every SHA the proposal and PR body cite; a merge keeps them and the squash erases
  the merge. The header stamp and fixture count you verify are then the ones that land.
- **Read the appendix last.** The classes a build's own review lost are the ones to read cold
  first; opening the sealed list before that turns the cold read into a checklist.

Added by the v0.2.2 review (PR #15, 4 Sep 2026):

- **Run a workflow step's own text under a fake `gh` before believing its failure paths.**
  Extract the `run:` block from the YAML (an indentation parser is twenty lines; no YAML
  library is in `node_modules`), substitute `${{ steps.x.outputs.y }}` with env vars, and
  run it under `bash -eo pipefail` — GitHub's shell — with a `gh` shim on `PATH` that logs
  every call and answers from a scenario. Two shapes fail open every time: `cmd || true` and
  `cmd | grep -q`; both turn an API failure into the safe-looking answer. The v0.2.2 "Obey"
  step had one of each and had never executed in any run (the dispatch was a dry run).
- **Prove a GitHub setting with a probe PR whose required check is deliberately red.** A
  BLOCKED PR is the only safe target for `gh pr merge --auto`: gh 2.92.0 merges a CLEAN PR
  *immediately* under `--auto` (`isImmediatelyMergeable` in its source), so running it
  against a green PR "to see what happens" lands the PR. Read the installed gh's source
  (`raw.githubusercontent.com/cli/cli/v<version>/pkg/cmd/pr/merge/merge.go`), not the
  manual. Close the probe, delete its branch, say so in the PR body.
- **Headless Chrome through CDP beats the pane for anything that must happen before the
  app's module runs.** Node 24's global `WebSocket` needs no dependency;
  `Page.addScriptToEvaluateOnNewDocument` installs the `Date` shim and a `data-lens`
  observer before the first script — but `<html>` does not exist yet at that moment, so
  observe `document` with `subtree: true`, and install the shim first in its own IIFE so a
  later throw cannot take it down. 48 cells with a fresh load each ran in about ninety
  seconds against the dev server.
- **The sandboxed and unsandboxed shells have different `$TMPDIR`s.** A file written by one
  is invisible to the other; write to the scratchpad path by name.
- **A `--check` against a branch's snapshot is not the dispatch's number.** The dispatch
  diffed the freshly checked-out branch; a day later the same branch's snapshot was 195
  lines behind ESPN (the bot's PR #16 had not merged). Compare a report line only with a run
  from the same snapshot, and read the kinds, not the count: 53 DATE_MOVED at `merge=auto`
  is the contract question, not a number to admire.
- **A contract sentence read by another repo is a test with no runner.** "Carried nothing
  inside 72 hours" was falsified by one line of `diff.ts` (`NEW` is `urgent: false` by
  construction). When a doc states what a verdict guarantees, derive the sentence from the
  verdict's code paths, then list what the verdict does *not* see.

