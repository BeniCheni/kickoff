# v0.2.6 — candidate work after the resilience patch

Written cold, 5 Sep 2026, by Codex after implementing v0.2.5. This is a ranked candidate
list, **not a release scope**. The inherited spec is `v0.2.5-proposal.md`; its atomic snapshot
boundary and accepted costs are decisions, not invitations to reconsider them in a small fix.
Beni kept the resilience patch whole. The design-cycle scope and its numbering decision
are recorded in `docs/design-cycle-proposal.md`; this list does not assign a release number.

CONFIRMED: the current code and this build's tests establish the completed work below.
The two new provider candidates were reproduced with modified audit payloads; that proves
the mapper's behavior, not their occurrence in today's provider feed. Rows marked
**inherited** carry an earlier review's rationale, with their originating file named; they
need a fresh scope-time verification. **ESTIMATE:** ranking and all sizes (XS < an hour,
S < half a day, M roughly a day or two) are judgment, not promises. Verification can dominate
an XS edit, as this release's asynchronous sync proof and browser matrix demonstrate.

## Ranked ideas

Original row numbers below refer to **`docs/v0.3.0-ideas.md`**, unless another file is named.
Rows 1, 2, 16, 17 and 18 in that file remain the approved v0.4.0 group. The parallel design
brief owns stale-LIVE, palette work and the two remaining ticker changes. Newly ranked work
here does not silently enter either scope.

| # | Idea | Why it matters | Size |
|---|------|----------------|------|
| 1 | **Missing time confidence must not imply certainty.** (New v0.2.5 audit candidate.) `normalizeEvent` uses `timeValid === false ? 'round_placeholder' : 'exact'`; an absent flag therefore means exact. Decide the treatment of absent or malformed confidence using provider evidence, then test each input shape and its rendered consequences. Explicitly deferred by Beni from v0.2.5. | CONFIRMED mapper path: absence of evidence currently becomes a confident clock. Occurrence in live payloads remains VERIFY. | S |
| 2 | **Missing result scores must not become zero.** (New v0.2.5 audit candidate.) `Number(null)` and `Number('')` become zero; null full-time scores reproduced a schema-valid 0–0. Validate presence/type before numeric conversion and define how incomplete results fail. Explicitly deferred by Beni from v0.2.5. | CONFIRMED mapper path: an unknown score can look like a settled result. Occurrence in live payloads remains VERIFY. | S |
| 3 | **Result corrections and team renames in the diff.** (Original row 1.) `diffFixtures` still omits result comparisons and name-only changes when identity stays the same. Add `RESULT_CHANGED` and `TEAM_RENAMED`, with an explicit urgency policy and tests. | CONFIRMED omission: row 4 cannot call an empty report complete while these are invisible. v0.4.0. | S |
| 4 | **Stamp-only publication after an empty report.** (Original row 2, remaining half.) Advance the verified snapshot stamp on `changed=false` after row 3 closes the blind spots. Keep the existing change-bearing urgency gate. Recheck current GitHub settings and the PR's own required check before changing workflow behavior. | CONFIRMED workflow behavior: quiet runs leave the last change-bearing stamp. Inherited proposal to pay off that accepted staleness cost; v0.4.0. | M |
| 5 | ~~**Disarm a held sync PR before pushing its next tree.**~~ (Original row 17.) **Overtaken by PR #28 (6 Sep 2026):** no PR is held, so there is no sticky hold to protect. The disarm step survives only to narrow the window in which an older auto-merge request could act on a new head, and PR #29's review made its two readbacks non-fatal. Retire the step, or keep it and say what it is for. | The premise — a hold that must survive an approval-policy change — no longer exists. | S |
| 6 | ~~**Keep every reason a PR has been held.**~~ (Original row 18.) **Overtaken by PR #28 (6 Sep 2026):** nothing is held, so reasons never need to survive across runs for a human who is waiting. Row 7 is the live version of this need. | Superseded, not done. | S |
| 7 | **A digest of changes that merged without a read.** (Original row 16.) Inherited: append auto-merged change lists to a rolling issue or digest. Decide delivery and retention explicitly. | Restores visibility for open positions beyond the urgent horizon; v0.4.0. | S |
| 8 | **FixtureRow's stale-LIVE treatment.** (Original row 4; `docs/v0.2.0-ideas.md` row 4.) Inherited design work for “kicked off, outcome unknown to this snapshot.” The hero's remaining-count fix already shipped. | A snapshot cannot claim continuous liveness. Implemented on the design-cycle branch; review/merge pending as of 6 Sep 2026. `docs/design-cycle-proposal.md` records the KICKED OFF state. | S–M |
| 9 | **Text-safe palette variants in both themes.** (Original row 10; `docs/v0.2.0-ideas.md` row 6.) Inherited light floodlight, muted text, small accent fills and dark competition-color debts. | Semantic text/pill tokens implemented on the design-cycle branch; review/merge pending as of 6 Sep 2026. Competition hex variants remain a separate design-system pass. | M |
| 10 | **Marquee states, speed and reduced-motion pause control.** (Original `docs/v0.3.0-ideas.md` row 11.) Empty and approximate NEXT states are implemented on the design-cycle branch; review/merge pending as of 6 Sep 2026. The static empty state has no pause button. NEXT’s later-day qualifier already shipped in v0.2.5. | The width-independent 36 s duration and the nonempty reduced-motion track’s inert pause control remain open. The governing design spec did not define them; `docs/design-cycle-proposal.md` records the narrow exception for the empty state. | S |
| 11 | **Period navigation history and month anchor.** (Original row 9; `docs/v0.2.0-ideas.md` row 8.) Date navigation still replaces history; monthly paging anchors to the first. The active tab still pushes a duplicate URL. Decide push/replace per action and add a same-URL guard. | CONFIRMED current wiring. Stable codecs and fresh popstate defaults now exist, but this navigation policy was deliberately left alone. | S |
| 12 | **A portable review command.** (Original row 14.) Inherited separate deliverable, not a later version of `/kickoff-pr-review`. Take repo-specific hunt classes as inputs; Beni chooses its name. | The method can travel without making Kickoff's classes universal. | M |
| 13 | **A docsite when navigation warrants it.** (Original row 15.) Inherited VitePress default and roughly-six-reader-pages/search trigger; re-evaluate the actual reader-facing set before building. | Documentation infrastructure follows reader need. | S–M |
| 14 | **Repository Default ruleset.** (Owner remainder of original row 13.) VERIFY: the inherited report says its branch include is empty. No repository settings inspection or modification was performed for this release. Beni owns this act. | Keep a repository-settings decision separate from app and sync code. | XS |
| 15 | **In-product failed-sync reader — deferred with a trigger.** Beni adjudicated on 5 Sep 2026: the authoritative boundary ships as built; the reader is the procedure owned by `../Sportsbooks/CLAUDE.md` ("Check the sync run succeeded BEFORE trusting the app"). | Adjudication baseline: 30 `sync.yml` runs since 1 Sep, zero failures. Reopen when observed sync failures recur often enough that the procedural reader is inadequate; the first observed failure prompts a rate/operational-burden review, not an automatic UI build. No numeric failure-rate threshold has been approved. | S |
| 16 | ~~**Team identities through `providerIdentity`.**~~ (New review finding, fixed in PR #23 Pass 2.) Both ESPN competitor IDs are now required and validated before coercion/deduplication. Missing or malformed IDs reject the event and therefore the snapshot; diagnostics name the role and raw ID. The persisted schema remains compatible with older snapshots. | Pass 2 disproved "can only miss": JSON `["1068"]` falsely joined row `"1068"`, inventing form/next-fixture data. This was a gap in the boundary, despite event IDs being the explicit example in `docs/v0.3.0-ideas.md` row 8. Tested at the mapper, duplicate-event collection and table join. | XS |
| 17 | ~~**`CONTRIBUTING.md` and `CLAUDE.md` still describe an all-Claude build.** (New, from the PR #24 review, 5 Sep 2026.) PR #24 documented in README that the SDLC now routes implementation across Claude Code and Codex per task shape (Beni's own account, recorded in that PR's review thread). `CONTRIBUTING.md`'s "Why the commits are authored by Claude" section and `CLAUDE.md`'s "How this repo gets built" ladder / CEO-Designer-CTO-PM-TPM role list are unchanged and now describe a narrower process than README does.~~ **Closed by the UCL design-cycle Round 1 review, PR #34 (8 Sep 2026):** `CONTRIBUTING.md`'s authorship section and `CLAUDE.md`'s role list now name both builders and point at `README.md`'s "Two builders, one repo" table as the owning copy of the routing, rather than restating it. | Three governing docs should agree about who builds this repo; a cold reader of any one alone currently gets a different picture than the other two. Deliberately left out of PR #24's scope (Beni asked only for README). | S |
| 18 | ~~**Empty-NEXT provenance beside LIVE/FT segments.** (New, PR #26 Pass 1 finding 1; pending Beni's adjudication.) `TickerStrip` gates the static empty line on `segments.length === 0`; the design spec defines empty on NEXT alone ("no scheduled fixture after now, exact or placeholder"). On the tail of the sync window, a day with FT results and nothing scheduled after renders scores and no `window ends …` line (reproduced: 20 FT segments, no provenance). If the spec's definition is upheld: emit the empty NEXT as a segment (a `windowTo` argument to `tickerSegments`, or a pure `withEmptyNext`) and keep the static rendering for the only-segment case.~~ **Implemented in PR #26 Pass 2, 6 Sep 2026; merge pending.** Emit empty NEXT with snapshot-window provenance beside LIVE/FT; render statically only when it is the sole segment. | The window end is the provenance the state exists to show; of the spec's two named occurrences, the window tail is the one that carries FT. | XS |
| 19 | ~~**One "next" for the marquee and the glow.** (New, PR #26 Pass 1 finding 2; an accepted cost in `docs/design-cycle-proposal.md`.) `tickerSegments` picks NEXT within the earliest eligible Brooklyn date; `hotFixtureIds` still takes `nextKickoffId` over all fixtures. With an all-placeholder earliest date the strip says `NEXT Sat 10 Oct · TBC · 23 kickoffs` while Sunday's first exact row glows (reproduced in Broadcast, both themes). Decide whether the glow follows the marquee's day (nothing glows on an all-TBC day) or stays exact-only, and derive both from one selector.~~ **Implemented in PR #26 Pass 2, 6 Sep 2026; merge pending.** Marquee and glow share `nextMatchdaySelection`; an all-TBC earliest date has no next-kickoff glow. | Two instruments naming different "next" fixtures on one screen is the disagreement v0.2.2 removed between the heroes. | S |
| 20 | ~~**The still-to-kick-off gate exists twice.** (New, PR #26 Pass 1.) `src/lib/fixtures.ts` `stillToKickOff` says "two copies of one gate would be the same bug in disguise"; `tickerSegments` in `lensSelectors.ts` now carries an inline copy (`scheduled && date ≥ today && (placeholder || kickoff > now)`) because the pure module cannot import the snapshot module. Move the predicate into `lensSelectors.ts` and re-export it from `fixtures.ts`.~~ **Implemented in PR #26 Pass 2, 6 Sep 2026; merge pending.** Move `stillToKickOff` into the pure selector module and re-export it from `fixtures.ts`; no cycle. | The heroes and the marquee should retire a fixture on one rule; the shared predicate now makes that agreement true by construction. | XS |
| 21 | ~~**The retired standings soft-failure is still described in three places.** (New, PR #26 Pass 1; the v0.2.5 sweep reached `CLAUDE.md` and the review skill only.) `.github/workflows/sync.yml`'s header comment lists "a failed standings fetch" as a hold reason and its report step keeps a `standings=failed` warning branch; `scripts/diff.ts`'s `mergeVerdict` doc comment lists it as a live reason. All three are unreachable since v0.2.5. The regex and the defensive branch may stay; the prose should say "legacy".~~ **Implemented in PR #26 Pass 2, 6 Sep 2026; merge pending.** Finish the legacy-only prose in the workflow header, warning and diff-policy comment; runtime gates stay unchanged. | Completes the same authoritative-snapshot doctrine in every current description. | XS |
| 22 | ~~**`tests/contrast.ts` restates three alphas and skips one ground silently.** (New, PR #26 Pass 1.) The FT fill (0.12/0.14), the dark 24h tint (0.14) and the 72h tint (0.10) are typed into the test rather than parsed from `--border`, `--floodlight-bg` and the component class, and the dark loop omits floodlight-bg without saying why — `text-muted` there would read 4.43:1 (nothing renders muted on that tint today; checked by inventory). Parse the token alphas; state the exclusion, or assert the inventory that justifies it. Tailwind v4 emits `bg-accent/10` as an oklab `color-mix`; the rendered 72h pixel measured 4.53:1 against the 4.55 sRGB prediction.~~ **Implemented in PR #26 Pass 2, 6 Sep 2026; merge pending.** Parse the token and component alphas, guard every floodlight-tint source site and mount its text inventory; rendered pixels arbitrate thin margins. | Source-derived alphas and a guarded inventory close the drift gap in the existing live assertions. | XS |
| 23 | **Hollow-family semantics — design system.** Owner: Claude Design (designer). Reopen the design spec's three-shape grammar: hollow now means unresolved or terminal, since CANCELLED joined KICKED OFF and POSTPONED. Compare keeping that family with a third treatment for CANCELLED. Beni approved shipping the current pixels in v0.3.0; this is the designer's next grammar decision. | The 15% CANCELLED tint failed at 4.29:1 even with the new token; labels distinguish the statuses beyond hue, but the shared hollow shape widens the meaning of “declines to assert.” | XS–S |
| 24 | **A per-PR pause that survives the next run.** (New, PR #29 review, 6 Sep 2026.) Since PR #28 there is no way to stop one sync PR: the label is dead, and closing the PR only buys a cron interval — the next run re-runs the sync against `main`, finds the same diff, and opens a new PR that merges itself. The only stops are repo-level (turn off "Allow auto-merge", disable `sync.yml`), which stop *every* PR, not the one in question. Options worth costing: a tracked `sync/paused` marker file the workflow reads before arming; a `paused` repo variable; or accepting the repo-level stop and saying so in the app. | The v0.2.2 hold was removed because it stalled routine data; the answer to "this specific snapshot is wrong" was removed with it, and that is the case where money is on the table. Row 7's digest is the after-the-fact half of the same gap. | S–M |
| 25 | **The Fixtures lens's third state for `scheduled` rows.** (New, PR #30 review, 7 Sep 2026.) **Pass 2.5 (7 Sep 2026): deferred; confirmed in the browser at the head — the Fixtures row for Getafe–Celta read a bare "1:00 PM" three hours after kickoff while the Table beside it said KICKED OFF. Designer's row, not #30's.** v0.3.0's hollow KICKED OFF pill fires only for `in_play` past `LIVE_WINDOW_MS`; a `scheduled` row whose league-set kickoff has passed still shows its kickoff time with no pill in all three lenses, while the Table (since #30) says KICKED OFF for the same match and the heroes drop it from REMAINING without a word. It is the design brief's state 3 — kicked off, outcome unknown to this snapshot — reached by the clock rather than by a frozen status. Decide whether `FixtureRow` wears the same pill from the kickoff minute, and whether the four-hour window has any meaning for it. Owner: Claude Design for the pixels; the selector is `hasKickedOff`. | Two surfaces on one page disagreeing about the same match is the class #30 fixed; the Table now names the state and the row beside it does not. | S |
| 26 | **What the Table's NEXT lane shows mid-match — Beni's call.** (New, PR #30 review.) **Pass 2.5 (7 Sep 2026): both vendors converge — underway-first ships in #30 as the one-fixture invariant; "Both, next first" is the preferred end state (Codex's sizing re-measured: 5.7 px mono advance, 120 px lane, 773 px grid in a 740 px container at 1000 px) and needs a design pass, implementation, regression coverage and a fresh review; Beni rules now-or-later.** The review made the match underway outrank the following fixture in every layout, so opponent and marker never come from different matches; the column is still headed *Next*, and the club's genuine next fixture is invisible for the match's duration plus however long the snapshot stays unresolved. Options: keep underway-first (current); show the next fixture with a one-line "kicked off" note for today's; show both, which at the 120 px desktop lane needs a design pass. Record the ruling in the code comment above `matchLane`. | The precedence is a choice dressed as a fix until Beni rules on it; a Step 0 reader looking for Sunday's kickoff on Monday afternoon does not find it in this column. | XS–S |
| 27 | **A formatter, or nothing and say so.** (New, PR #30 review.) **Pass 2.5 (7 Sep 2026): deferred; #30 lands without a formatter, the two mis-indents fixed on the branch.** `verify` runs typecheck, tests and build; nothing checks whitespace, and #30 landed two mis-indented lines only a human reading the diff would see. Prettier at the repo's existing style, run in `verify`, or a stated decision not to. | Low on its own; a row so the next mis-indent is a decision rather than a surprise. | XS |
| 28 | ~~**`tests/dom/designCycle.test.tsx` fails whenever the real snapshot carries a genuine `in_play` row — and `sync.yml` runs it on every fresh snapshot.** (New, PR #30 Pass 2.5, 7 Sep 2026.) The test mutates one snapshot fixture to `in_play` (line 34) and asserts with an unscoped `getByText('LIVE')` (lines 49, 52); a snapshot that already holds a live match renders two pills and the query throws "Found multiple elements". The 20:00Z scheduled sync (run 34157750491) fetched cleanly — `report: changed=true changes=4 urgent=4 standings=changed rank-moves=8 merge=hold` — then failed the "Verify the written snapshot" step exactly this way: six tests red, no commit, no PR, `main`'s snapshot left at 00:02Z through all of Monday's matches, production showing the very bug #30 fixes. First failure in the 39 `sync.yml` runs GitHub lists; it recurs on every cron slot that coincides with a live match, which is most weekend slots. Reproduce without the sync: set one `scheduled` fixture to `in_play` in an uncommitted copy of `src/data/fixtures.json`, `npx vitest run tests/dom/designCycle.test.tsx` (6 failed / 3 passed on 7 Sep), revert. Fix: scope every LIVE / KICKED OFF query to the mutated fixture's row with `within`, or mock `FIXTURES` as `tableUnderway.test.tsx` does; add the case where the snapshot already carries an `in_play` row; strike "the snapshot holds zero … in-play fixtures" from `browser-matrix.md` and SKILL §4. Own PR, one file; land before Sat 12 Sep.~~ **Shipped in v0.3.2 (PR #33, 9 Sep 2026)** | The repo's required verify blocks its own data during the only hours the data matters, and the reader procedure in `../Sportsbooks/CLAUDE.md` has its first live case. | XS |
| 29 | **Auto-merged sync PRs never reach Pages.** (New, PR #30 Pass 3, 8 Sep 2026.) `pages.yml` deploys on `push` to `main`, and PR #31 — the first sync PR the bot merged itself, `64fca44` at 00:31Z on 8 Sep — produced no Pages run at all: GitHub does not create workflow runs for pushes made with the workflow's own `GITHUB_TOKEN`, so every auto-merge since #28 lands on `main` and never deploys, while `pages.yml`'s own comment still promises the demo "is redeployed within minutes of the data landing". Human merges (#27, #28, #29) deployed as normal. `workflow_dispatch` and `repository_dispatch` are the documented exceptions to that rule, so `sync.yml` can dispatch `pages.yml` itself (it already holds `actions: write`) once it observes the merge, or `pages.yml` can gain a `schedule` an hour behind each sync slot that deploys `main` when the deployed SHA differs. Until fixed: `gh workflow run pages.yml` by hand after any auto-merge. | The #28 design's whole purpose was data reaching the demo without a human; it reaches `main` and stops one step short, and nothing in the app or the Actions tab says so. | S |
| 30 | ~~**ESPN publishes the three UEFA club competitions; the canvas's F1 was never established.** (Round 1 finding, corrected by Round 2, 9 Sep 2026.) The supplied brief marked provider availability ESTIMATE; Beni says the attributed earlier curl was never run. Own Round 2 probes: UCL Sep 8–11, UEL Sep 16–17 and UECL Oct 15 each return 18 events; all three season-2026 standings return 36 League Phase entries. UCL normalizes 18/18 fixtures and 36/36 standings, with 21 existing team IDs. Actual phase path is `events[].season.slug`, not `season.type.slug`; `week` is absent and notes empty on all 18 UCL events. Config entries enable fetching but are **not the complete safe activation path**: row 42's venue clocks and matchday/progress work remain. Five uncapped slices contain 144 unique UCL events; the maximum observed 28-day slice is **54**, not 36, still below the observed 100 cap.~~ **Implemented in #39; release pending.** | Correct unsupported provenance as well as the negative conclusion. Provider choice (a) stays open; full receipts and limits: `design-cycle-ucl-review-pass-2.md`. | S |
| 31 | ~~**`src/lib/competitions.ts` lines 4–5 are false.** (New, Round 1 review.) The header says competitions without an `espnCode` "carry hand-authored placeholder rows"; `src/data/fixtures.json` at `0c37716` holds zero rows for `ucl`, `uel`, `uecl`, `supercoppa` and `supercopa`. It is the one sentence in the repo that reads as a licence to hand-author fixtures against rule 1. Under `src/`, so out of the design-cycle PR's reach; fix with row 30's `espnCode` change or on its own.~~ **Implemented in #39; release pending.** | A comment that describes a practice the repo forbids is a provenance bug in prose. | XS |
| 32 | ~~**The Table's per-row wrapper defeats the scoped sticky divider; markup is not universally required.** (Round 1 finding, corrected by Round 2, 9 Sep 2026.) Real wrapper 81.5 px: nested top at offsets 100/200/300 is −18/−118/−218; hoisted stays 28 at 390/375/360. A synthetic grouped sixteen-row band (891.5 px) stays at 28 then releases at its end. A **CSS-only** counterexample sets wrappers to `display:contents` and every divider sticky/opaque; later dividers cover earlier ones. It disproves the claim that both alternatives require markup, but changes the canvas's >8-only rule and domestic behavior. Desktop's 34 px header is static.~~ **Implemented in #39; release pending.** | Designer must settle scoped band containment and item (g), without treating the counterexample as approval to change the rule. | S |
| 33 | ~~**Three tabs stack at all phone widths, but add height only at 390/375.** (Round 1 finding, corrected by Round 2, 9 Sep 2026.) Tabs 193.80 + gap 16 + switcher 201.87 = 411.67 px; content widths 350/335/320. Nav grows **37.5→65 px at 390/375**, but is already **65→65 at 360**. Canvas's own three-tab boards render the stacked state; its caption understates the threshold.~~ **Implemented in #39; release pending.** | Designer chooses the layout; do not repeat a 27 px push at 360, where the two-tab nav already stacks. | S |
| 34 | ~~**The sixth chip adds a third row at every phone width in actual config order.** (Round 1 finding, corrected by Round 2, 9 Sep 2026.) Cloned Champions League chip is 144.86 px. When appended as drawn, picker heights are 62.5/62.5/96.75 at 390/375/360. When prepended as `COMPETITION_KEYS` orders it, all three are **96.75**, versus five-chip baseline 62.5. `TABLE_LEAGUES` and `parseLeague` use that supported key set.~~ **Implemented in #39; release pending.** | Designer chooses ordering and layout; the canvas's sixth position is not the existing code's order. | XS |
| 35 | **Competition-name contrast must be checked against each component's actual ground.** (Round 1 finding, qualified by Round 2, 9 Sep 2026.) UCL on cream/white/night/dark-hot is 3.477/3.793/4.631/4.010. Broadcast's small text is 10.5 px, other lenses 11; Broadcast hot rows paint `--surface` (white or #142a1f). All five domestic hues fail cream, selected-chip white text, and dark-hot grounds (Serie A 4.490 still fails). Several of the fourteen cup hues pass cream, so “every light hue” is too broad. Full fourteen-hue, four-ground table in Round 2. | Existing debt requires a component-ground inventory for the design-system patch; it is not an automatic exemption for new text surfaces. No token repaint in this cycle. | S–M |
| 36 | ~~**A tie moved into another matchday window is misnumbered silently.** (Round 1 finding, reproduced by Round 2, 9 Sep 2026.) Same event moved Sep 9→Sep 30 yields unknown, but Sep 9→Oct 13 yields MD2. Five uncapped ESPN slices show 144 unique events, 18 in each supplied window and zero outside; this corroborates current clusters, not original-round identity or UEFA's exception policy. Ordered/disjoint/season checks and outside counts all miss an in-window transfer. Existing event-identity kickoff diffs could trigger review; baseline/exception handling needs a ruling, including already-moved events on initial fetch.~~ **Implemented in #39; release pending.** | Eight windows remain ESTIMATE with named brief provenance; Round 3 must supply a contradictory signal or an explicit uncertainty policy. No resolver or policy chosen here. | S |
| 37 | ~~**The canvas's media ratio is typed and wrong.** (Round 1 finding, reproduced by Round 2, 9 Sep 2026.) Canvas 1h line 643 prints 1.14; #e4e0d2 on #efead9 computes **1.097506→1.10**. `renderVals()` has no media calculation. A purely decorative background need not meet non-text contrast, but an essential boundary or state cue does; non-text alone is not an exemption.~~ **Implemented in #39; release pending.** | Designer should compute or remove the number; no media token change here. | XS |
| 38 | ~~**The canvas resolves its own not-decided item (c) in body text.** (New, Round 1 review.) 1g's footer states Moments "live in `src/data/moments.json`" while 1h lists where the file lives as undecided; `src/data/` is the folder rule 1 reserves for `npm run sync`. Load-bearing under rule 1, and the design should say one thing. Round 3's ruling; the canvas text should not pre-empt it.~~ **Implemented in #39; release pending.** | An undecided item that one artboard has already decided is a spec that will be built two ways. | XS |
| 39 | ~~**Provider phase/zone fields offer checks, but presence does not prove usable semantics.** (Round 1 finding, corrected by Round 2, 9 Sep 2026.) `events[].season.slug` is `league-phase`; league `season.type.name` is League Phase. Neither `season.type.slug` path exists. Single-date calendar has six coarse phase periods, range calendar is empty; neither gives eight matchdays. `note.description` supplies 8/8/8/12 rank bands. `advanced` is zero on all 36 rows and does not distinguish qualifications; `ppg` is also all zero, including six winners with true PPG 3.00. Normalizer reads nine of fourteen stats. Four bands split the middle sixteen but retain a twelve-row band.~~ **Implemented in #39; release pending.** | Reference-data derivation and zone design remain Round 3 choices. Do not adopt provider fields solely because their names match app concepts. | S |
| 40 | **A `/kickoff-design-review` repo skill.** (Round 1 retro, extended by Round 2, 9 Sep 2026.) Artifact serving, live prop/cold arithmetic checks, mechanism-driven browser probes, current-head recount and explicit routed-evidence status need a home outside code-diff review. Rebuttal additionally needs open prior findings, provenance traced back to the brief/human, counterexamples that distinguish an absolute claim from its scoped design requirement, real source order vs illustrated order, normalization plus semantic/integration probes, restoration proof, actual viewport identity, and downstream corrections preserving history. | Both review documents and their retros are raw material; this pass did not find or create an installed `/kickoff-design-review` skill. Beni owns commissioning it. | M |
| 41 | ~~**PPG advice disagreement remains for Round 3; contradiction is not established.** (Round 1 finding contested by Round 2, 9 Sep 2026.) Real and synthetic mid-matchday data produce 24 clubs in hand. PPG normalizes games played but does not normalize opponent strength, so “sort by PPG” and “weaker comparator against different opponents” can coexist. Provider `ppg=0` for all 36 includes six wrong winner values; the app's points/played calculation remains useful. F2's denominator eight is separate. Today's feed does not prove the provider child stays frozen after future MD8.~~ **Implemented in #39; release pending.** | No mandatory suppression/rewording inferred. Designer may improve the combined advice after Beni adjudicates the contested characterisation. | XS |
| 42 | ~~**Resolve venue time zones before activating UCL.** (New high build finding 18, Round 2, 9 Sep 2026.) `normalizeEvent` assigns the competition's Europe/Zurich to every venue. Passing real MD1 events through `fixtureTimes` gives wrong stadium-local clocks for **6/18**: the app is one hour early for Athens/Istanbul and one hour late for Porto/Lisbon/Liverpool/Manchester. Anfield UTC19:00 renders local21:00 instead of20:00; Brooklyn15:00 is correct. Existing schema accepts the valid but wrong zone.~~ **Implemented in #39; release pending.** | A gap in row 30's proposed two-config activation path, not a deployed UCL regression (current UCL rows zero). Round 3 needs venue-to-IANA provenance and honest missing/conflicting-venue behavior; no src change in this design pass. | S–M |

## Completed rows, retained in place

The original list also carries these strikes, so old row citations remain useful. “Completed”
means implemented and verified on this release branch; Beni's merge and tag are separate acts.

| Original row | Work | Disposition |
|---|---|---|
| 3 | ~~Poster remaining semantics~~ | Shipped in v0.2.2; original hero assertions retained. |
| 5 | ~~Component test rig~~ | Shipped in v0.2.4; extended for matchMedia changes. |
| 6 | ~~Error boundary~~ | v0.2.5: keyed App seam, Reload and Reset view; pre-mount module failures remain outside it. |
| 7 | ~~URL hygiene~~ | v0.2.5: stable codecs, fresh effect-event popstate handler, load normalization and hash preservation. Today's date unpins on arrival. |
| 8 | ~~Last silent normalization path and provider identity~~ | v0.2.5: fixtures + standings fetch/validate together; either failure exits 2 before writes, with diagnostics. Counts do not excuse rejected entries. |
| 11, one third | ~~NEXT day qualifier~~ | v0.2.5: later Brooklyn day includes weekday and date. Row 10 above retains the two design-owned thirds. |
| 12 | ~~Theme flash and OS-change listener~~ | v0.2.5: generated classic head script from shared code; stored/session choices win. |
| 13a–d | ~~Title relocation, hero TBC count, structured hero time, Brooklyn window edges~~ | 13b already shipped in v0.2.2. v0.2.5 completes a/c/d; `inWindow` stays in `scripts/window.ts`. The owner ruleset act is row 14 above. |
| 19 | ~~Poster day-header count versus time~~ | v0.2.5: extracted `posterSubLine` names `N TBC`, with pure and component tests. |

## Process notes

Read **all** the process-note tails in `docs/v0.3.0-ideas.md` and
`docs/v0.2.0-ideas.md` as well; this file adds method instead of replacing it.

- **Define the authoritative snapshot boundary before promising atomicity.** Fetch and
  validate every member before any write. Here the members are fixtures + standings;
  metadata describes that snapshot. The Git commit publishes them together. This is not a
  multi-file filesystem transaction, and future ancillary datasets need a separate membership
  decision. Test failure before writes by invoking the real orchestrator with intercepted IO;
  also pin the actual CLI's exit code in a subprocess.
- **Expected counts do not excuse a rejected entry.** Add a malformed extra entry to an
  otherwise complete standings table. A minimum-count-only test cannot see that loss.
  Validate identity before coercion and deduplication; otherwise two missing identities can
  collapse before the schema sees either one. Diagnostics need the available identity and
  context, not a reconstructed one.
- **A generated bootstrap needs two proofs.** Execute the compiled output against the shared
  decision across storage/OS/lens combinations, then serve `dist/` and hold the application
  module at the network layer. Inspect root attributes and computed background while React's
  root is empty, release the module and compare again. A dev-server load does not establish
  production ordering. Reuse the existing build dependency; avoid a second hand-maintained
  decision string.
- **An error boundary's claim must name its seam.** Inject a throw into a served component
  response in an isolated browser, without changing snapshots or shipping a crash switch.
  Check tab and lens remounts, Reload's actual navigation, and Reset view's cleared URL in
  all six lens/theme combinations. A module-import validation error before React mounts is
  outside this boundary. Assert against the real markup: this Table uses divs, not `<table>`.
- **A pin regression must start from a nondefault pin.** Once the product normalizes
  `?date=<today>` away on arrival, that URL cannot establish the old test's persistent pin.
  Start on the preceding day, then tick through the pinned day and the following midnight.
  Keep the pin assertions; separately prove today's unpinning and a Sunday-to-Monday follow.
- **Browser assertions need the running app's values.** Read competition keys instead of
  guessing an abbreviation. Import the exact served clock URL and verify positive subscriber
  count before driving focus catch-up. Keep viewport-before-capture and width assertions on
  auxiliary captures too. Inspect the whole matrix, then scroll to changed content hidden
  below a long hero; the first viewport does not prove a day-header sub-line.
- **Run the asynchronous proof early, then record its exact head.** A successful branch dry
  run proves that tree and the unchanged report regex. App edits to an imported time module
  justify another dry run on the final code. Documentation receipts can follow without
  amending away the SHA. Required PR checks still need the PR's own event run.
- **Keep transient QA evidence outside the source tree.** An isolated Chrome profile, native
  CDP driver and machine-readable case receipts can exercise the matrix without adding
  Playwright or a second permanent rig. Retain the captures for the handoff, distinguish local
  artifacts from GitHub-visible evidence, and put the reproducible conditions in the proposal.

Added by the v0.2.5 review (PR #23, 5 Sep 2026):

- **`matchMedia` change events need a rendering opportunity.** They dispatch in the "update
  the rendering" steps, which a hidden document can defer: the pane's emulated
  `prefers-color-scheme` flip read `matches: true` while the app's listener had not run, and
  looked like a broken listener. Force a frame (a screenshot does it) before reading the DOM.
- **Derive every clock-shim jump from the real clock, not from `Date.now()` after the shim is
  installed.** A second `__setOffset(target − Date.now())` compounds the first offset; capture
  `realNow` before the shim goes in. The compounded run drove Sep 5 → 6 → 12 → 7 and still
  "passed" every assertion, which is the tell: a probe whose intermediate dates you never
  print can pass for the wrong reason.
- **An uncommitted, hash-gated throw is one boundary probe.** One line in a served
  component (`if (location.hash === '#boom') throw`), scoped to one tab so the other
  recovers, covers six lens × theme cells at two widths, Reload and Reset view. Restore
  only the probe from a saved copy, preserving any pre-existing edits; the review used
  `git checkout` on its otherwise clean file. Read React's
  caught-error report in the console to confirm the boundary named itself.
- **The pane refuses `file://`.** A single-file build's "opens from the filesystem" claim
  cannot be checked there. An HTTP preview proves served loading, not filesystem loading;
  use a browser that permits `file://` or a human double-click for the latter. Pass 2
  verified both with isolated Chrome and the repository's existing `npm run preview` script.
- **When a doctrine retires a report value, grep the method too.** `standings=failed` is
  unreachable since v0.2.5, yet `CLAUDE.md`'s hold-reason list and
  `.claude/skills/kickoff-pr-review/SKILL.md` §0 still recite "a failed standings fetch" as a
  live hold. Left as they are by this review (the skill is not this PR's tooling); the next
  patch that touches the skill should reword both. The `--check` form the skill allows still
  works under the new entry-point guard, and now returns through `process.exitCode` (exit 1
  with a report line on 5 Sep).
- **Retro on the skill, counted.** Right: viewport before capture, the MessageChannel settle,
  the clock-shim recipe, the one-comment shape, and "re-run what the proposal promised" — the
  proposal's own table was the checklist. The held production-module load and filesystem
  opening were exceptions, as the review comment records; Pass 2 supplied those proofs. Wrong or
  missing: the three notes above (MQL frames, `file://`, the retired hold reason), and no
  line on how to smoke a *built* app in the pane when the diff changes what only a build shows
  (here, head order). A charset budget the build could silently cross was the one finding the
  build's own 48-cell matrix could not see; a byte-order read of `dist/index.html` should join
  the smoke pass whenever `transformIndexHtml` or `index.html` changes.

Added by the PR #24 review (5 Sep 2026):

- **A badge or credit line is a claim too.** A one-line README diff adding a "built with X"
  badge looked trivial until it was checked against `CLAUDE.md`'s stated build process and
  didn't match — the fix wasn't code, it was asking the human whether the claim was current
  rather than guessing (it was: the SDLC had genuinely grown a second builder, README just
  hadn't caught up). Treat provenance/credit claims with the same "verify, don't vibe"
  standard as a data-honesty assertion, and escalate rather than silently editing or silently
  merging when a new claim and the repo's own docs disagree.
- **A PR body is a claim about its own diff, and can be wrong too.** This PR's body said it
  would "replace" a badge; the diff only ever added one. Read the two side by side before
  trusting either — `gh pr edit` is cheap once the mismatch is found.

Added by the PR #26 review (Pass 1 of the cross-vendor 360, Claude Code, 6 Sep 2026):

- **A headless CDP driver (Node 24's global `WebSocket`, no dependency) is what reaches
  `prefers-reduced-motion`.** The pane cannot emulate it; `Emulation.setEmulatedMedia` can,
  and `matches` read true only after a forced frame — the v0.2.5 note held. Three driver
  traps cost a run each: `Runtime.evaluate` returning a DOM node by value fails with "Object
  reference chain is too long" (end the expression with `; true`); `await import()` needs an
  async wrapper, so cache the app's module on `window` once; a hover dispatched at page
  coordinates below the fold never fires — `scrollIntoView` first, viewport coordinates for
  input, page coordinates for a screenshot clip.
- **A derived field is not in the JSON.** Standings rows carry `gf`/`ga`, not `gd`; a query
  on `r.gd < 0` said "no negative rows" and was wrong — Racing Santander sits at −1 in the
  rendered table. Check a claim about a computed value against the renderer (or `tableFor`),
  never the raw file.
- **An in-memory fixture edit renders only when the consuming memo's dependency moves.**
  `WeekView`/`MonthView` key on the minute (`nowUtcIso`); `TablePage`'s rows on `today`.
  Mutate, then jump across a minute (or a day) and dispatch `focus`; a same-minute catch-up is
  a no-op by design and looks like a mutation that did not take.
- **Tailwind v4 opacity modifiers are oklab `color-mix`, and the rendered pixel is the
  ground.** Chrome reports `bg-accent/10` as `oklab(… / 0.1)`; the composited pixel differed
  from the sRGB prediction by one channel unit (72h banner: 4.53 measured, 4.55 predicted).
  Under a 0.05 margin, sample the pixel — `Page.captureScreenshot` with a 1×1 clip decodes
  with `zlib` alone — rather than trusting either computation.
- **A clock shim whose `__setTarget(ms)` subtracts the real `Date` captured inside its own
  closure cannot compound.** Every receipt prints `now`, `real` and the offset per jump; a
  probe whose intermediate dates you never print can pass for the wrong reason (the v0.2.5
  note, kept).
- **Retro on the skill, counted.** Right: "latent paths need synthetic data" and the
  boundary framing (−1 ms, 0, +1 ms, one constant, the complement, the stored status) were the
  review's method; the sealed-appendix discipline surfaced two spec deviations the appendix had
  guessed at and one sub-claim it overstated (KICKED OFF and POSTPONED differ by label, not
  hue alone). Wrong or missing: §4 has no line on reaching a `prefers-reduced-motion` state
  (above), none on sampling a rendered pixel when a contrast margin is thin (above), and §3's
  Theme class asks for computed-style reads without asking which *ground* the test asserts
  against versus the one the component renders on — the Table hover and the banner tints were
  that gap, and this PR closed it by inventory; the line to add is "list the grounds by
  component before trusting a contrast table". The skill's own §0 edit in this PR is accurate
  against `scripts/` as they stand, and a review that reads its own instructions should say so
  in its comment.

Added by the PR #30 review (Pass 1 of the cross-vendor 360, Claude Code, 7 Sep 2026):

- **A predicate proof is not a render proof.** `next` and `underway` were mutually exclusive
  over one fixture, and the JSX still put Sunday's opponent over Monday's marker: `a ?? b`
  chose the line above, `a ? … : b` the line below. Trace every consumer of a new lane to the
  text it renders, and give the synthetic club the *ordinary* case — a match underway and the
  next one set — not only the edge.
- **An assertion behind a conditional derived from the state under test is not an
  assertion.** The head's DOM test read the row label, then checked the pre-kickoff render only
  `if (!wasUnderway)`; an implementation showing KICKED OFF an hour early would have passed it.
  Assert unconditionally at each instant, and scope with `within` — Celta's lane wore the same
  `1:00 PM EDT` as Getafe's.
- **When two gates guard one path, test the case only one of them can reach.** The instant
  gate retires an exact time before the date floor can, so only a placeholder in the
  00:00–04:00Z band (Brooklyn yesterday) reaches the floor; the head's floor test used an exact
  fixture the instant had already retired, and the floor was unpinned. Today's placeholders all
  sit at 18:00Z/20:00Z, so the leak was latent — say which.
- **A default parameter that reads the clock is a hidden import of the system time.**
  `nowUtcIso = new Date().toISOString()` sat beside a comment saying the instant is never read
  internally; every caller passed it, so nothing failed. Remove the default; TypeScript is the
  test.
- **When a PR borrows a word, grep for where it already lives.** KICKED OFF was defined in
  `FixtureRow` and `CHANGELOG.md` `[0.3.0]` as the state LIVE expires *into* — non-expiring by
  design — which settled whether the Table's lane should expire (it should not) before the
  question was argued from first principles.
- **`TZ=America/New_York date` first, then ask whether the real clock already exercises the
  path.** This review ran during the match it was about; the running app at the head was the
  receipt for two claims, no shim needed. A screenshot taken in the same batch as a programmatic
  `scrollIntoView` came back blank; capture in a following call.
- **`today` and `nowUtcIso` are one instant in two shapes, in tests too.** A pair like
  `('2026-09-08', '2026-09-08T00:00:00.000Z')` names Tuesday and an instant on Monday evening;
  harmless where the instant does the work, but a floor test with such a pair can pass for the
  wrong reason. Derive both from one instant, or write the EDT wall-clock beside the pair.
- **Retro on the skill, counted.** Right: the sealed-appendix re-derivation — all six claims
  reproduced on the review's own evidence, and a no-refutation result is still a result;
  viewport before capture; the MessageChannel settle; one tab on the worktree's own port. Missing:
  §3's Tests class says nothing about giving the synthetic subject the ordinary case, or about an
  assertion inside a conditional on the state under test (both above), and §4 has no line on
  using the real clock when the review coincides with the event. The skill is not this PR's
  tooling, so these stay here.

Added by the PR #30 Pass 2.5 (synthesis, Claude Code PM seat, 7 Sep 2026):

- **A harness defect can look like a passing matrix.** The first 24-cell run reported one theme
  twice: the toggle finder matched an `aria-label` the button does not carry (it is button
  *text*), so no click fired and every "dark" cell was light. The tell was identical marker
  colours across the "two" themes. Confirm `data-theme` per cell and confirm a colour actually
  changed before counting a theme cell; a cell count without that is a claim.
- **Positive-control every stub.** The header's fixture count reads `meta.json`, not the
  fixtures array, so "1007 FIXTURES" says nothing about whether a stubbed row loaded. Prove it
  by importing the served module (`performance.getEntriesByType('resource')` →
  `/src/lib/fixtures.ts`, then `FIXTURES.length` and the stub id) or by rendering the stub
  where it must appear (the ghost row in its own week).
- **"The snapshot holds zero in-play fixtures" is true only between matches.** Row 28: the
  assumption sat in `browser-matrix.md`, in SKILL §4 and, silently, in a DOM test that
  `sync.yml` runs on every fresh snapshot. A review that coincides with a match must expect a
  live row, and a test that mutates a real-snapshot fixture must scope its queries to that row.
- **When the review and the sync coincide, read the sync run before the app.** A red verify
  step means the served snapshot is older than the header suggests; today the branch's
  snapshot was frozen at 00:02Z with four scheduled rows past kickoff, which is why the real
  clock still reproduced the state without a shim.
- **Two vendors and a third pass agreeing on a measurement is worth recording as a
  constraint.** 57 px marker, 120 px lane, 5.7 px mono advance at 9.5 px, card blocks
  328/343/358 in 332/347/362, and a 773 px desktop grid inside a 740 px container at 1000 px
  (pre-existing, in-table scroll) — any "Both" design starts from these.
- **Retro on the skill, counted.** Right: re-run not read (every vendor number reproduced or
  was marked not re-run); the gate's "nothing still contested" clause correctly forced an
  escalation on a label alone — a disagreement about a word is still a disagreement, and it
  cost nothing to carry. Missing: §4 has no positive-control line for stubs; the 48-cell count
  needs "theme confirmed per cell" to mean anything; and the cycle table's Pass 2.5 row scopes
  the matrix to "every surface the rebuttal touched", which reads as zero when the rebuttal
  touched nothing — 2.5 corroborates, so the matrix re-runs regardless. The PR is not tooling,
  so these stay here.

Added by the PR #33 review (Pass 1 of the cross-vendor 360, Claude Code, 8 Sep 2026):

- **A test's input is fixed by construction, or it moves with every sync.** Picking rows by
  `status` or `result` from the committed snapshot forks on what the cron last wrote; the two
  shapes that cross a week boundary are a Sunday-evening snapshot (the last Sunday kickoff
  `in_play`, every other Sunday row resolved, so the next unresolved row is Monday's) and a
  postponed row anywhere earlier in the file (its old instant anchors the week). Both threw
  "Unable to find an accessible element" at `cfbdccc`; the recipes are in the PR's Pass 1
  comment. Pick by date — `BY_DATE`'s first day with two exact kickoffs — and override states.
- **Shape the snapshot a named cron slot would write, not just one live row.** One `in_play`
  row reproduced row 28; "every row before the live one resolved" reproduced the fork. Write
  the mutation as the state at a slot (Sun 13 Sep 19:23Z) and say which.
- **Accessible names in jsdom concatenate without spaces** — `"2:30 PM1. FC Union Berlin vs
  Schalke 041–0LIVEBundesliga · …"` — so anchor on `startsWith(time)` and the row's own
  `"home vs away"`; `includes` alone lets a 12:30 PM row answer for a 2:30 PM one. Read names
  with `computeAccessibleName` from `dom-accessibility-api`, and write probe output to a file:
  `console.log` inside a dom-project test did not reach vitest's output in this harness.
- **Prove a restore guard by breaking it.** Two nested in-place mutations made
  `toEqual(pristine)` fail in `afterEach`; the PR's own shape (a replaced `result` object, a
  moved `kickoffUtc`) restored clean. The guard restores top-level edits and detects nested ones.
- **Port ownership before any browser claim, on every port, not only 5173.** 5174 was another
  worktree's dev server (`lsof -p <pid>` → cwd). A temporary `.claude/launch.json` entry
  serving `dist/` with `vite preview` on a third port, reverted before the commit, is the smoke
  pass for a diff with no `src/` change.
- **zsh does not word-split a command held in `$C`.** A commit alias in a variable ran nothing
  and left three files staged and a push saying "Everything up-to-date"; use a shell function.
- **Retro on the skill, counted.** Right: the sealed appendix — S1 and S5 killed on evidence,
  S3/S4 upgraded from a suspicion to a dated red run; "latent paths need synthetic data"; the
  smoke-pass rule for a no-`src/` diff; the one-comment shape with an overturn column.
  Wrong or missing: §4's Latent-paths bullet said "scope the query" and nothing about how the
  row is picked — fixed on this branch (`68c2377`); §0's port line names only 5173.

Added by the UCL design-cycle Round 1 review (Claude Code, Fable 5.1, 8 Sep 2026 — the first
run of the three-round design review; `docs/design-cycle-ucl-review-pass-1.md`):

- **Re-run routed evidence before you review anything built on it.** The canvas led with a
  blocking finding ("ESPN publishes no `uefa.champions` scoreboard") that reached the designer
  as a human's report from a container with no egress. One `curl` from a machine with network
  inverted it (row 30). A finding a design *leads* with is the first thing to re-derive, and a
  review harness without network must say so in its first paragraph, not its last.
- **A design's computed numbers are checked by recomputing and by changing the input.** Read
  the rendered figures from the browser (they are not in the file), recompute them cold, then
  drive the Tweaks prop — here `window.__dcSetProps(window.__dcRootName(), {uclCandidate})` —
  and confirm every dependent figure and verdict moves. The one figure that did not move was the
  one typed by hand, and it was wrong (row 37). The two sRGB thresholds in the same file
  (0.03928 in `lum()`, 0.04045 in `lab()`) cannot disagree on 8-bit input — no integer channel
  falls in (0.03928, 0.04045] — so that suspicion was killed by arithmetic, not by inspection.
- **Test a designed mechanism on the real DOM before the build exists.** Apply the design's own
  class recipe to the live element with the JavaScript tool and measure (the sticky divider,
  row 32); clone an existing control to add the sixth chip or the third tab and read the layout
  at every matrix width (rows 33, 34). Fifteen minutes of DOM injection found three findings a
  designer cannot see and a diff review would only meet after the code was written.
- **Recount every repo fact an artboard prints at the review's head, and say which moved.**
  The header stamp was right at the commit the design read and stale one sync later; the
  `FRI 11 (4) · SAT 12 (25) · SUN 13 (14)` counts, `All 14 competitions shown` and all twenty
  Premier League rows held. A design is dated to a commit; the review says which of its facts
  survived the commits since.
- **"Pixel-identical" is a claim about box-sizing too.** The canvas's PL rows are 55 px where the
  app's are 54: inline `min-height:54px` without `border-box` adds the hairline. Compare computed
  styles element by element (rail 3 px `#8B6FE8`, divider 9 px / 1.08 px letter-spacing, Pts 19 px
  Oswald, header 28 px), not screenshots.
- **A canvas's "not decided" list can be contradicted by its own body text.** Grep the prose for
  each lettered item before accepting the list (row 38).
- **Retro on `/kickoff-pr-review` as the method, counted.** Right, and reused as written: §0's
  pre-flight (the tree was one sync past the prompt's SHA and the prompt said to expect that);
  §2's real numbers; the one-comment shape; viewport before capture and `scrollWidth ===
  innerWidth` on every cell; the sealed-appendix discipline — ten suspicions, eight reproduced,
  one killed by arithmetic (S4), one inverted by evidence (the F1 premise behind S2). Wrong or
  missing for a design artifact, and the raw material for row 40: §1 assumes a diff and a
  spec lineage under `docs/` — this artifact was a local-only canvas plus a brief that was not on
  the machine, so "read the diff" had no object and the archive step had to be a transcription;
  §3's hunt classes are all code-shaped — the ten D-classes in the Round 1 prompt had no home
  in the skill; §4's matrix presumes the diff touched `src/` — here nothing did, yet the
  browser was the only place four findings could be made, so "smoke pass when docs" is the
  wrong rule for a design review; the fix policy's "fix what you can reproduce" has no analogue
  when the artifact is not in the repo — the review can correct a doc and must *state* a design
  finding, and the skill should say that a design finding's deliverable is the handoff column
  (what Round 2 must produce to overturn it), not a commit; and §7's PR-shaped landing was the
  right shape only because the paper trail is docs — the branch, the PR and the comment exist to
  carry the review, not the change.

Added by the UCL design-cycle Round 2 rebuttal (Codex, 9 Sep 2026;
`docs/design-cycle-ucl-review-pass-2.md`):

- **Trace an attributed result back to an actual run.** Correction to Round 1's first retro
  bullet: the brief routed a question, not a negative finding; the attributed human check
  was never performed. Keep supplied testimony distinct from current network receipts.
- **Challenge the quantifier, preserve the scoped problem.** A CSS-only sticky counterexample
  disproved “both need markup” but changed the >8-only rule; compatible PPG advice disproved
  necessary contradiction without choosing new copy. Record both halves in the disposition.
- **Presence, shape, semantics and integration are separate checks.** A 200 did not establish
  UEL fixtures until its own window was probed; a field named ppg was zero for winning teams;
  valid fixture schemas still carried the wrong venue time zone. Rebut the proposed path all
  the way to the user-visible result.
- **Probe the code's order as well as the illustration's.** Sixth-last stays at two rows on
  larger phones; sixth-first wraps at all three. A sixteen-row band is needed to test release,
  not just sticking. Restore every cloned node/style and assert the actual viewport, theme
  and lens: the browser viewport tool initially targeted another tab, so that batch was
  discarded and rerun with actual innerWidth checks.
- **Rebuttal residue is not the original findings list.** Keep contested judgments at the top,
  remove accepted-and-closed fixes, name evidence needed for open rulings, and update downstream
  guidance while preserving the historical reviews. These are row 40's rebuttal requirements.

### European week implementation (#39, 10 Sep 2026)

- Preserve an absent first-seen round as deliberately as a present one: a later in-window move must not acquire a number silently.
- Inspect the live venue country inventory. The earlier 36-country list gained Wales and Azerbaijan; the live Sabah feed supplied Baku Olympic Stadium, so the null-venue acceptance case stays synthetic.
- Verify the actual governing-body calendar: Annex C now corroborates all eight windows, so the implementation can remove ESTIMATE while still labelling the derived number COMPUTED.
- Keep curation outside the snapshot boundary explicitly. Retain venue zone, time confidence and status at curation so a card cannot fabricate a clock after its fixture leaves the window.
- Measure browser scroll reach as well as sticky containment. At 1000 × 900 the natural document ends before the play-off divider can release; a disclosed temporary spacer tests containment without shipping extra whitespace.
- Browser probes must wait for a new document, not a complete old page. Use the served module actually imported by the app; verify injected fixtures are visible before capturing.
- The report printer was already in scripts/diff.ts and a workflow-regex mirror already existed. Extend those sources; do not create a second API or weaken its six original fields.
- Detailed counts, contrast values, layout differences and design confirmations are in [the verification record](european-week-verification.md). Rows 35 and 40 stay open: partial component contrast corrections are not a completed design-system inventory.
