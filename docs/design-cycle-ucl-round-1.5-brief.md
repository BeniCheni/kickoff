# Executive Brief — UCL design-review cycle, Round 1 → Round 2 handoff

You are the Chief TPM and Product seat: a Claude Cowork session on Opus Extra, running `/football-soccer-deity` Track B, the Kickoff build track. This brief hands you the Round 1 cold review of the UCL league-phase design cycle so you can do two things, in order: settle the logistics of Round 2, then write the Round 2 Design Review prompt that Beni will paste into Codex (GPT-6 Astra, Extra High). You are not the reviewer and not the builder. You are the seat that decides how the next seat works and hands it a prompt it can run cold.

Two rules govern everything you write. First, the repo is ground truth and this brief is a claim about it: read every file it names before you repeat a number from it, because every count in here moves when the scheduled sync merges itself. Second, generated prompts are never hand-wrapped: one line per paragraph however long it runs, a four-backtick outer fence when the prompt carries its own fenced sub-blocks, delivered in the chat message ready to paste. The `docs/` copy is the archive, never the delivery. `../Sportsbooks/CLAUDE.md` owns that rule and `Kickoff/CLAUDE.md` restates it.

## The cycle, and where it stands

This is the first run of the three-round design-review cycle, distinct from the six-pass 360 that reviews a code PR. Round 1 (Claude Code, Fable 5.1 Extra) reviewed a finished Claude Design canvas against the repo and landed its paper trail as PR #34. Round 2 (Codex) rebuts every Round 1 finding as accept, contest, or accept-but-contest-the-characterisation, reproducing or disproving each in code, and may recommend a blocker. Round 3 (Beni, then a Claude Code session) adjudicates whatever is still contested, then writes the Executive Handoff Brief and the declarative build spec. Round 2's prompt is built from what Round 1 left unresolved, not from a template. That is your job here.

Beni's scope ruling for the cycle, made before Round 1: design-only. Round 1 changed nothing under `src/`, bumped no version, wrote no CHANGELOG section. The release number is the canvas's not-decided item (f) and stays Beni's at Round 3. Whether Round 2 inherits the same `src/` prohibition is the first logistics decision below, and it is yours to put to Beni with a recommendation.

## Read fresh, in this order

1. PR #34, https://github.com/BeniCheni/kickoff/pull/34, branch `design/ucl-league-phase`. As of 8 Sep 2026 23:40 EDT: tip `449dbae`, four commits authored `Claude <noreply@anthropic.com>` (`8ed3b0d` handles and ideas row 17; `67bdc69` brief and spec archives, README rows, ideas rows 30–41; `bc88478` the review; `449dbae` naming the PR), `verify` green at the tip (CI run 34307357432, 33 s), docs only, `--no-merge`. The review's record is its one PR comment: https://github.com/BeniCheni/kickoff/pull/34#issuecomment-5595347252. This brief is archived on the same branch as `docs/design-cycle-ucl-round-1.5-brief.md`, so the tip will have moved by one commit when you read this; the comment's tip line says so.
2. `docs/design-cycle-ucl-review-pass-1.md` on that branch: the full review. Ten hunt classes and what closed each, seventeen findings with a "Round 2 overturns by" column, what was killed, the matrix as run, which not-decided items are load-bearing, the ranked unresolved list, and the retro whose extra question seeds a `/kickoff-design-review` skill.
3. `docs/design-cycle-ucl-spec.md`: the canvas transcribed (the four rulings, both rule amendments verbatim, data-shape findings F1–F8 and F-DS1, per-state tokens, not-decided items (a)–(g), the VERIFY list, the screen map). `docs/design-cycle-ucl-brief.md`: a placeholder, because the brief's own text (`claude/kickoff-ucl-design-prompt.md` in the Football/Soccer Claude project) was not on the review machine.
4. `docs/v0.2.6-ideas.md`: rows 30–41 (new, from this review), row 17 (struck), and the process notes added at the tail under "Added by the UCL design-cycle Round 1 review".
5. `CLAUDE.md` in full, especially "How this repo gets built", "Verification discipline" and "Release management", and `.claude/skills/kickoff-pr-review/SKILL.md` with its `pr-comment.md`. This cycle borrows the one-comment shape, the sealed-appendix discipline and the commit-authorship rules from that skill and nothing else; its hunt classes are code-shaped and Round 1 replaced them with D1–D10.
6. The canvas itself: `../Kickoff European week design/UCL League Phase - Design Cycle.dc.html`, SHA-256 `7fcd369554f5b0dc87f61bfc20046a83286d330fd7558da80b2a7a89d2d95785`, 138,306 bytes, eight artboards, local-only and unreleased like `../Fergie Time Design System/`. From a worktree the path is `$(dirname "$(git rev-parse --git-common-dir)")/../Kickoff European week design/`, never `$PWD/..`. Neither folder is ever committed. Its colour-sheet figures exist only at render time (a `class Component extends DCLogic` at the tail of the file), so any seat that reviews it must serve the folder over HTTP and open it in a browser; the pane refuses `file://`.

## Pre-flight you run before you write a number

```bash
cd ~/Documents/Claude/Projects/Kickoff
TZ=America/New_York date
git fetch origin
git rev-parse --short origin/main; git log --oneline 0c37716..origin/main
git rev-parse --short origin/design/ucl-league-phase; git log --oneline origin/main..origin/design/ucl-league-phase
git branch -r | grep -i row28
lsof -iTCP:5173 -sTCP:LISTEN
```

Expect `origin/main` to have moved past `0c37716`. The scheduled sync merges itself once `verify` is green (`CLAUDE.md`, "Scheduled sync"), and a row-28 hotfix (`codex/row28-designcycle-live-scope`, the branch the main checkout sat on during Round 1) may have landed. Say what moved. If `main` moved, every snapshot count in the Round 1 review and on the canvas is ESTIMATE for Round 2 until recounted, and the Round 2 prompt must say so in its own pre-flight. Note also the calendar: Saturday 12 September carries 25 domestic fixtures and Sunday 13 carries 14, so a Round 2 that runs during matches will meet live rows in the snapshot, and until row 28's hotfix merges the DOM suite `tests/dom/designCycle.test.tsx` goes red on any snapshot holding an `in_play` row. The Round 2 prompt must tell Codex how to read that red.

## What Round 1 found, ranked, and what overturns each

The headline inverts the canvas's premise. The canvas leads with "ESPN publishes no `uefa.champions` / `uefa.europa` / `uefa.europa.conf` scoreboard (Beni's curl check, 8 Sep)" and builds F1 ("a second provider is a prerequisite; `source.provider: z.literal('espn')` must widen") and not-decided item (a) on it. The Round 1 prompt itself noted that this reached the designer as routed evidence from a container whose egress refused `site.api.espn.com`. From the review Mac at 23:10 EDT on 8 Sep 2026, every endpoint answered:

| Request | Result |
|---|---|
| `https://site.api.espn.com/apis/site/v2/sports/soccer/uefa.champions/scoreboard?dates=20260908` | 200, 69,886 B, six League Phase events, all `STATUS_FULL_TIME`, `season.type.slug: league-phase` |
| the same for `dates=20260909` and `dates=20260910` | 200, six scheduled events each; eighteen MD1 fixtures over the three days |
| the same for `dates=20260916` and `dates=20260917` | 200, zero events (no matchday) |
| `…/soccer/uefa.europa/scoreboard`, `…/soccer/uefa.europa.conf/scoreboard` | 200, league present, no events on the date probed |
| `https://site.api.espn.com/apis/v2/sports/soccer/uefa.champions/standings?season=2026` | 200, 143,450 B, one child "League Phase" with 36 entries; stats `gamesPlayed, wins, ties, losses, pointsFor, pointsAgainst, points, rank, rankChange`, the exact names `scripts/providers/espn-standings.ts` `normalizeStandingEntry` reads |

The team ids are ESPN's global ids: 21 of the 36 standings entries (Barcelona 83, Liverpool 364, Bayern 132, Dortmund 124, Atlético 1068 among them) already appear in `src/data/standings.json` under the same id, so the `teamId` to `sourceId` join is one namespace. ESPN carries no per-event matchday (`week` undefined, `competitions[0].notes` empty on every event), so the canvas's derived-matchday ruling stands on its own merits rather than on the absence of a provider. ESPN does carry two things the canvas hand-authors: `season.type.slug` plus a `leagues[0].calendar` of six dated phase windows, and a `note.description` on every standings entry that reads "Qualifies for round of 16" (1–8), "Knockout phase playoffs - seeded" (9–16), "Knockout phase playoffs - unseeded" (17–24), "Eliminated" (25–36). Round 1 does not know which URL Beni's check used. Only he can supply it, and the Round 2 prompt must carry either the URL or the statement that it is unknown.

The seventeen findings, condensed. Severity per `pr-comment.md`. "Against" says whether the defect is in the design or in the repo the design describes. The last column is what Round 2 must produce to overturn the finding; if it cannot, the finding stands into Round 3.

| # | Sev | Class | Against | Finding | Overturned by |
|---|---|---|---|---|---|
| 1 | high | D10 | design | F1 is false from the review network; a second provider is not a prerequisite; the build path is `espnCode: 'uefa.champions'` on `ucl` plus a `LEAGUE_TABLES.ucl` entry with `teams: 36` | A 404 or an empty `leagues` array from a second network using the exact URL Beni used |
| 2 | medium, load-bearing | D6 | repo, as the design assumes it | A sticky `ZoneDivider` is unreachable on `TablePage.tsx`'s mobile markup: the `<div key={r.teamId}>` wrapper confines it to 82 px; measured on the live DOM at 390 with the canvas's exact recipe, −118 px nested versus 28 px hoisted, and hoisted it overruns the band into the next divider. Needs per-band grouping or every divider sticky and opaque (it is transparent today); the desktop header is `position: static` | A CSS-only recipe that sticks under the 28 px header for the band's height on the unchanged DOM |
| 3 | medium | D9 | design | Three tabs stack the lens switcher above the row at 390, 375 and 360 (tabs 194 px + gap 16 px + switcher 202 px = 412 px against 350 / 335 / 320 px of content); the nav grows from 38 to 65 px on every mobile screen. The canvas's 390 px Moments boards render exactly that; its caption "stacks above under ~375" restates the two-tab threshold (two tabs fit 375 by 1 px and stack at 360) | Real Oswald label widths that fit 350 px, or the designer accepting the corrected caption and the 27 px push |
| 4 | medium | D7 | design | A sixth picker chip ("⭐ Champions League", 145 px) makes a third picker row at 360 (63 to 97 px; 390 and 375 stay at two); `TABLE_LEAGUES` follows `COMPETITION_KEYS` order, in which `ucl` precedes `laliga`, so the code renders the ⭐ chip first, not sixth as drawn | The reorder the build will make, or a 360 artboard showing three rows as intended |
| 5 | low | D1 | design, freshness | The header stamp `synced 2026-09-08 00:30 UTC` matched `22ca01a` and was `17:17 UTC` at `0c37716`; 1007 fixtures, 14 competitions, `FRI 11 (4) · SAT 12 (25) · SUN 13 (14)` and all twenty Premier League rows held | Not a defect; recount at Round 2's head |
| 6 | medium | D4 / S1 | repo | `src/lib/competitions.ts` lines 4–5 say espnCode-less competitions "carry hand-authored placeholder rows"; `fixtures.json` holds zero rows for `ucl`, `uel`, `uecl`, `supercoppa`, `supercopa`. The one sentence in the repo that reads as a licence to hand-author fixtures | Rows for any of the five keys in any committed snapshot |
| 7 | medium | D5 / S3 | repo | `matchdayProgress` (`standings.ts` 157–161) prints "1 of 70" for 36 rows; the canvas's F2 (`meta.matches ?? (teams-1)*2`) is the right fix; the sentence stays true after MD8 because the league-phase table freezes at 36 × 8 and ESPN's knockout rounds have no standings child | ESPN UCL `gamesPlayed` advancing past 8, or rows lost from the League Phase child after MD8 |
| 8 | medium | D8 | design | The 11 px meta line in `FixtureRow.tsx` line 89: violet is 3.48 on cream, 3.79 on white, 4.63 on night bg, and 4.01 on dark `--surface`, which is the ground Broadcast's `[data-hot]` rows paint (`index.css` 184); the canvas measured cream and night bg only. Pre-existing class: all five domestic hues fail on cream at 11 px (pl 3.45, laliga 3.55, seriea 3.10, ligue1 3.30, bundesliga 3.41) and white chip text fails on every on-chip (3.39–3.87). Round 1 ruled it not a blocker: not a new surface; a named debt for the token-mirror patch, which must list grounds by component first | A hot-row ground other than `--surface` in Broadcast dark, or a ruling that a sixth instance of an existing failure is a blocker |
| 9 | low | D9 / D2 | design | `--media` (#e4e0d2) on `--surface-alt` (#efead9) is 1.10:1, not the 1.14:1 the canvas prints; it is the one contrast figure on the sheet that is typed rather than computed (`renderVals()` computes no media figure). The no-ratio argument for a non-text placeholder ground holds under WCAG 1.4.11 | Arithmetic |
| 10 | low, load-bearing | D4 | design | Artboard 1g's footer says Moments "live in `src/data/moments.json`" while 1h lists item (c) as undecided; `src/data/` is the folder HONESTY.md rule 1 defines as written only by `npm run sync` | The designer striking one of the two sentences |
| 11 | medium, load-bearing | D3 / S7 | design | The amended rule 3 admits a confident lie: a tie rescheduled into another matchday's window derives the wrong number and the absence state does not fire; nothing synced contradicts a stale window list (no matchday in any ESPN payload). Two derivable checks exist and neither is designed: a test that the hand list is eight disjoint ordered windows for the synced season, and a sync-time count of UCL fixtures outside every window as evidence of staleness | A synced field or a designed check that can contradict the list, or an accepted cost written into the amended rule |
| 12 | low | D10 / S2 | repo | `provider: z.literal('espn')` appears at `schema.ts` lines 46, 57, 94; the join is ESPN-id-bound. Moot for this cycle (same provider, same ids); survives only for a hypothetical non-ESPN source, as `schema.ts` lines 122–123 already say | Not applicable; S2's premise was inverted by finding 1 |
| 13 | low | Beni's items / S8 | repo | Three stale skill handles (`CLAUDE.md` 5 and 156, `SKILL.md` 38) corrected to `/football-soccer-deity` in `8ed3b0d` | Fixed |
| 14 | low | D7 | design | Artboard 1e's Premier League rows are 55 px to the app's 54 (inline `min-height:54px` without `border-box` adds the hairline); rail 3 px `#8B6FE8`, divider 9 px / 1.08 px letter-spacing, Pts 19 px Oswald, 28 px sticky header and both grounds match, so "pixel-identical" is off by one hairline per row | Not applicable |
| 15 | low | D1 | design | "synced 3h ago" on 1d and 1e cannot follow from the header's `00:30 UTC` on a "today" of Wed 9 Sep 11:00 EDT (14.5 h) | Not applicable |
| 16 | low | D5 | design | Mid-matchday `max(played)` is N with twenty-four clubs on N−1, so the existing games-in-hand callout says "24 clubs have a game in hand — sort by PPG" under a legend that says PPG compares unlike schedules | A callout rule for competitions whose `phase` is set |
| 17 | low | Beni's items / row 17 | repo | `CONTRIBUTING.md` and `CLAUDE.md` described an all-Claude build where README (PR #24) and `git log` (three `Codex <noreply@openai.com>` co-author trailers) do not; both now point at README's "Two builders, one repo" table, `8ed3b0d` | Fixed |

Killed by Round 1, so Round 2 need not re-argue them unless it has new evidence: S4 (the `lum()` 0.03928 versus `lab()` 0.04045 thresholds cannot disagree on 8-bit input; no channel value falls in (0.03928, 0.04045]); S2 as stated (the provider the design waited for is the one it has); and the design's repo facts, which all held at `0c37716`. Round 1 also drove the canvas's Tweaks prop (`window.__dcSetProps(window.__dcRootName(), {uclCandidate: '#2F4B7C'})`) and every dependent figure and verdict moved and matched a cold recomputation, so the colour sheet is live, not cached.

Not-decided items, as Round 1 judged them. Load-bearing for the build: (a) which provider and whether a round string outranks the derived window, where the evidence has changed to "ESPN, which has no round string"; (c) whether Moments live under `src/data/` or a sibling `src/curated/`; (g) whether the desktop divider fires at eight rows, which finding 2 makes a markup-shape question. Riding to Round 3: (b) the tie-break sentence, provided it is sourced to the regulations before merge; (d) history push for the Moments tab; (e) the still's aspect ratio. Beni's alone: (f) the release number. The VERIFY items: the league-phase tie-break order is open; Saturday 12 Sep's league count measured 5 at `0c37716` and stays ESTIMATE until the build's head; whether UEFA's windows shift for a rescheduled tie is open and finding 11 shows the harder case is a tie moved into another window. Round 2 resolves none of these. It states, for each load-bearing one, what evidence Round 3 needs.

## What Round 1 could not do, and Round 2 must not inherit blindly

The brief's text was not on the machine; `docs/design-cycle-ucl-brief.md` is written to be replaced when Beni pastes `claude/kickoff-ucl-design-prompt.md`. The canvas was verified by computed styles and rendered text, not pixels, because the hidden pane would not composite the canvas tab. Only the UEL and UECL scoreboards were probed, not their standings. Every ESPN result comes from one network, once; Round 2 re-running from a second network is the single most valuable act in the cycle. UEFA's official matchday window dates behind the canvas's "8–10 Sep" and "13–14 Oct" were not checked against a primary source, though ESPN's MD1 events do fall on 8–10 Sep.

## Logistics you settle first, and output as a table before the prompt

For each row, state the decision, who made it (you, or Beni in one word), and the sentence that carries it into the Round 2 prompt. Round 1's recommendation is given; you may overrule it with a reason.

| # | Decision | Round 1's recommendation |
|---|---|---|
| L1 | Round 2's write scope | Same ruling as Round 1: nothing under `src/`, no version, no CHANGELOG section. Codex may write throwaway probes anywhere (a scratch directory, an uncommitted local edit, a browser injection) and commits only its rebuttal document, one PR comment, and documentation corrections a finding names. Anything that needs `src/` is a build item in its table, not a commit. Beni confirms in one word |
| L2 | Round 2's network | Finding 1 cannot be rebutted without egress to `site.api.espn.com`. If Codex's harness has none, the prompt says so in its first section and routes the four `curl` commands below to Beni with the expected outputs, and the rebuttal labels the result ROUTED again; the cycle must not repeat the trap that produced the false premise silently. Ask Beni which harness Codex runs in |
| L3 | Where Round 2 lands | On PR #34's branch `design/ucl-league-phase`: one commit adding `docs/design-cycle-ucl-review-pass-2.md`, one PR comment in `pr-comment.md`'s shape with the accept / contest / accept-but-contest-the-characterisation verdict per finding, and a line in the comment saying the branch moved under the next seat. Not a new PR. Commits authored `Codex <noreply@openai.com>` via per-commit `git -c`, never `git config` |
| L4 | Beni's ESPN URL | Ask Beni for the exact command his check ran and its output. It travels in the Round 2 prompt as his, verbatim, or the prompt states that it is unknown. This is the one fact only he can supply |
| L5 | The brief's text | Ask Beni to paste `claude/kickoff-ucl-design-prompt.md`. If he does, the Round 2 prompt carries it in a sealed section and instructs Codex to replace `docs/design-cycle-ucl-brief.md`'s summary with the verbatim text, keeping the provenance note. If he does not, Round 2 works from the archive as it stands and says so |
| L6 | Model and reasoning | Codex GPT-6 Astra, Extra High, per Beni; `CLAUDE.md`'s routing line for the review pass is the Extra tier |
| L7 | The designer-owned rows | Rows 33 (three-tab stack) and 34 (sixth chip at 360, picker order) are design decisions. Round 2 measures and characterises; it does not choose. Say so |
| L8 | Timing against the calendar | If Round 2 runs Saturday 12 or Sunday 13 Sep during matches, the snapshot carries live rows and row 28's suite is red until its hotfix merges. The prompt tells Codex: if `npm test` is red for that reason and no other, say so and continue |
| L9 | Names and archive | Round 2's document is `docs/design-cycle-ucl-review-pass-2.md`; your prompt is archived as `docs/design-cycle-ucl-round-2-prompt.md` (no release number; say in the file that the number is item (f)). Both get a row in `docs/README.md`'s archive table |
| L10 | What Round 3 receives | Round 2 ends with the same "what remains unresolved, ranked" section Round 1 wrote, rewritten from its own results, so Round 3's adjudication prompt is built from Round 2's residue, not Round 1's |

## The Round 2 prompt must contain

A first section that states the cycle, the seat, the scope ruling (L1) and the network ruling (L2), and that Round 1's findings are conclusions for Round 2 to attack, not a sealed appendix. The pre-flight commands above, with the instruction to say what moved and to treat every count as ESTIMATE until recounted. The seventeen findings as a table with the "Overturned by" column intact, plus a fourth column Codex fills: `accept` / `contest` / `accept-but-contest-the-characterisation`, each with the command or measurement that decided it. The three killed items, so Codex does not re-argue them without new evidence. The not-decided items with their load-bearing marks and the instruction not to resolve them but to state the evidence Round 3 needs. The reproduction recipes below, verbatim, so Codex re-runs Round 1's measurements rather than trusting them. The permission to recommend a blocker, with the rule that a blocker recommendation names the finding, the evidence and the one-word question Beni must answer. What Round 2 must not do (below). The deliverables: one PR comment, one docs file, `docs/README.md` row, real numbers from `npm run typecheck` and `npm test` at its head, `TZ=America/New_York date` before any date, commit authorship as `Codex <noreply@openai.com>` via `git -c`, and the `Co-Authored-By` trailer its harness gives. The closing "what remains unresolved, ranked" section for Round 3. And a retro question of its own: what did rebutting a design review need that rebutting a code review does not, as raw material for the `/kickoff-design-review` skill (ideas row 40).

## Reproduction recipes to paste into the Round 2 prompt

The provider, from Round 2's own network. Expected as of 8 Sep: 200 on each, six events per MD1 date, 36 standings entries. Events on 9 and 10 Sep will read `STATUS_FULL_TIME` by the time Codex runs.

```bash
for d in 20260908 20260909 20260910 20260916; do curl -s -o /tmp/ucl-$d.json -w "$d %{http_code} %{size_download}B\n" "https://site.api.espn.com/apis/site/v2/sports/soccer/uefa.champions/scoreboard?dates=$d"; node -e 'const j=require(process.argv[1]);console.log("  events",(j.events||[]).length,"league",j.leagues?.[0]?.slug,"type",j.leagues?.[0]?.season?.type?.name,"week",JSON.stringify((j.events||[])[0]?.week??null),"notes",JSON.stringify((j.events||[])[0]?.competitions?.[0]?.notes??null))' /tmp/ucl-$d.json; done
curl -s -o /tmp/ucl-st.json -w "standings %{http_code} %{size_download}B\n" "https://site.api.espn.com/apis/v2/sports/soccer/uefa.champions/standings?season=2026"; node -e 'const j=require("/tmp/ucl-st.json");const c=j.children?.[0];const e=c?.standings?.entries||[];console.log("child",c?.name,"entries",e.length,"stats",e[0]?.stats.map(s=>s.name).join(","));console.log("notes",[...new Set(e.map(x=>x.note?.description))].join(" | "))'
```

The matchday windows ESPN implies, which is the one derivable check on the canvas's hand-authored list (finding 11): pull the whole league phase and cluster events by date; a matchday is a cluster of eighteen.

```bash
curl -s "https://site.api.espn.com/apis/site/v2/sports/soccer/uefa.champions/scoreboard?dates=20260901-20261231" -o /tmp/ucl-range.json; node -e 'const j=require("/tmp/ucl-range.json");const by={};for(const e of j.events||[]){const d=e.date.slice(0,10);by[d]=(by[d]||0)+1}console.log("events",(j.events||[]).length,"(ESPN caps a response at 100; chunk the range if you see exactly 100)");console.log(by)'
```

The sticky divider on the real DOM, in the browser pane's JavaScript tool on the Table tab, Premier League, viewport set to 390 before anything else. Round 1 read −118 nested, 28 hoisted.

```js
const mobile=document.querySelector('.-mx-5'); const d0=[...mobile.querySelectorAll('div.flex.items-center.gap-2')].find(d=>d.querySelector('span.label-caps')); const wrapper=d0.parentElement; d0.style.cssText='position:sticky;top:28px;z-index:1;background:var(--bg)'; const natural=d0.getBoundingClientRect().top+scrollY; scrollTo(0,natural-28+200); await new Promise(r=>setTimeout(r,300)); const nested=Math.round(d0.getBoundingClientRect().top); mobile.insertBefore(d0,wrapper); await new Promise(r=>setTimeout(r,300)); const flat=Math.round(d0.getBoundingClientRect().top); wrapper.insertBefore(d0,wrapper.firstChild); d0.removeAttribute('style'); scrollTo(0,0); ({nested, flat, wrapperChildren: wrapper.children.length})
```

The third tab and the sixth chip, injected into the running app at 390, 375 and 360 (set the viewport, then run; remove the probes after). Round 1 read nav 38 px with two tabs and 65 px with three at all three widths, and picker 63 px with five chips at all three widths, 63 px with six at 390 and 375, 97 px with six at 360.

```js
const nav=document.querySelector('nav[aria-label=Primary]'); const tabs=nav.firstElementChild; const t3=tabs.children[1].cloneNode(true); t3.textContent='Moments'; t3.removeAttribute('aria-current'); tabs.appendChild(t3); const picker=document.querySelector('button[aria-pressed]').parentElement; const c6=picker.children[1].cloneNode(true); c6.textContent='⭐ Champions League'; c6.className=picker.children[0].className; c6.removeAttribute('style'); picker.appendChild(c6); await new Promise(r=>setTimeout(r,300)); const out={width:innerWidth, scrollWidth:document.documentElement.scrollWidth, navH:Math.round(nav.getBoundingClientRect().height), pickerH:Math.round(picker.getBoundingClientRect().height), tabsW:Math.round(tabs.getBoundingClientRect().width), switcherW:Math.round(nav.querySelector('[role=radiogroup]').getBoundingClientRect().width)}; t3.remove(); c6.remove(); out
```

The contrast and ΔE arithmetic, cold, for any hue and ground. Round 1's values: `#8B6FE8` on `#f7f5ee` 3.48, on `#ffffff` 3.79, on `#0e1c15` 4.63, on `#142a1f` 4.01; `#e4e0d2` on `#efead9` 1.10.

```bash
node -e 'const hx=h=>{h=h.replace("#","");return[0,2,4].map(i=>parseInt(h.slice(i,i+2),16))};const lin=c=>{c/=255;return c<=0.04045?c/12.92:Math.pow((c+0.055)/1.055,2.4)};const lum=r=>{const[a,b,c]=r.map(lin);return .2126*a+.7152*b+.0722*c};const cr=(a,b)=>{const x=lum(hx(a)),y=lum(hx(b));return((Math.max(x,y)+.05)/(Math.min(x,y)+.05)).toFixed(2)};for(const g of["#f7f5ee","#ffffff","#0e1c15","#142a1f"])console.log("#8B6FE8 on",g,cr("#8B6FE8",g));console.log("#e4e0d2 on #efead9",cr("#e4e0d2","#efead9"))'
```

The recount of every repo fact the artboards print, at Round 2's head.

```bash
node -e 'const f=require("./src/data/fixtures.json");const m=require("./src/data/meta.json");const bk=i=>new Intl.DateTimeFormat("en-CA",{timeZone:"America/New_York",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date(i));const by={};for(const r of f){const d=bk(r.kickoffUtc);(by[d]=by[d]||[]).push(r)}console.log("total",f.length,"meta.total",m.total,"lastSyncAt",m.lastSyncAt);for(const d of["2026-09-11","2026-09-12","2026-09-13"]){const rows=by[d]||[];console.log(d,rows.length,"fixtures",new Set(rows.map(r=>r.competition)).size,"competitions")}console.log("in_play",f.filter(r=>r.status==="in_play").length,"ucl rows",f.filter(r=>r.competition==="ucl").length)'
```

The canvas, served and driven. `cd "../Kickoff European week design" && python3 -m http.server 8791`, open `http://localhost:8791/UCL%20League%20Phase%20-%20Design%20Cycle.dc.html` in the pane, read the 1f figures from the rendered text, then run `window.__dcSetProps(window.__dcRootName(), {uclCandidate: '#2F4B7C'})` and confirm the second swatch re-renders as `#2F4B7C · MEASURED` with ΔE 38.0 / 30.6 / 0.0 and contrast 8.68 / 7.96 / 2.02.

## What Round 2 must not do

Resolve any of items (a) through (g) or the three VERIFY items. Pick a version number. Touch `src/` unless L1 is overruled by Beni in one word, and then only as the ruling says. Run `npm run sync` in any form except `-- --check`. Review or merge the bot's rolling `sync/scheduled` PR. Repaint a Fergie Time token; a token that fails AA is flagged to the design system. Write Round 3's build spec or its adjudication prompt. Merge PR #34; the PR comment is where Round 2 stops. Treat Round 1's measurements as evidence without re-running the ones it can; a rebuttal that says "accept" without a command beside it is a read, not a rebuttal.

## Precedence, for every seat in this cycle

Design system template > design brief > implementation prompt. The canvas outranks the brief; both outrank whatever build spec Round 3 writes. The repo outranks every description of it, this brief included. Every precedence resolution is noted in the PR comment. Fergie Time hard rule 6, the three lenses with Poster as default, the two clocks from one `kickoffUtc`, the motion budget, the shape-and-line rules, the four colour voices, the four-hour LIVE window, the `?only=` and `&date=` URL contract and the 390 / 360 / ~1000 matrix are settled and do not reopen.

## Deliver

First the logistics table (L1–L10) with decisions and owners, and the one-word questions for Beni gathered at its foot. Then the Round 2 prompt, in one fenced block in the chat message, one line per paragraph, a four-backtick outer fence because it carries the recipes above as sub-blocks, ready for Beni to paste into Codex. Then the archive commit: `docs/design-cycle-ucl-round-2-prompt.md` on `design/ucl-league-phase` and its `docs/README.md` row, authored `Claude <noreply@anthropic.com>` via per-commit `git -c`, with the harness's `Co-Authored-By` trailer. Say in your final message that the branch moved under Round 2 and by which commit.
