# Round 3 technical-resolution pass — the UCL league-phase design cycle (Claude Code, CTO seat)

You are the CTO seat: a fresh Claude Code session (Fable 5.1, Extra) in its own worktree of `BeniCheni/kickoff`. This is the technical-resolution pass of the three-round design review: Round 1 (Claude, PR #34) reviewed a Claude Design canvas against the repo; Round 2 (Codex) rebutted it with measured evidence and opened one new high finding; Round 3 is Beni's adjudication, and this pass gives him the evidence his remaining rulings need. You are not the reviewer, the rebutter, the designer or the builder. You reproduce or disprove the load-bearing claims that are still open, assess the implementation options and their dependencies, and return recommendations with closure criteria, so that the next seats can rule, correct the canvas, and only then write a build spec.

**Scope, fixed by Beni and not yours to widen.** This is a docs-and-disposable-probes pass. No authored change under `src/`. No edit to `src/data/*.json`. No `npm run sync` in any form except `-- --check`. No version bump, no `CHANGELOG.md` section, no tag, no merge of anything, no review of the bot's rolling `sync/scheduled` PR, no Fergie Time token repaint, no resolution of the canvas's not-decided items (a)–(g) or its VERIFY items, no release number, no build spec, no Claude Design prompt. Probes are uncommitted and restored; `git status --porcelain` shows nothing under `src/` or `tests/` before you commit. Your commits are documentation only, authored `Claude <noreply@anthropic.com>` through per-commit `git -c user.name=Claude -c user.email=noreply@anthropic.com`, never `git config`, ending with the `Co-Authored-By` trailer your harness gives you. You land on the existing branch `design/ucl-league-phase` (push `HEAD:design/ucl-league-phase` from a local branch of your own) and you say in the PR comment that the branch moved under the next seat.

**Precedence.** The repo is ground truth over every document named here, this prompt included. Design system template > design brief > implementation prompt; the canvas outranks the brief; neither overrides generated-data honesty. Settled and not reopened: the three lenses with Poster as default, the two clocks derived from one `kickoffUtc`, Fergie Time hard rule 6 (the Table's language extended, not restyled; the five domestic tables unchanged), the motion budget, the shape-and-line rules, the four colour voices with amber reserved for provenance, the four-hour LIVE window, the `?only=` and `&date=` URL contract the betting pipeline's Step 0 reads, and the 390 / 360 / ~1000 matrix with 375 in the phone set. The two-clocks rule is settled; finding 18 is about which zone feeds it, not whether two clocks exist.

## Pre-flight, before you write a number

```bash
cd ~/Documents/Claude/Projects/Kickoff
TZ=America/New_York date
git worktree list
git branch --show-current
git fetch origin
git rev-parse --short origin/main; git log --oneline 437826d..origin/main
git rev-parse --short origin/design/ucl-league-phase; git log --oneline origin/main..origin/design/ucl-league-phase
lsof -iTCP:5173 -sTCP:LISTEN; lsof -iTCP:5174 -sTCP:LISTEN
```

As of Wed 9 Sep 2026 12:35 EDT: `origin/main` was `437826d` (v0.3.2 plus one sync), `origin/design/ucl-league-phase` was `9ff470f` (Round 2's rebuttal on merge `1199aca`), PR #34 open, `MERGEABLE`, `CLEAN`, `verify` green at `9ff470f` (run 34375306815). Expect both to have moved: the scheduled sync merges itself when `verify` is green, and the PM seat may have landed archive commits on the design branch. Say what moved. Every fixture and standings count in the documents below is ESTIMATE at your head until you recount it. Do not switch the shared main checkout's branch, and do not touch the untracked `.round2-handoff/` files in it; work in your own worktree from a local branch name of your own. The main checkout may hold a dev server on 5173; serve the app from your worktree on its own port (`.claude/launch.json` → `kickoff-dev-worktree` on 5174, or another free port) and prove which checkout serves it by the header stamp. Saturday 12 and Sunday 13 Sep carry 25 and 14 domestic fixtures; if you run during matches, expect live rows in the snapshot, and note that v0.3.2 fixed the suite that used to go red on them.

## Read, in this order

1. `docs/design-cycle-ucl-review-pass-2.md` at the branch tip: the disposition table (findings 1–18), the provider receipts and their JSON paths, the matchday-window clustering and the resolver counterexample, the browser mechanism evidence with its three tables, the fourteen-hue contrast table, new finding 18 with its six counterexamples and reproduction command, and "What remains unresolved, ranked".
2. `docs/design-cycle-ucl-review-pass-1.md` (the hunt classes D1–D10 and the original findings), `docs/design-cycle-ucl-round-1.5-brief.md` (the reproduction recipes), `docs/design-cycle-ucl-spec.md` with its Round 2 correction notice (the canvas transcribed: rulings, both rule amendments, F1–F8, per-state tokens, items (a)–(g)), and `docs/design-cycle-ucl-brief.md` (a placeholder; the original brief is still not in the archive).
3. `docs/v0.2.6-ideas.md` rows 30–42 and both design-cycle process-note blocks at its tail.
4. `CLAUDE.md` and `AGENTS.md` in full; `docs/HONESTY.md` rules 1, 3 and 5; `.claude/skills/kickoff-pr-review/browser-matrix.md` for the pane methods (viewport before capture, the MessageChannel settle, the clock shim, one tab of your own). Read that file; do not invoke `/kickoff-pr-review`, which is a PR-review command that pushes and comments on a code diff. No `/kickoff-design-review` skill exists; ideas row 40 proposes one. Where the method you need is in neither place, say so and describe what you did.
5. The code the questions turn on: `scripts/providers/espn.ts` (`normalizeEvent`; line 166 `venueTz: COMPETITIONS[competition].tz`; lines 183–186, the deliberate decision never to set `round`; the 28-day chunking and the 100-event cap guard), `scripts/providers/espn-standings.ts` (`normalizeStandingEntry`; the `LEAGUE_TABLES` count cross-check), `scripts/sync.ts` (the preserve step for hand-authored `note`, around line 110; the report line), `scripts/diff.ts` (`DATE_MOVED`, the urgency window, `mergeVerdict`), `src/lib/schema.ts` (`round` at line 41, unused; `provider: z.literal('espn')` at 46, 57, 95; the identity comment at 122–123), `src/lib/time.ts` (`fixtureTimes`, `brooklynDate`), `src/lib/competitions.ts` (`RAW`, `LEAGUE_TABLES`, `TABLE_LEAGUES`, the false header comment at lines 4–5), `src/lib/standings.ts` (`tableFor`, `matchdayProgress`), `src/components/TablePage.tsx` (the per-row wrapper at lines 273–274, the sticky column header, the desktop header), `TabNav.tsx`, `CompetitionChip.tsx`, `FixtureRow.tsx` (line 89), `src/lib/urlCodecs.ts` (`parseLeague`, `canonicalSearch`), `src/index.css` (tokens; `[data-hot]` at line 184).
6. The canvas, served over HTTP from its own folder: `cd "$(dirname "$(git rev-parse --git-common-dir)")/../Kickoff European week design" && python3 -m http.server 8791`, then `http://localhost:8791/UCL%20League%20Phase%20-%20Design%20Cycle.dc.html` in the pane. Confirm SHA-256 `7fcd369554f5b0dc87f61bfc20046a83286d330fd7558da80b2a7a89d2d95785`, 138,306 bytes; if it differs, the designer has revised it and you say so before anything else. Local-only; never committed.

## Baseline at your head

`npm run typecheck`, `npm test`, `npm run build`. Report the real numbers (Round 2 read 344 tests in 27 files at `9ff470f`; report yours, not that).

## The questions, in the order they block design approval

Each question says what to reproduce or disprove, the options to assess, the evidence to return, and what closes it. End each with a one-line recommendation and an S / M / L feasibility estimate labelled ESTIMATE. Where a question needs Beni's ruling, end it with the one-word question he must answer.

### Q1 — Venue clocks (finding 18, ideas row 42). Blocks implementation; P1.

Reproduce first. Fetch the UCL MD1 range to your scratchpad and run Round 2's command against the real functions:

```bash
curl -s "https://site.api.espn.com/apis/site/v2/sports/soccer/uefa.champions/scoreboard?dates=20260908-20260911" -o "$SCRATCH/ucl-r2.json"
./node_modules/.bin/tsx -e 'import fs from "node:fs"; import {normalizeEvent} from "./scripts/providers/espn.ts"; import {fixtureTimes} from "./src/lib/time.ts";
const e=JSON.parse(fs.readFileSync(process.env.SCRATCH+"/ucl-r2.json","utf8")).events.find((e:any)=>e.id==="401915446");
const f=normalizeEvent(e,"ucl","2026-09-09T15:43:38Z")!;
console.log(f.venueTz,fixtureTimes(f.kickoffUtc,f.venueTz),fixtureTimes(f.kickoffUtc,"Europe/London"));'
```

Expected at `9ff470f`: `Europe/Zurich`, a 21:00 local where Anfield's true local is 20:00, Brooklyn 15:00 in both. Then extend it to all eighteen events and confirm six wrong (Athens, Porto, Liverpool, Lisbon, Istanbul, Manchester). Note what the provider gives you to work with: `competitions[0].venue` carries `id`, `fullName` and `address.{city,country}` and no time-zone field; the PM seat's probe on 9 Sep found the country strings are ESPN's own ("England", "Türkiye", "Czechia") and the city unreliable (Signal Iduna Park listed under "Aue").

Assess three sources for the zone. (A) A hand-authored country → IANA reference table keyed on `venue.address.country` as ESPN spells it, with a per-venue override list for multi-zone countries, applied in `normalizeEvent` for every competition, domestic included: the domestic path shares the assumption today and is correct only because no Big-5 club currently plays outside its country's primary zone (no Canary side in La Liga this season; check it yourself). (B) A per-club home-zone table keyed by ESPN team id: count the rows it needs across the three UEFA competitions' standings (108 clubs) and say what a neutral venue does to it. (C) A provider field: confirm none exists in the event, the competition or the venue objects. For (A), pull all three competitions' 36-entry standings and the MD1 events, inventory every country string ESPN uses, mark which countries span more than one zone (Kazakhstan, and Spain's and Portugal's islands at minimum; Russia is not in the competitions), and propose the override key (venue id) and its first rows.

Design the honest fallback and say what the app renders when the country is missing, unmapped or contradicts the venue override: the recommendation the PM seat put to Beni is that the fixture carries no `venueTz`, the local pill renders "local time not known" while the Brooklyn clock renders normally, and the sync report names the count; the competition zone is never inherited as a guess. Say whether the schema needs `venueTz` to become optional, what `fixtureTimes` and every renderer of the local clock do with an absent zone, and what the diff engine reports when a zone changes.

Return: the 108-club country inventory with proposed zones; the six counterexamples rendering 19:45 / 20:00 correctly through the real `normalizeEvent` → `fixtureTimes` with your proposal applied as a probe (a temporary, uncommitted mapping is fine; it must not be committed); a zone-consistency test specification over the whole snapshot (every fixture's `venueTz` agrees with its venue's country); the missing-country path rendered in the pane in one lens with the viewport at 390. Closure: those six lines correct, Brooklyn unchanged, the test named. One-word question for Beni: fallback state as recommended, yes or no.

### Q2 — Matchday round identity (finding 11, ideas row 36, HONESTY rule 3). Blocks design approval and implementation.

Reproduce Round 2's resolver counterexample (Sep 9 → MD1; Sep 30 → null; the same event on Oct 13 → MD2, silently) and its clustering (five uncapped month slices, 144 unique events, 18 per window, zero outside). Distinguish two things the canvas conflates: progress (`max(played)` of 8, which F2 fixes) and round identity (which matchday a fixture belongs to, which nothing synced carries: `week` absent, `notes` empty, phase only at `events[].season.slug`).

Assess three policies. (A) A first-seen baseline in the sync: at first sight of an event the sync derives the matchday from the hand-authored windows and writes it to the existing, unused `round` field, then preserves it by event id exactly as it preserves hand-authored `note`; a later kickoff whose Brooklyn date falls outside the baseline window renders "MATCHDAY n · RESCHEDULED" (the `DATE_MOVED` diff already fires on that change); a first sighting outside every window renders the absence state. Trace this through `scripts/sync.ts`'s preserve step, `scripts/diff.ts` and the schema, and say what changes. Quantify the residual honestly: an event moved into another window before the first-ever sync would be baselined wrong with a computed label; state how many days separate the UEFA draw from the first activation sync, and whether any of today's 144 events already sits outside the window its identity implies (Round 2 found zero outside any window; that does not prove none has moved between windows). (B) Option 1 from the brief: show no matchday. (C) A hand-authored exceptions list beside the windows, which is a file nobody can contradict, again. Say which of (A)'s three states the canvas's 1c artboard already draws and which it lacks.

Return: the three synthetic tests as specifications (in-window transfer detected; out-of-window absence; first-seen-moved residual named); the amended rule 3 sentence you would propose, naming the residual explicitly; the feasibility estimate for (A). Closure: Beni chooses A, B or C on this evidence, and the accepted residual, if any, is written into rule 3 rather than discovered later. One-word question for Beni: A, B or C.

### Q3 — The sticky region header and desktop (finding 2, ideas row 32, item (g)). Blocks design approval for (g).

Reproduce Round 2's four measurements at 390, 375 and 360 with the pane's JavaScript tool on the Table tab, Premier League: nested (the canvas recipe on the real divider inside its per-row wrapper), hoisted, a synthetic grouped 16-row band (clone a real row sixteen times inside one wrapper, then a twelve-row band after it; measure the divider at offsets 200, 600, 850, 900), and the CSS-only alternative (`display:contents` on every per-row wrapper, every divider `position:sticky; top:28px; z-index:1; background:var(--bg)`). Restore the DOM after each and prove it (computed `position` static, node counts equal).

The design rule Beni is being asked to uphold is scoped: sticky only when a band exceeds eight rows, and the five domestic tables unchanged. Propose the DOM shape that implements that rule and no more: per-band grouping (rows grouped by zone so the divider's containing block is its band) versus a conditional application of the CSS-only recipe to deep bands only. For your recommendation, prove hard rule 6 with a before/after computed-box diff over the Premier League table at 390 and 1000 (every row's top and height; the dividers' positions) and say what the jsdom rig can and cannot assert about it (layout is browser-only; say which assertions belong in `tests/dom/` and which in the browser matrix).

Desktop: the column header is `position: static` at 34 px and rows are 46 px, so sixteen rows are 736 px. Measure against 1000 × 700, 800 and 900 viewports. Lay out the two options for (g): no sticky on desktop (the canvas), or a sticky header plus sticky divider on desktop as well, with what each costs. Do not rule.

Return: the measurement tables; the DOM proposal with its release behaviour at the band boundary at all three phone widths; the hard-rule-6 diff; the desktop measurements and options. Closure: Beni rules (g); the designer confirms the scoped rule. One-word question for Beni: desktop sticky, yes or no.

### Q4 — Picker order and the third tab (findings 3 and 4, ideas rows 33 and 34). Designer's layout; your mechanism.

Confirm Round 2's numbers by injecting the sixth chip appended and prepended, and the third tab, at 390, 375 and 360 (picker 62.5 / 62.5 / 96.75 appended, 96.75 at all three prepended; nav 37.5 → 65 at 390 and 375, 65 → 65 at 360). Then answer the build question: `TABLE_LEAGUES` follows `COMPETITION_KEYS` insertion order, in which `ucl` precedes `laliga`. Propose the smallest mechanism that renders the ⭐ chip last as drawn (an explicit order for the Table's picker, or a sort by group with domestic first) and say what else reads `TABLE_LEAGUES` (`parseLeague`, `canonicalSearch`, tests). Confirm `?league=ucl` round-trips through `parseLeague` once the entry exists, and that `tab=moments` would be an additive value that leaves `?only=` and `&date=` untouched.

Return: the numbers, the mechanism, and what it touches. Closure: the designer's layout ruling is recorded; the mechanism is an S build item. No question for Beni beyond the PM packet's D9.

### Q5 — Progress, phase and the post-MD8 assumption (finding 7, F2, F3, ideas row 41). Acceptance criteria.

Confirm F2 and F3's shape (`LeagueTableMeta.matches?: number`, `phase?: string`; `matchdayProgress` using `meta.matches ?? (teams − 1) × 2`). The canvas's sentence "As of matchday 8 of 8 · league phase" is asserted to stay true after MD8 because the standings child freezes at 36 rows; today's feed cannot prove it. Propose the guard as an acceptance criterion: the UCL bands and progress line render only while the standings child is the 36-row League Phase (its name and count both checked at sync, like the `teams` cross-check), and say what the Table shows otherwise. On the callout (finding 16): implement whatever Beni rules on the PM packet's D2 as an acceptance criterion; do not choose copy.

Return: the criterion text and where the check lives. Closure: in the build spec.

### Q6 — Component-ground contrast for new text (findings 8 and 9, ideas row 35). Build acceptance criterion; design-system decision flagged.

Inventory every text surface the design introduces and every ground it renders on, by lens and theme: the matchday provenance line (floodlight-strong on bg, and on Broadcast's hot-row `--surface`), the divider range text, the progress line's phase suffix, and on the Moments tab the banner title, card title, meta line, provenance line and link-out pill, on `--surface` and on `--media` where any text overlaps it. Compute every ratio from the tokens in `src/index.css` (unrounded 4.5 threshold for text under 18 px; 3:1 for non-text where it carries information) and confirm from the live DOM in the pane for at least the matchday line in Broadcast dark on a hot row.

The UCL competition name in `FixtureRow.tsx` line 89 is new text on an existing surface and fails at 3.477 (cream), 3.793 (white) and 4.010 (Broadcast dark hot); the brief's ruling is that existing domestic debt does not waive it. Return component-level options within the design system's rule that token repaints are the designer's and component-level AA fixes are fair game: for example, the meta line's competition name in `--ink-muted` with the hue carried by the flag and the rail (a change all fourteen competitions would inherit, so hard rule 6 does not apply but the Fixtures rows' look does); or per-hue text-safe variants as reference data in `competitions.ts` (row 35's design-system patch, fourteen hues); or a UCL-only exception (inconsistent, listed to be rejected). Say which is a design-system decision and which a build decision.

Return: the inventory table, the ratios, the options with what each changes on screen. Closure: the designer chooses with Beni's nod; the build criterion is "no new text below 4.5:1 on any ground it renders on". One-word question for Beni: none; the designer's.

### Q7 — Moments: storage, history, stills (items (c), (d), (e); F7; findings 10 and 38). Feasibility for Beni's ruling.

Assess `src/curated/moments.json` against `src/data/moments.json` through everything that touches `src/data/`: `scripts/sync.ts`'s writes and preserve step, `fixturesFileSchema` and the validate script, the diff engine, the staleness clock and the header stamp, Vite's import of the snapshot, and the authoritative-snapshot boundary in HONESTY rule 5 ("membership needs an explicit decision"). Propose the F7 Zod schema (`{id, category, fixtureId, title, source:{name,url}, still?:{url,credit}, curatedAt}`) and say how a Moment's `fixtureId` is validated against the snapshot and what the tab does when the referenced fixture leaves the sync window. (d): the URL codec for `tab=moments`, push versus replace consistent with the Table tab, and the `main.tsx` pre-paint decode. (e): `object-fit: cover` in the 16:9 frame versus letterbox on `--media` when a rights holder's frame is not 16:9; note that no image is hosted, only linked, so what the app can know about the frame's ratio before render.

Return: the option comparison with what each costs, the schema, the S / M estimate. Closure: Beni rules (c); the designer rules (d) and (e). One-word question for Beni: curated or data.

### Q8 — The tie-break sentence and UEFA's rescheduling provisions (item (b); two VERIFY items). Sources.

If your harness has network, locate UEFA's regulations for the current Champions League cycle and cite, by document title and article number, the league-phase ranking criteria (the order of tie-breakers) and the provisions on postponed or rescheduled league-phase matches. Write the `tieBreak` sentence in the house style of the domestic ones in `competitions.ts`, sourced. If you have no network, say so in your first paragraph and hand Beni the exact fetch to run; do not reconstruct the criteria from memory.

Return: URLs, article numbers, the sentence, and what the rescheduling provisions imply for Q2's policy. Closure: a sourced sentence in the build spec; the windows VERIFY either closed by a UEFA statement of the matchday dates or left ESTIMATE with the reason.

### Q9 — Activation checklist, dependencies and sequencing. The one-page summary Beni reads first.

From Q1–Q8, produce: (1) what must be resolved before design approval; (2) what becomes a build acceptance criterion; (3) what can be deferred independently without weakening the experience (the canvas's own list: UEL and UECL screens, domestic matchday markers, strength of schedule, hue change, in-app playback, a Results tab; add anything your evidence adds). Then the dependency graph for activation: `espnCode: 'uefa.champions'` and `LEAGUE_TABLES.ucl` (`teams: 36`, the zones, `matches: 8`, `phase`), the false comment at `competitions.ts` 4–5 (row 31), the venue policy (Q1), the round policy (Q2), the picker order (Q4), the count growth (144 UCL events plus 36 standings rows in the snapshot; the 28-day chunk cap headroom, 54 observed against 100; any test that assumes today's counts; `verify`'s runtime), and ideas row 29 (auto-merged syncs never reach Pages), which the PM seat treats as a deploy-correctness prerequisite for any flagship release and proposes to carry in the v0.4.0 sync theme; say whether you agree. Unified versus staged for UCL plus Moments: recommend staging only on a concrete dependency or verification advantage you can name; otherwise unified. Give S / M / L estimates per item as the implementing seat's estimate, labelled ESTIMATE, and no dates.

## Deliverables

1. One docs commit on `design/ucl-league-phase`: `docs/design-cycle-ucl-round-3-technical-resolution.md` (Q1–Q9 with evidence, recommendations, closure criteria and the one-word questions), a row in `docs/README.md`'s archive table (`open (item f)`), and, if the PM seat's two files are not yet in `docs/`, copies of `design-cycle-ucl-round-3-decision-packet.md` and `design-cycle-ucl-round-3-cto-prompt.md` from `../Sportsbooks/` with their own rows. `verify` green at your tip.
2. One PR comment on #34, in the shape of `.claude/skills/kickoff-pr-review/pr-comment.md` adapted: the verdict line is a readiness statement (what blocks design approval, what blocks implementation, what rides), then a table of Q1–Q9 with recommendation, closure and blocks, then the matrix as run (which widths, which probes, restoration proved, `scrollWidth === innerWidth` per cell), then "not verified", then the line that the branch moved under the next seat and by which commit.
3. In chat: the Q9 summary on one page, then the list of one-word questions for Beni that your evidence changes, then a short retro: what a technical-resolution pass needed that neither `/kickoff-pr-review` nor the Round 1 and Round 2 methods provided, as raw material for ideas row 40.

## Must not

Author under `src/` or `tests/`; edit `src/data/*.json`; commit any probe, scratch file or fetched JSON; run `npm run sync` except `-- --check`; choose a provider, a policy, a directory, a release number or a canvas correction on Beni's or the designer's behalf; repaint a token; write the build spec or the designer's prompt; merge, tag or dispatch a workflow; treat Round 1's or Round 2's numbers as your own without re-running the ones you can; report a claim you could not re-run as verified.
