# v0.5.0 review prompt — Pass 0.5 (PM/TPM sprint prioritization for PR #39)

Written cold, 12 Sep 2026, from a fresh read of the repo and PR #39. This session ran the PM/TPM
seat (`CLAUDE.md`'s Release management roles) rather than a Pass 1 cold review, because the
invoking request asked for sprint prioritization and an Executive Pass 1 Handoff prompt, not an
adversarial review — the bare `/kickoff-pr-review 39 --no-merge` command defaults to Pass 1, so
this deviation is recorded rather than silent.

## Where PR #39 actually stands (fresh read, 12 Sep 2026 ~10:20 ET)

- `codex/european-week` → `main`, tip `17c24b0` (Beni's own version-bump commit, 11 Sep
  02:35 UTC). `mergeable: CONFLICTING`, `mergeStateStatus: DIRTY`.
- `main` has moved twice since: v0.4.0 (`b4b6699`, 11 Sep) and v0.4.1 (`cdd4680`, 12 Sep), plus
  the v0.4.1 retro docs PR (#46, `7b13d12`, adds ideas row 52) and several `sync: verified
  unchanged` / real sync commits, most recently `77c5dd2` (7:39 AM ET) and one more `sync.yml`
  run completing successfully at 11:39 AM ET today (run 34691606325).
- The branch-refresh ("Prompt 2" in the 11 Sep plan) has not been sent: no refresh branch exists
  for PR #39 (`codex/sync-v0.4.0` and `codex/sync-v0.4.1` are separate, already-shipped work).
- `sync.yml` is **not** paused — still on its live cron (`23 1,4,7,10,13,16,19,22 * * *` UTC) and
  ran again this morning. Beni's 11 Sep ruling #2 ("pause sync.yml at the Prompt 2 paste") has
  not yet been actioned.
- Already resolved, no fresh ruling needed: release number is v0.5.0 (`CLAUDE.md`); competition
  chips stay as implemented, designer re-mirrors post-release (`CLAUDE.md` Roadmap pointers).

## Design review — nothing new blocks v0.5.0

Cross-checked `Kickoff European week design/UCL League Phase - Design Cycle.dc.html`
(138,306 bytes — matches the SHA-256/byte count PR #39's own body cites as the implemented
canvas) and the `Kickoff European week design Boosted/Executive Handoff Brief.md` (a VIP-corrected
canvas revision, Wed 9 Sep, that PR #39's body says "was not saved" — its rulings and
corrections were implemented directly as text). All ten of the Boosted brief's Round-3 rulings
reconcile 1:1 with PR #39's own "Ten rulings as implemented" table. The Boosted canvas is a
superseded/orphaned reference, not new scope. `Fergie Time Design System/` is token/DS-mirror
reference only; `src/index.css` + `src/lib/competitions.ts` remain the source of truth.

Both the canvas (ruling 4) and PR #39's own "Deliberately not done" list say, independently,
that Moments ships as a linked still with **no in-app playback** — a decision made twice, not
an oversight.

## Roadmap grooming toward v0.5.0 and beyond

v0.5.0 ships infrastructure (UCL sync, venue clocks, matchday provenance) plus an intentionally
**empty, hand-curated-only, link-out-only** Moments tab — zero video hosting, zero curation
automation, zero licensing exposure, by design.

Ranked next candidates once v0.5.0 tags:
1. Hand-curate the first real Moments entries into `src/curated/moments.json` — zero
   engineering, the schema and validator already ship in #39.
2. Chip re-mirror to Fergie Time DS (already promised in the README Roadmap).
3. Ideas rows 35 (full 14-hue contrast inventory) and 40 (`/kickoff-design-review` skill).
4. The betting-bridge in README's "Beyond" section (`positions.json` join, token-expiry-vs-
   kickoff badge) — worth raising with the pipeline PM seat now that fixtures carry stable ids
   across two competitions.
5. **Separate, proposal-gated:** in-app video/highlights playback. This reverses the decision
   above, made twice. Before it becomes a backlog row it needs its own cold proposal covering
   rights/licensing basis, hosting cost, and what a "curation algorithm" would even optimize
   against — there is currently no engagement telemetry in this app to train one on. Does not
   block item 1.

## Sequencing (two prompts, in order)

**Step 1** — Codex/GPT-6 Astra, Extra High reasoning (Beni's 11 Sep ruling #4), refreshes the
branch: merge `main`, re-sync via `--baseline-dir` exported from the branch's own pre-merge tip
(the technique that already worked once in this PR's Pass 2 rebuttal, commit `1832b2c`),
resolve the known hazards, recount everything, re-verify green. **Before pasting it, pause
`sync.yml`** (Beni's ruling #2) — open question below on which day.

**Step 2** — once Step 1's PR shows `MERGEABLE` and `verify` green at the new tip, paste the
Pass 1 cold-review prompt into a fresh Claude Code session (Opus 5, Extra reasoning effort,
substituting for Fable 5.1 Extra per `CLAUDE.md`'s routing line, because Fable 5.1 is
unavailable until Monday afternoon — record this substitution in the PR comment).

Both prompts were delivered in chat, one line per paragraph, ready to paste; this file is the
archive copy only, per the prompt-ladder rule.

## Open questions for Beni (one word each where possible)

1. Pause `sync.yml` today (a 25-fixture Saturday) or wait for a lighter match day, matching
   your own stated criterion for ruling #2?
2. Still Codex/Astra Extra High for the refresh, or swap given the same Monday constraint?
3. OK to actually flip "Allow auto-merge" off / disable `sync.yml` now, or do you want to do it
   yourself in GitHub settings?
