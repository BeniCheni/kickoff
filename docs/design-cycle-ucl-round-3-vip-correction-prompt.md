# VIP correction and sign-off pass — the UCL league-phase design cycle (Claude Design, VIP designer seat)

You are the designer of the canvas `UCL League Phase - Design Cycle.dc.html` in this project (SHA-256 `7fcd369554f5b0dc87f61bfc20046a83286d330fd7558da80b2a7a89d2d95785`, 138,306 bytes, unchanged since you produced it on 8 Sep 2026). Three review rounds have run against it: a cold review by Claude Code, a rebuttal by Codex with measured evidence, and a technical-resolution pass by the CTO seat, after which Beni ruled on every open item that touches the canvas. This pass produces one corrected revision of that same file and records your fidelity sign-off on it. It is a correction pass, not a redesign: every ruling below is settled, nothing on the canvas moves except what this prompt names, and no Fergie Time token is repainted.

**Precedence, unchanged.** Design system template > design brief > implementation prompt. The canvas outranks the brief; both outrank any build spec. The repo is ground truth over every description of it, this prompt included: where a number here disagrees with a file you can read in the repo, the file wins and you say so in your revision notes rather than choosing silently.

**The record, in reading order, all in the Kickoff repo on branch `design/ucl-league-phase` (PR #34) at `9d09b43` or later under `docs/`:** `design-cycle-ucl-round-3-rulings.md` (Beni's ten rulings, what each fixes), `design-cycle-ucl-round-3-technical-resolution.md` (the CTO's measured answers Q1–Q9), `design-cycle-ucl-round-3-decision-packet.md` (the PM packet; its section 5 is the sign-off packet's shape), `design-cycle-ucl-review-pass-2.md` (Codex's rebuttal, with the contrast inventory and finding 18), `design-cycle-ucl-review-pass-1.md` (the cold review) and `design-cycle-ucl-spec.md` (the transcription of your canvas as it stands). Every fact you need is restated below so the pass can run from this prompt alone; the record is where to go when a restatement seems wrong. The original design brief is not archived yet (its placeholder is `design-cycle-ucl-brief.md`); cite the spec transcription, not the brief.

## What is settled and must not reopen

Your canvas's five rulings stand: the matchday is derived from UEFA's published windows and says so in the amber provenance voice; `--comp-ucl` stays `#8B6FE8` and identity comes from the ⭐ glyph, the 3 px rail and the day-level treatment; the 36-row table keeps the rail and gains the sticky region header for a band deeper than eight rows, with no new colours; Moments ship as shape 2 (a linked still on `--media` inside a 4 px card, no in-app playback, a third tab, empty state primary); the never-list amendment for the Moment still stands as written. Fergie Time's hard rules stand: three lenses with Poster default, both clocks from one `kickoffUtc`, the motion budget, the single-gradient rule, the four colour voices with amber reserved for provenance, hard rule 6 (the Table's language extended, not restyled; 1e is the proof), the four-hour LIVE window, and the `?only=` and `&date=` URL contract. The candidate hue `#5C7CE6` stays rejected. UEL and UECL screens are not designed in this pass. Release numbering is item (f), Beni's alone: "this release" throughout, no number anywhere on the canvas.

## Beni's ten rulings, verbatim, and the artboards each one reaches

| # | Question | Ruling | Reaches |
|---|---|---|---|
| 1 | Venue-clock fallback as the CTO recommended | Yes | 1a (and 1h F9) |
| 2 | Round identity: A first-seen baseline, B no matchday, C exceptions list | A | 1c, 1h (F4, rule 3) |
| 3 | Desktop sticky region header, item (g) | Yes | 1d, 1h (F5, per-state tokens) |
| 4 | Moments location, item (c): curated or data | Curated | 1g, 1h (F7) |
| 5 | A competition whose standings table structurally ends: abort or degrade | Degrade with a report line | 1h (F10, one note) |
| 6 | D1: uphold the scoped sticky rule (deeper than eight rows only; domestic tables unchanged) | Yes | 1d, 1e, 1h |
| 7 | D2: reword the games-in-hand callout for phase competitions | Yes | 1d |
| 8 | D5: ESPN is the provider; a future provider round string outranks the derived window | Confirm | header block, 1h (F1) |
| 9 | D9: sixth chip last with a third row at 360; the 65 px tab row with three tabs | Approve | 1g, 1e caption, a 360 board |
| 10 | Remove the "Beni's curl check, 8 Sep" line | Yes | header block, 1h |

## The revision, artboard by artboard

**Header block and the blocking finding.** Delete the blocking finding and its attribution (rulings 8 and 10). Beni did not run the check the canvas credits him with, and the check itself fails from every network that has tried it: ESPN serves `uefa.champions`, `uefa.europa` and `uefa.europa.conf` scoreboards in their matchday windows (18 events per window; 144 UCL events across the phase, 144 UEL, 108 UECL) and a 36-entry league-phase standings child for each, in the exact shape the repo's normaliser reads; 21 of the 36 UCL club ids already sit in the domestic standings file. What ESPN does not carry is a per-event matchday: `week` is absent on every event, `notes` is empty, and the one text field that looked like a marker reads "UEFA Champions League, League Phase" on all of them; the phase lives at `events[].season.slug = league-phase`. So item 1's option 3 is off the table because no round string exists, not because no provider exists. Replace the block with two sentences saying exactly that, and make the header chrome honest: keep the figures you read at `22ca01a` (`v0.3.1 · 1007 fixtures · synced 2026-09-08 00:30 UTC`) but label the chrome ILLUSTRATIVE, read at `22ca01a`, and make every "synced … ago" on the canvas agree with that stamp and with one stated illustrative "today" (at the Wed 9 Sep 11:00 EDT the boards imply, the age is 38.5 hours, not 3). One "today", one stamp, one age, everywhere.

**1a European matchday, three lenses.** Add one state (ruling 1): the expanded row's local pill, which today reads the stadium clock ("🏴 8:00 PM local", the `rounded-full border border-line bg-surface px-2 py-0.5` pill at 11 px), renders "local time not known" when the fixture carries no venue zone, in the amber provenance voice you already use for the matchday line (`font-mono text-[10px] font-medium tracking-[0.06em] uppercase text-floodlight-strong`, which measures 5.18 on the background and 5.65 on `--surface` in light, 11.21 and 9.71 in dark). The Brooklyn pill beside it does not change; the Brooklyn clock is always right. Draw it once, in one lens at 390, on one row, with a caption saying when it fires: a fixture whose venue in the feed has no country to map, which is live today (Sabah FK's four home ties arrive with no venue object at all), or a country the reference table does not know. Do not show a wrong stadium time as a "fallback"; the whole point of the state is that the app now refuses to. If you choose contrast option (i) on 1f, the rows on 1a and 1b show the meta line's competition name in `--ink-muted` with the hue kept on the flag and the rail.

**1b mixed week.** No change beyond the contrast choice above and the illustrative-today consistency. Saturday's leagues count stays labelled ESTIMATE; the build recounts it at its head.

**1c matchday marker and its absence.** Add the third state (ruling 2). The three states, side by side at day level in the same amber mono voice: COMPUTED ("MATCHDAY 1 · COMPUTED — UEFA WINDOW 8–10 SEP", as drawn), absent ("MATCHDAY — · DATE OUTSIDE UEFA'S PUBLISHED WINDOWS", as drawn), and RESCHEDULED, your copy, for a fixture whose baselined matchday no longer matches the window its kickoff now sits in (something like "MATCHDAY 1 · RESCHEDULED — NOW OUTSIDE ITS UEFA WINDOW"; keep the size, the placement and the voice of the other two). Caption the mechanism in one sentence each: the app fixes a fixture's matchday from the windows the day it first sees the fixture and keeps it; UEFA's Article 27.01 says a match that cannot be played is, as a rule, played the next day, so the common reschedule either stays in its two-day window (Tuesday to Wednesday, still COMPUTED) or leaves every window (Wednesday to Thursday, or matchday 1's Thursday to Friday, the absent state); Article 27.02's new date, the rarer case, lands inside another matchday's window and fires RESCHEDULED. Do not draw the residual here; 1h states it.

**1d league-phase table.** Four changes. First (rulings 3 and 6), state the sticky rule as scoped on both widths, in the words the resolver uses: a band deeper than eight rows (`to - from + 1 > 8`), so no domestic band, none of which exceeds eight rows, ever gets a sticky divider. Second, add a desktop board at about 1000 px wide and 700 px tall showing the column header stuck at the top and the KNOCKOUT PLAY-OFFS · 9–24 divider stuck beneath it, with a note that the desktop column header is `position: static` today (34 px) and becomes sticky in this release, the divider sitting at `top` 34 px; annotate the release at the band's end, because the sixteen-row band is 736 px plus the divider and overflows a 700 or 800 px viewport but fits 900. The per-state tokens on 1h change from "Desktop: range only, no sticky" to sticky on both widths, `top-7` on mobile and `top-[34px]` on desktop, with the range text on both. Third (ruling 7), for competitions with `phase` set the games-in-hand callout states the fact and drops "sort by PPG to compare like with like"; write the copy and draw the callout mid-matchday, when twelve clubs have played one more than the other twenty-four. Fourth, replace `tieBreak: VERIFY` with the sourced sentence below (Article 18 of the UEFA Champions League regulations 2026/27, fetched by the CTO from `documents.uefa.com`), in the legend's existing house style. The progress line stays "As of matchday 1 of 8 · league phase · synced …", with the age made consistent with the header. The three bands and their colours do not change; ESPN's own standings note splits 9–24 into seeded and unseeded, which the legend may mention and the bands do not show.

```
The Champions League league phase separates level clubs on goal difference first, then goals scored, then away goals, then wins and away wins; only after that do the opponents' collective records, disciplinary points and the club coefficient come into it (Article 18, UEFA Champions League regulations 2026/27).
```

**1e Premier League table, unchanged.** Correct one word and add one sentence. "Pixel-identical" becomes "unchanged": the canvas draws rows at 55 px content-box where the app renders 54 px border-box, one hairline of the canvas's own box model, and every token and class on the board is the app's. The sentence: no domestic band is deeper than eight rows, so no domestic divider is ever sticky at any width, which is how hard rule 6 is kept. Nothing else on 1e moves; it is the proof board and it stays boring.

**1f colour sheet.** No hue change. Two corrections and one choice. Corrections: the sheet measured the UCL name on cream and night only; add the two grounds Broadcast paints, white for light hot rows and selected chips, and `#142a1f` (`--surface` in dark) for dark hot rows, so the row reads 3.477 cream, 3.793 white, 4.631 night, 4.010 dark hot, and note that Broadcast's small text is 10.5 px where the other lenses use 11. And the `--media` pair: `#e4e0d2` against `#efead9` in light is 1.10, not 1.14; either compute it in the same render-time logic the hue figures use, or print 1.10 and say the figure is typed. `--media` stays the flagged fifth surface role and a decorative ground that never carries text (muted text on it in light measures 4.12). The choice (decision D10, yours to make within design-system rule B4, which forbids repainting a token without a design-system decision): the UCL competition name at 11 px is new text on an old surface and the existing domestic debt (all five domestic hues fail cream and dark hot) does not waive it. Option (i): the Fixtures rows' meta line renders the competition name in `--ink-muted` with the hue on the flag and the 3 px rail, which measures 4.99 and 5.44 on the background and `--surface` in light and 6.04 and 5.23 in dark, a component-level change all fourteen competitions inherit and a visible change to every domestic row that you must draw on 1a and 1b and own. Option (ii): per-hue text-safe variants as reference data, fourteen hues, which is ideas row 35's design-system patch and a token decision flagged to Beni rather than made here; if you choose it, the UCL name stays in its hue on this revision with the debt named beside it. A UCL-only exception is not on the menu. Write the choice and its consequence on 1f.

**1g Moments.** Three changes and two decisions. The footer's `src/data/moments.json` becomes `src/curated/moments.json` (ruling 4), matching 1h. The tab caption: three tabs stack the lens switcher above the tab row at every phone width (tabs 193.80 px plus the 16 px gap plus the switcher 201.87 px is 411.67 px), which raises the nav from 37.5 to 65 px at 390 and 375 and adds nothing at 360, where two tabs already stack; "under ~375" is wrong and comes out. Draw the tab row at 390 and 360 as it will render with Fixtures · Table · Moments (ruling 9 approves the 65 px row). Decisions (d) and (e), yours, recorded on the board: (d) the Moments tab pushes history like Table does, which the app gives you for free (its tab hook already pushes), so record it rather than redesign it; (e) the still's aspect ratio when a rights holder's frame is not 16:9: `cover` inside the 16:9 frame with letterboxing on `--media` past a tolerance you state, or letterbox always; both are one class, the app cannot know the ratio before the image loads, so say which and why. Populated and empty states both stay; the empty state stays primary.

**1h amendment and handoff spec.** This board carries most of the text changes. F1 is rewritten (rulings 8 and 10): provider ESPN, `uefa.champions`, the normaliser's shape, the domestic id namespace, no per-event round string; and the precedence sentence for a provider that does not exist yet: a provider's round string, when one exists, outranks the derived window, so this cycle the derived window is the only matchday source. F4 gains the first-seen baseline (ruling 2): `round` is set once from the published windows on the day the sync first sees a fixture that carries none, carried forward by fixture id exactly as the hand-authored `note` is, guarded by the same season constant as the zones; three render states, COMPUTED, absent, and RESCHEDULED when the stored round no longer matches the window the kickoff now resolves to; and the accepted residual in one sentence: a fixture already moved between windows before the app first saw it is numbered by the window it was found in, and that is the one case the rule cannot detect. Rule 3's amended last sentence becomes the block below, verbatim, replacing the canvas's current amendment. F5 names the DOM shape (ruling 6): one container per zone band with the divider as its first child, sticky only when the band is deeper than eight rows, on both widths, the desktop header becoming sticky; and it records that the CSS-only alternative (every divider sticky and opaque behind `display: contents` wrappers) is a different design because it fires on every band, and is not adopted. F7 names the location `src/curated/moments.json` (ruling 4), keeps the exclusion sentence (outside the authoritative snapshot boundary, the diff engine and the staleness clock by explicit decision), and adds a denormalised `fixture: { home, away, kickoffUtc, competition }` captured at curation, because the sync window runs thirty days back and a September Moment's fixture leaves the snapshot in October while the card must still render its match line. Two new findings: F9, venue time zones (ruling 1): the stadium clock's zone comes from a hand-authored country-to-IANA reference table keyed on the venue country as ESPN spells it, with a per-venue override map keyed by ESPN venue id that is empty this season and structurally required (a Canary or Madeira side, a neutral venue), applied to every competition including the five domestic ones; the zone is optional, an absent zone renders the "local time not known" pill and never the viewer's own zone, the sync report counts `zones-unknown`, and the diff engine gains a non-urgent zone-changed kind. F10, a structurally ended table (ruling 5): the UCL bands and progress line render only while the standings child is the 36-row league phase; when that child changes shape after the phase ends, the sync drops that competition's table for the run with a report line and every other competition lands as usual; the app shows its existing "No table in the snapshot" state for the competition, and you decide whether that copy (which today tells the reader to run a sync) serves a phase that has ended or whether one phase-ended sentence is drawn; a fetch or validation failure still aborts the whole sync as before. Item (g)'s "≥8 rows" becomes "deeper than eight rows" to match the rule. The not-decided list is rewritten: (a) closed, ESPN, with the precedence sentence; (b) closed by the sourced sentence; (c) closed, `src/curated/`; (d) and (e) recorded on 1g; (f) open, Beni's; (g) closed, sticky on both widths under the scoped rule. The VERIFY list: tie-break order closed by Article 18; Saturday 12 Sep's league count recounted at the build head; whether UEFA's windows shift for a rescheduled tie closed by Articles 23 and 27 with the first-seen baseline, while the eight window dates themselves stay ESTIMATE until Annex C of the regulations is read. The media figure reads 1.10. The curl line is gone from this board too.

```
Matchday numbers are not invented either. Where the provider exposes none, the app derives the number once, from the governing body's published windows, on the day it first sees the fixture, labels it computed, and keeps it; a fixture whose date later leaves that window is shown as rescheduled from it, and a fixture first seen outside every window is shown without a number. A fixture already moved between windows before the app first saw it is numbered by the window it was found in, and that is the one case this rule cannot detect.
```

**Nothing else moves.** No fourth lens, no opponents lane or strength-of-schedule figure, no hue change, no in-app playback, no Europa or Conference screens, no Results tab, no domestic matchday markers, no change to staleness thresholds, the diff engine, URL policy or the LIVE window. The never-list amendment text stands. The `_ds/` mirror of Fergie Time in this project is not edited; the repo's `src/index.css` and `src/lib/competitions.ts` remain the token source of truth and the canvas mirrors them in its own head block.

## What the revision carries as evidence

Boards at 390 (the judge) for every state this prompt adds, at 360 (the jury) for the tab row and the picker, at 375 where the tab row changes, and at about 1000 by 700 for the table's desktop sticky. Each new state present at least once: the "local time not known" pill, RESCHEDULED beside COMPUTED and absent, the mid-matchday callout with its new copy, the desktop header and divider stuck with the release annotated. The contrast figures on 1f computed at render time the way the hue figures already are, or marked typed where they are typed; never a typed figure presented as computed, which is how 1.14 happened. A "Revision notes" block on 1h listing each artboard changed, the ruling or correction it answers, and the two designer decisions with their consequences. A sentence on 1e that no class or token on it changed. Rendering the canvas at a width is the canvas's own responsibility; the app-side checks (`scrollWidth === innerWidth` on every capture, the box diff proving the five domestic tables unchanged, the six MD1 stadium clocks) are the CTO's at re-read and the build's at acceptance, and the revision does not claim them.

## Handoff and the three sign-offs

Save the revision as the same file at the same path; Beni saves it, and the CTO computes its SHA-256 and byte count on re-read and cites them in the repo's docs, which is the revision's identity from then on. Three sign-offs are recorded separately against that one hash, none inferred from silence or from PR #34's green docs check. Yours first: a dated block on 1h, "Designer's fidelity and interaction sign-off", stating that this revision answers rulings 1–10 and the corrections above, names the two decisions you made and the contrast option you chose, and states that nothing else moved and no token was repainted, or names exactly what could not be drawn as ruled and why. Then the CTO's feasibility re-read of Q1–Q9 against the corrected revision. Then Beni's design approval, which means the policies and behaviour are unambiguous enough to specify and is not a claim that a build is correct; the release's own cold review and CI decide that later. Approval of PR #34's documentation is not authorisation to ship any of it.

## Must not

Do not reopen any of the ten rulings or the canvas's five. Do not repaint a Fergie Time token; `--media` is the one addition and stays flagged. Do not design Europa or Conference screens. Do not choose or print a release number. Do not claim any network check, curl or fetch; the provider facts above are the Round 2 and Round 3 receipts and are cited as such. Do not write to the repo. Do not invent data: rows come from the repo's real snapshot labelled with the SHA they were read at, and anything invented is captioned ILLUSTRATIVE. Do not resolve anything this prompt leaves to the build (the picker sort mechanism, the tests, the report-line format, HONESTY rule 5's wording); name it as the build's and move on. Where a ruling cannot be drawn as stated, say so in the revision notes rather than improvising a different design.

## Deliver back

The revised canvas saved in place, and in chat: the revision notes (artboard, change, ruling), the contrast option chosen with its consequence, decisions (d) and (e) with one sentence of reasoning each, the phase-ended copy decision under F10, and anything you could not do as ruled.
