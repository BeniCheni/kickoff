# Executive Hotfix Fast-Track Review Brief — PR #33 → v0.3.2 · Wed 9 Sep 2026, 00:09 EDT (TZ=America/New_York) · supersedes the 8 Sep 23:58 brief

Written by the Pass 1 seat (Claude Code, the cold review) after Beni's two rulings, for the CEO, the Chief TPM and Product. Every number below comes from a command run in this session at the head it names; nothing is carried from a document or a PR body. Archived as `docs/v0.3.2-fast-track-brief.md` in this commit, posted as one PR comment, and delivered in chat for the PM seat to align on and hand to Codex — the last section is the release prompt, complete, written to be pasted whole.

**Decisions, both Beni's, both first-hand in the Pass 1 session:** (1) **v0.3.2, a patch** — 8 Sep 2026, 23:55 EDT. (2) **Fix now** — 9 Sep 2026, 00:06 EDT: the three stale skill handles come in with the hotfix; there is no ideas row 30. Fast-track shape, also Beni's: no separate Pass 1.5 / 2 / 2.5 seats. Codex answers Pass 1's five questions in its release comment, cuts the release commit, squash-merges and tags on his word. `--no-merge` holds for this seat: nothing is merged or tagged here.

## What ships

- The scheduled sync's own verify step survives a live match. `tests/dom/designCycle.test.tsx` no longer asks the whole document for LIVE: every assertion is scoped to the mutated fixture's own row, resolved by its accessible name, and a new case renders two in-play rows on one kickoff and proves the document-wide query still throws while the scoped one picks each row.
- That case's input is fixed by construction — the snapshot's first Brooklyn date carrying two league-set kickoffs, both states overridden in memory — so it cannot go red on what the sync last wrote. At `cfbdccc` it would have: on the snapshot a Sunday 13 Sep 19:23Z run writes, and with any postponed row in the file.
- Nothing in the deployed app changes. The header moves from V0.3.1 to V0.3.2 with no visible difference, exactly as v0.2.4 did. The app still refuses to claim what the snapshot does not know.
- The method moves with it: `/kickoff-pr-review` §4 and `browser-matrix.md` no longer claim the snapshot holds zero in-play fixtures; §4 now says a test that mutates a real-snapshot row must scope every query to that row and must not pick its rows by `status` or `result`.
- The pipeline skill is named by its live handle, `/anthropic-skills:football-soccer-deity`, in `/kickoff-pr-review`'s Pass 0 row (was `…football-soccer-god`) and in `CLAUDE.md` lines 5 and 156 (was `/beni-betting-pipeline`; line 5 keeps the old name in parentheses so the archived `docs/` prompts that cite it still read). Archived `docs/` files are untouched.

## Where it sits

- v0.3.2 follows v0.3.1 (8 Sep 2026, PR #30). No numbering fork: `CLAUDE.md` reserves v0.4.0 for the sync theme, the UCL design cycle's number is separately deferred, and v0.3.2 collides with neither. Precedent is direct — v0.2.3 was a hotfix released and tagged with no `docs/` rung; v0.2.4 shipped test infrastructure as a release.
- Why now: the committed snapshot carried genuine `in_play` rows in 3 of the last 16 commits on `main` (30 Aug, 31 Aug, 1 Sep); Saturday 12 Sep carries 25 fixtures in the window; run 34157750491 (7 Sep, 20:00Z) already failed this way and left `main`'s snapshot frozen at 00:02Z through all of Monday's matches.
- What stays on the list (`docs/v0.2.6-ideas.md`): rows 25 and 26 (the designer's — the Fixtures lens's third state and the NEXT lane), 27 (a formatter), 29 (auto-merged sync PRs do not reach Pages). Row 30 is not created.

## What the review found

Reviewed at `cfbdccc`; six Claude commits on top — `f2900c8` test, `68c2377` SKILL §4, `1c2a5b8` CHANGELOG, `66d65ca` process notes, `b75d400` this brief's first archive, `253a324` handles + the `[Unreleased]` Changed block; `verify` pass at `253a324` and at this brief's own commit (see the PR checks); MERGEABLE; `origin/main` unmoved at `0c37716` — the 00:21Z sync on 9 Sep ran quiet.

| # | Sev | Finding | Disposition | Evidence |
|---|---|---|---|---|
| 1 | high | The new two-row case picked its rows by `status`/`result`; on a Sunday-evening snapshot the second pick is Monday's, in the next week, and `fixtureRowFor` throws "Unable to find" — the same for any postponed row earlier in the file. `sync.yml`'s verify would go red on the first Sunday evening this PR exists to protect. | Fixed, `f2900c8` | Red at `cfbdccc` under both recipes (1 failed / 9 passed); green under all five inputs after |
| 2 | medium | The case never asserted that a second LIVE pill existed, so it could not fail its claim. | Fixed, `f2900c8` | Control: the old document-wide query put back fails the case with "Found multiple elements: LIVE" |
| 3 | low | The row resolver matched by substring — `'12:30 PM'.includes('2:30 PM')` is true. | Fixed, `f2900c8` | Probe: 19 buttons, 1 match; now anchored on `startsWith(time)` and the row's own "home vs away" |
| 4 | low | A dead `'—'` branch; the parameter re-declared `Fixture`. | Fixed, `f2900c8` | A placeholder row's name starts with `—`; no call site passes one |
| 5 | low | The `aria-expanded` tail tested row expansion, not scoping. | Fixed, `f2900c8` | Read |
| 6 | low | SKILL.md §4's continuation lines flush-left (row 27 recurring); "prefer" where the sibling file says "must"; the durable-coverage clause dropped. | Fixed, `68c2377` | Diff |
| 7 | low | The CHANGELOG entry named the symptom and hedged "could fail" where a run id exists. | Fixed, `1c2a5b8` | Compared with `[0.3.1]` |
| 8 | low | Ideas row 28's strike cites a branch name that dies at the squash. | **Codex, in the release commit** | — |
| 9 | low | Three stale skill handles: SKILL.md line 38, `CLAUDE.md` lines 5 and 156. Pre-existing, outside the diff; Beni ruled `fix now`. | Fixed, `253a324` | `git grep` — no live occurrence left; archived `docs/` untouched by design |

Killed on evidence: the month-view day cell (its aria-label names a date and competitions, never a club, so the predicate cannot match it); the restore guard (fires on nested mutations, restores the PR's own shape clean). Codex's PR-body reproduction ("6 failed / 3 passed") was re-run here and holds.

## Verification as re-run

- Red on `main`: 6 failed / 3 passed, "Found multiple elements" — on KICKED OFF with the prompt's one-row mutation; run 34157750491 hit LIVE one line earlier, the same defect.
- Green at `cfbdccc` with one and with two file-level live rows: 10 passed. Red at `cfbdccc` on the Sunday shape and on the postponed shape: 1 failed / 9 passed. Green after `f2900c8` on all five inputs: 10 passed. The snapshot was restored and `git diff --stat -- src/data/fixtures.json` empty after every run; the two recipes are in the Pass 1 comment.
- Head `1c2a5b8` (the last commit that touches a test): `npm run typecheck` clean; `npm test` 344 tests in 27 files; `npm run build` clean. Every commit since touches only `docs/`, `CLAUDE.md`, `CHANGELOG.md` and one table cell of `SKILL.md`; `verify` runs the same three commands on each push.
- Smoke pass: the built `dist/` served by `vite preview` from this worktree (port 5174 belonged to another worktree's dev server, by `lsof`); 390×844 and 1000×800 with the viewport set before each read; header `V0.3.1 · 1007 FIXTURES · SYNCED 2026-09-08 17:17 UTC`; `scrollWidth === innerWidth` true at both.
- Not re-run: the real Sunday sync — the shaped snapshot stands in, and the Sun 13 Sep 19:23Z slot's run is the first live proof; `npm run sync -- --check` (no script changed); the browser matrix (no `src/` or `index.css` change); `npm run build:single`.

## Risks and accepted costs

- **No user-visible change under a version bump.** Accepted: v0.2.4's precedent, and the CHANGELOG's subject line says so.
- **A test that reads the committed snapshot still reads it.** Its input is now fixed by date, not by state; the six loop tests anchor on their own fixture's date and held under every shape tried. A `FIXTURES` mock in this file would take the real-snapshot tests with it and is not this hotfix's job.
- **The first live proof is Sunday.** The shaped snapshot reproduced the fork and its fix; the real 19:23Z run on 13 Sep is the receipt. Read it (`gh run list --workflow=sync.yml`) before trusting the app for any position on Monday — the sync-run reader in `../Sportsbooks/CLAUDE.md`.
- **Who merges and tags.** The repo's rule (`/kickoff-pr-review` §7 step 5) is that Beni clicks release merges and tags; this fast-track has Codex do both on his word — Beni's ruling of 8 Sep. The squash body says who merged.
- **A hotfix that is no longer single-purpose.** `fix now` adds two `CLAUDE.md` paragraphs to a test-and-method diff; accepted by ruling, and named in the `[0.3.2]` Changed block so a cold reader sees it.
- **Pages after the merge.** A merge made with a user token deploys on push; a bot-token merge does not (row 29). Confirm a `pages.yml` run followed, else `gh workflow run pages.yml`.
- **No third seat before the merge.** Pass 2.5's corroboration is folded into the release checklist: `verify` green at the release commit, the seven version places checked by grep before the squash, and the three green commands on `main` after the tag.

## Handed to the next patch

- Row 29 stays open: a workflow change, its own PR.
- The first Sunday run after the merge: read it, and add to the ideas file's process notes whether the real snapshot matched the shaped one.
- `../Sportsbooks/CLAUDE.md` and the pipeline skill's own text may still name `/beni-betting-pipeline`; that repo is outside this PR and this review did not read it.

## Rulings

- **v0.3.2 — ruled by Beni, 8 Sep 2026, 23:55 EDT, first-hand.**
- **Fast-track — Codex answers, cuts, merges, tags — ruled by Beni, 8 Sep 2026.**
- **Fix now — the three handles in this hotfix, no row 30 — ruled by Beni, 9 Sep 2026, 00:06 EDT, first-hand; done in `253a324`.**

Nothing is left open for the CEO.

## Handoff — the release prompt for Codex (complete; paste from the next line to the end)

You are Codex, the builder of PR #33 (`codex/row28-designcycle-live-scope`) in `BeniCheni/kickoff`, and you are executing the v0.3.2 hotfix release. Objective, in one line: answer the cold review, cut the release commit, prove it, squash-merge, tag `v0.3.2`, confirm the deploy, and report — today, before Saturday 12 September's 25-fixture window. This prompt carries every context and every objective; the repo carries the truth. Where they differ, the repo wins.

Context you must hold. Row 28 of `docs/v0.2.6-ideas.md`: `tests/dom/designCycle.test.tsx` mutated one snapshot fixture to `in_play` and asked the whole document for LIVE; when the committed snapshot already held a live match, two pills rendered and the query threw, and the 7 Sep 20:00Z scheduled sync (run 34157750491) went red at its verify step — no commit, no PR, `main`'s snapshot frozen through Monday's matches. Your `cfbdccc` scoped the queries with `within` and added a two-row case. The cold review (Claude Code, Pass 1, comment issuecomment-5595461999 on the PR) found that case picked its two rows by `status`/`result`, so on the snapshot a Sunday 13 Sep 19:23Z run writes — the last Sunday kickoff in play, every other Sunday row resolved — the second pick was Monday's fixture in the next week and the resolver threw "Unable to find"; the same with any postponed row in the file. The review fixed it on your branch (`f2900c8`: the input is `BY_DATE`'s first day with two exact kickoffs, states overridden; the document-wide query is asserted to throw), re-indented and tightened `/kickoff-pr-review` §4 (`68c2377`), rewrote the CHANGELOG entry (`1c2a5b8`), added process notes (`66d65ca`), archived the executive brief (`b75d400`), and — on Beni's `fix now` ruling — renamed the pipeline skill's handle in SKILL.md line 38 and `CLAUDE.md` lines 5 and 156 and wrote the `[Unreleased]` Changed block (`253a324`). Beni's rulings, both first-hand: v0.3.2, a patch; fast-track — you answer, cut, merge and tag. `verify` was green at every one of those commits; read the head yourself.

Rules that bind you. Read `CLAUDE.md` (its "Release management" section is the law on the version string), `AGENTS.md`, the Pass 1 comment and `docs/v0.3.2-fast-track-brief.md` before touching anything. `git fetch origin` and take the head SHA from the branch, never from this prompt. Never run `npm run sync` in any form but `-- --check`. Do not touch `src/data/*.json`. Do not force-push. Do not weaken any assertion in `tests/dom/designCycle.test.tsx`; a fixed finding is not re-litigated without evidence. Commit as `Codex <noreply@openai.com>`; wrap prose in `docs/`, `CHANGELOG.md`, `README.md` and `CLAUDE.md` at the files' existing width; never hand-wrap a table row. Report outcomes faithfully — a red step is reported red, not retried into silence.

Step 1 — answer Pass 1 in one PR comment, in its table shape (finding · accept / contest / accept-but-contest-the-characterisation · evidence). The five questions, verbatim from the Pass 1 comment's §4: (1) Finding 1 — accept `f2900c8`'s input in place of the `status`/`result` pick? Yes, or a run of the case at `cfbdccc` against the Sunday recipe (in that comment) that passes. (2) Finding 3 — accept the anchored predicate (`startsWith(time)` + `"home vs away"`)? Yes, or a rendered pair of rows where it matches two or zero. (3) Finding 7 — is `1c2a5b8`'s entry the text the `[0.3.2]` section folds in? Yes, or a counter-draft. (4) Finding 8 — will the release commit replace the branch-name pointer in row 28 with "Shipped in v0.3.2 (PR #33, <date>)"? Yes or no. (5) Is `68c2377`'s rule — never pick rows by `status` or `result` — accepted as method? Yes or no. Finding 9 (the handles) is closed by Beni's ruling and needs no answer.

Step 2 — the release commit on the PR branch: one commit, subject `v0.3.2 — the sync's verify survives a live match`. Set the date first — `DATE_ISO=$(TZ=America/New_York date +%F)` and `DATE_TXT=$(TZ=America/New_York date '+%-d %b %Y')` — and use those values everywhere below. The version string moves in seven places, all in this commit, all named as they sit at the head: `package.json` line 4 and `package-lock.json` lines 3 and 9 via `npm version 0.3.2 --no-git-tag-version` — a hand edit does not move the lock; `CHANGELOG.md` — a new `## [0.3.2] — $DATE_ISO` section into which the whole `[Unreleased]` block (its Changed and Fixed entries, verbatim) moves, with an empty `## [Unreleased]` left above it; `README.md` line 4's badge `version-0.3.1-1d4ed8` → `version-0.3.2-1d4ed8`; line 76's `## ⏱️ What it does (v0.3.1)` → `(v0.3.2)`; the Lineage list under line 196 — a new entry above the v0.3.1 one at line 221; and the annotated tag, which comes after the merge (step 5). One more edit in the same commit: `docs/v0.2.6-ideas.md` row 28 — replace "Implemented in this PR (`codex/row28-designcycle-live-scope`)" with "Shipped in v0.3.2 (PR #33, $DATE_TXT)". Do not add a row 30.

The `[0.3.2]` section opens with one sentence naming the subject, then the moved Changed block, the moved Fixed entry, then Deliberately not done. Subject line: "The sync's verify step survives a live match. Paper trail: `docs/v0.3.2-fast-track-brief.md`; Beni's rulings, 8–9 Sep 2026: v0.3.2, fast-track, fix the handles now." Deliberately not done, three bullets: "**A `FIXTURES` mock for the designCycle file.** The scoped queries and a date-fixed input close row 28 without one; a mock in that file would take the six real-snapshot tests and the ticker tests with it." — "**The browser matrix.** No `src/` or `index.css` change; the smoke pass at 390 and 1000 px is what the method owes a test-and-docs diff." — "**Auto-merged sync PRs still do not reach Pages.** Row 29; a workflow change, its own PR."

The README Lineage entry, above the v0.3.1 one: "- **v0.3.2** *($DATE_TXT)* — the sync's verify survives a live match: the designCycle DOM test scopes every LIVE / KICKED OFF query to its own row and fixes its input by date, so a genuine in-play row in the snapshot no longer turns `sync.yml`'s verify red; the pipeline skill is named by its live handle; nothing in the app changes."

Step 3 — prove the commit before pushing it:

```bash
npm run typecheck && npm test && npm run build
grep -rn '0\.3\.1' package.json package-lock.json README.md   # must print nothing; CHANGELOG.md keeps its [0.3.1] history
grep -rn "$DATE_TXT" CHANGELOG.md README.md docs/v0.2.6-ideas.md   # the release date, in every place it lives
grep -n '^## \[Unreleased\]' -A2 CHANGELOG.md                   # the heading stays, empty
git push origin codex/row28-designcycle-live-scope
gh pr checks 33 --watch
```

Step 4 — squash-merge PR #33 with this subject and body, both trailers included:

```bash
gh pr merge 33 --squash --subject "v0.3.2 — the sync's verify survives a live match (#33)" --body "Beni's rulings, 8–9 Sep 2026: v0.3.2, fast-track, fix the handles now. Pass 1 cold review by Claude Code (f2900c8, 68c2377, 1c2a5b8, 66d65ca, b75d400, 253a324); build and release by Codex on Beni's word. Paper trail: docs/v0.3.2-fast-track-brief.md.

Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Claude <noreply@anthropic.com>"
```

Step 5 — tag the squash commit and prove `main`:

```bash
git checkout main && git pull
git tag -a v0.3.2 -m "v0.3.2 — the sync's verify survives a live match"
git push origin v0.3.2
npm run typecheck && npm test && npm run build
```

Step 6 — Pages: a merge made with a user token deploys on push; confirm a `pages.yml` run followed (`gh run list --workflow=pages.yml --limit 1`), else `gh workflow run pages.yml`. Then confirm the live demo's header reads V0.3.2.

Step 7 — one closing PR comment: the squash SHA, the tag, the three commands' real numbers on `main`, whether Pages ran, and whether the merge slipped past Brooklyn midnight — if it did, every release date moved before the tag (`grep -rn "$DATE_TXT"` finds them all).

Definition of done: PR #33 merged by squash with both trailers; `v0.3.2` tagged on that commit and pushed; `package.json`, the lock, the CHANGELOG, the README badge, heading and Lineage all say 0.3.2 on `main`; typecheck, tests and build green on `main`; the demo's header reads V0.3.2; the closing comment posted. The first live receipt for the fix itself is the Sunday 13 Sep 19:23Z sync run — whoever reads Step 0 on Monday reads that run before trusting the app.
