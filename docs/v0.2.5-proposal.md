# v0.2.5 — the resilience patch (proposal and spec of record)

Written 5 Sep 2026, Brooklyn time, by Codex from `main` at
`936475ddf7f56bc2ce1e32487686f54d47ceb242`. Beni approved the audit checkpoint and handed
implementation authority to Codex in this session. Scope, sequence and the doctrine below
are settled; implementation choices and review resolutions are recorded here as they land.

## The approved boundary and its cost

**A fixture or standings fetch/validation failure aborts the sync with exit 2 before any
snapshot writes.** Fixtures and standings form v0.2.5's authoritative snapshot boundary;
metadata describes that same successful fetch. Fetch and validate both before publication.
If either fails, publish neither. Committed data remains the last completely successful
authoritative snapshot rather than a partially advanced state.

A standings outage can intentionally delay otherwise valid fixture updates. This is an
accepted availability cost of atomic publication of the fixtures + standings authoritative
snapshot, not an accidental side effect. Atomicity applies to this explicitly defined
boundary: future injuries, odds, weather, broadcast or other ancillary datasets do not
inherit membership automatically. Their semantics and failure consequences need a separate
decision. This supersedes the standings soft-failure exception introduced before v0.2.5.

The machine-readable report line keeps its existing shape. A refused fetch has exit 2 and
diagnostics, not a successful report; the workflow already treats that as a hard failure.
The `standings=failed` report variant remains accepted for compatibility with earlier runs.

## Audit and scope resolutions

All row numbers here name **`docs/v0.3.0-ideas.md`**; original descriptions also live in
`docs/v0.2.0-ideas.md`. The inherited spec is `docs/v0.2.4-proposal.md` and its review
resolutions. v0.2.5 stays one release, with the sync half first so its asynchronous dispatch
proof can run while the app half is built.

- **Row 8:** reject un-normalizable events and standings entries, with actionable context.
  Validate event identity before coercion and deduplication. An audit probe reproduced
  schema-valid `ligue1:undefined`; two id-less events per competition collapsed into one
  without a warning. A standings response with the expected valid row count plus a malformed
  extra entry was accepted too. Counts alone cannot account for rejected new entries.
- **Row 13d:** relocate `inWindow` under `scripts/`, derive its date with `brooklynDate`,
  test both inclusive edges under EDT and EST. The helper imports `src/lib/time.ts`; it does
  not live there. The app half's formatter changes may then edit `time.ts` without moving
  sync orchestration into the app layer.
- **Row 7:** stabilize codecs, retain fresh date defaults in popstate handling, preserve
  hashes, normalize owned URL keys on load. Unknown parameters survive. Existing total
  decoder semantics stay: uppercase `BROADCAST` is invalid and falls back to Poster.
  **`?date=<today>` unpins on load**, already decided. Two inherited rollover tests need a
  nondefault initial pin; keep their pin assertions and add coverage of today's unpinning.
  Period navigation's push/replace policy is not this release's work (row 9).
- **Row 6:** a class boundary keyed by `tab:lens` at the App seam, covering ticker, banner
  and page while keeping the header and navigation. Reload retries the URL; Reset view
  starts at the current pathname. Module-import snapshot validation happens before React
  mounts and cannot be recovered by this boundary. Event-handler and asynchronous errors
  are not promised recovery by a render boundary either.
- **Row 12:** generate a classic inline head bootstrap from the production lens/theme
  decision, test the generated output, and follow OS theme changes when there is no explicit
  choice. Broadcast keeps its separate memory and dark default. Extend the existing DOM rig.
- **Rows 13a and 13c:** move `posterDayTitle` into `time.ts`; expose structured clock and
  meridiem parts instead of parsing the formatter's currently guaranteed space.
- **Row 13b is already shipped:** `slateSubLine` and its mixed/all-TBC tests are present
  since v0.2.2. Only the remaining Poster day-header counterpart, **row 19**, is implemented:
  extract the sub-line to `lensSelectors.ts`, name its TBC count, preserve time exclusions.
- **Row 11, one third:** qualify NEXT's time when its Brooklyn date is not today. Marquee
  speed, reduced-motion controls, palette changes and stale-LIVE design remain outside scope.

## Follow-up candidates, not implementation scope

Read-only audit probes against modified test payloads established two additional paths:
missing `timeValid` produces `exact`, and null full-time scores become schema-valid 0–0
through `Number(null)`. These are candidates for `docs/v0.2.6-ideas.md`, explicitly deferred
by Beni. The probes establish mapper behavior, not their occurrence in current ESPN data.

## Build and verification plan

Branch: `codex/kickoff-v0.2.5`, from the verified SHA above. Commits are authored
`Codex <noreply@openai.com>` by per-command configuration, overriding the inherited Claude
authorship convention by Beni's explicit instruction. Beni alone merges and tags.

Sequence: this proposal; row 8; row 13d and a branch-specific `sync.yml` dry-run dispatch;
URL state; boundary; theme; formatter/label fixes; release record and next ideas file.
Typecheck and the full suite must pass before each commit; no data-honesty assertion is
weakened. No dependency addition is planned. No generated snapshot is hand-edited or refreshed.

The sync proof includes rejected identities/entries, both fetch failures, pre-write
validation, the unchanged report contract and a real branch dispatch. The app proof extends
`tests/dom/rig.ts` for matchMedia changes and retains the environment-routing guard.
Production theme verification uses a built preview, including a delayed application module
so the bootstrap can be examined before React. Both normal and single-file builds are checked.

The browser matrix is 3 lenses × 2 themes × 2 tabs × 360/375/390/1000px = 48 captures.
Set viewport before load/capture; assert `scrollWidth === innerWidth` before every capture.
Record state, console errors and screenshots, inspect every capture, and additionally
exercise Monthly, URL rollover, OS-theme changes and deliberate throws in every lens/theme.
Use an isolated Chrome profile, CDP and Node's native WebSocket with scratch tooling outside
the repository. No Playwright or checked-in matrix runner. Browser-only synthetic failures
must not require editing `src/data` or adding a production crash switch.

## Evidence and accepted implementation details

**Label implementation (13a, 13c, 19, one third of 11):** `ZonedParts` exposes `clock` and
`meridiem` from `Intl.formatToParts`; both heroes consume them directly. Existing combined
time labels retain their output. `posterDayTitle` now lives in `time.ts` and formats its
month directly. The extracted `posterSubLine` names non-exact times with `N TBC` and retains
the existing bounds' postponed/cancelled exclusions. NEXT uses `Sun 6 Sep · 8:30 PM` for a
later Brooklyn date, including a later week; same-day output is unchanged. No marquee speed
or motion-control change. CONFIRMED: typecheck and 251 tests in 23 files passed, including
the Poster header's production component wiring and the original hero TBC assertions.

**Row 12 implementation:** Vite's existing build API compiles `src/themeBoot.ts` as a small
IIFE. A `transformIndexHtml` plugin injects it as a classic script at the start of head in
development, the normal build and the single-file build. The nested build explicitly disables
config loading; it adds no dependency and no second decision string. Node tests execute the
compiled code in fresh VM globals across 160 lens/storage/OS combinations and six hostile
storage cases, comparing with `resolveTheme`.

App subscribes to the OS scheme and removes its listener on cleanup. Stored choices win;
Broadcast stays dark by default. Explicit toggles are also remembered by lens family for the
current session, so blocked storage does not let an OS event undo the user's click. The rig
now exposes `setDark`, `setReducedMotion` and listener counts over stable MediaQueryLists.
CONFIRMED: 240 tests in 22 files, typecheck and both builds passed. The build's existing large
snapshot-bundle warning is retained; no code-splitting scope was added. Production browser
proof is recorded in the final verification table below.

**Row 6 implementation:** `ViewBoundary` surrounds ticker, staleness banner and selected
page in App; its `tab:lens` key clears a failed view when either selection changes. It uses
existing surface/ink tokens and two recovery actions, without a new error pill or design
state. React's caught-error reporting is retained. Tests now use the production boundary,
retain the uncontained-throw control, and throw under App in all six lens/theme combinations,
with additional ticker/banner coverage and recovery through both navigation controls.
CONFIRMED: typecheck and 235 tests in 21 files passed. Browser recovery is recorded below.

**Sync dispatch, CONFIRMED:** run
[`33988352447`](https://github.com/BeniCheni/kickoff/actions/runs/33988352447), head
`562f543435ef45846670e07914ddd22d710bcaef`, completed successfully. Its log reached
`--check: no files written.` and the explicit dry-run stop; the workflow accepted:

```
report: changed=true changes=18 urgent=11 standings=changed rank-moves=66 merge=hold
```

These are this run's observations against its checked-out snapshot, not a live data refresh
or a promise of the next run's counts. No sync PR was created or inspected by this build.
The sync half passed typecheck, standalone node tsc and 216 tests in 20 files before push.

**Row 7 implementation:** `urlCodecs.ts` owns the tab/view/league/filter codecs alongside
`canonicalSearch`. Date callbacks are memoized by `today`; the popstate subscription is
keyed only by its URL key and delegates to `useEffectEvent` for fresh render values. App
normalizes all owned keys once per load, including inactive-page keys, preserving unknown
keys, history state and the hash. This deliberately re-encodes commas as `%2C` and collapses
duplicate owned keys to the first value the decoder read. It does not lowercase invalid lens
values or change history policy. A date that becomes today after arrival stays pinned.

The two original pin-regression assertions are retained: their setup now establishes the
same pin on the preceding day and wakes to its date before the original midnight steps.
A separate test proves that a date equal to today on arrival is omitted and follows midnight.
Other DOM tests cover stable setter/subscription identity, latest defaults/decoders, the
deployment pathname and hash, StrictMode cleanup and Step 0 after rollover. CONFIRMED after
row 7: typecheck and 227 tests in 20 files passed.

**Row 13d implementation:** `scripts/window.ts` owns `inWindow(fixture, from, to)`;
`sync.ts` uses it for both the previous snapshot's diff window and the shrink guard's
baseline. Ten edge cases cover EDT and EST, plus one window crossing the US fall transition.
The report formatter and pinned regex are unchanged. Branch dispatch evidence is recorded above.

**Row 8 implementation:** `runSync()` is the CLI's tested exit-code boundary. Both providers
reject un-normalizable entries, with competition, available identity, entry position and
reason; standings awaits all five results to retain all league diagnostics. Empty schedule
arrays remain valid, but a missing/non-array `events` field is a malformed fetch response.
Provider identity is checked before coercion/deduplication, with a schema backstop against
empty and coerced missing IDs. Missing time-confidence flags and score coercion are unchanged.

Both datasets and metadata are validated, then all three payloads serialized, before the
first write. Publication is the workflow's single Git commit of those files. This is not a
multi-file filesystem transaction: a disk/process failure during local writes can leave a
partial working tree, but a failed run is not committed. The fetch/validation failure
invariant is stronger: no snapshot write has begun. No new storage format or dependency.

Integration tests intercept filesystem writes while invoking the real orchestration: either
provider failing, either schema rejecting, waiting for standings, successful complete writes,
and `--check`. A subprocess also pins the actual CLI's exit 2 on an offline provider.

**CONFIRMED baseline:** Node 24.15.0; `npm run typecheck` passed; `npm test` passed 176 tests
in 17 files (node 153/11, dom 23/6); `tsc -p tsconfig.node.json` passed independently.
Initial runs lacked the locked jsdom/Testing Library installation; `npm ci` restored it
without changing either manifest. Local/remote main agreed; v0.2.4's missing tag was the
acknowledged owner release act, not a numbering change.

## Final local verification — CONFIRMED

Run on 5 Sep 2026 with Node 24.15.0. The full source matrix and behavioral probes used
implementation commit `657f758`; the final version smoke used the same code with the six
release-record sites advanced to v0.2.5. No snapshot changed. The remote main remained at
the branch point when checked after this verification.

| Check actually run | Result |
|---|---|
| `npm run typecheck` on v0.2.5 | Passed. |
| `npm test` on v0.2.5 | **251 tests in 23 files passed**, 2.20 s reported by vitest; both projects included. |
| `tsc -p tsconfig.node.json` independently | Passed. |
| `npm run build` | Passed; normal build retains the existing >500 kB bundle warning. |
| `npm run build:single` | Passed; single HTML also opened directly from the filesystem in Chrome. |
| Ledger / Poster / Broadcast × light / dark × Fixtures / Table × 360 / 375 / 390 / 1000 | **48/48 captures**, all inspected; every capture preceded by the viewport setting and `scrollWidth === innerWidth` assertion. No console errors or uncaught exceptions in this matrix. |
| Monthly in every Fixtures matrix cell | **24/24** URL, rendered-month and page-width checks. |
| Deliberate throw below App, every lens/theme | **6/6** cases; **12** fallback captures at 390 and 360. Tab and lens remount recovery, actual Reload navigation and Reset view clearing the URL all passed. |
| Fallback computed text colors | 24 foreground/background pairs; minimum contrast **6.54:1**. Existing tokens only. |
| Built theme before React | **15/15** cases: each lens with OS light/dark, stored light/dark against the opposite OS, and blocked storage. Application module held at the network layer; root empty, correct theme/lens, computed background unchanged after release. |
| OS changes during the session | **15/15** cases retain the resolved policy; all three blocked-storage explicit-toggle cases retained the user's choice. |
| URL/clock browser probes | Today's date unpins and follows Sunday→Monday; a nondefault pin survives three midnights; Back uses fresh defaults and restores a pin; empty query preserves its hash; unknown parameters survive normalization. Exact served clock imported with positive subscriber count. |
| Changed labels below the first viewport | Poster: `23 MATCHES · 4 LEAGUES · 9 TBC · FIRST KICKOFF 7:30 AM`, inspected in both themes. NEXT: `Fri 9 Oct · 2:45 PM`, inspected before that Brooklyn day. |
| Final v0.2.5 smoke | **8/8** captures: normal and single-file builds, Poster light / Broadcast dark, 390 / 1000. Header version and page width asserted. Both builds contain the same **648-byte** classic bootstrap before styles and the application module. |

Browser conditions are reproducible: isolated headless Chrome profile, own CDP pages, native
Node WebSocket driver; built preview at port 4175 and this checkout's dev server at 5175.
The matrix froze the clock at `2026-10-10T12:00:00Z` to exercise a committed mixed exact/TBC
slate. Rollover probes advanced a Date shim and dispatched focus. Boundary faults were injected
only into served component responses; no data edits or production fault switch. These dates
are controlled test inputs, not live fixture verification. Existing palette debts and stale
LIVE design remain deferred.

Screenshots, case JSON and scratch drivers are retained in the local handoff artifact
`kickoff-v0.2.5-qa` outside the repository. They are **local evidence, not GitHub attachments**;
the conditions above and the checked-in DOM/pure tests are the cold review's reproduction
instructions. Scratch probes initially assumed a `<table>` element and the wrong competition
abbreviation; the actual markup and keys corrected those probe failures. No app change was
needed for the browser matrix.

The release record uses Brooklyn's 5 Sep 2026 date, read from `TZ=America/New_York date`.
`npm version 0.2.5 --no-git-tag-version` moved both manifests; CHANGELOG, README badge,
heading and Lineage moved with them. The missing v0.2.4 tag and the new release tag remain
Beni's acts. `docs/v0.2.6-ideas.md` is the next candidate list, not an assigned release scope.

## Final GitHub verification — CONFIRMED

Both dispatches checked out `67e88f64ff4eb82fb0ccdaf18a2131bec22376d8`, the complete release
implementation and record before this receipt-only addition:

- [CI run 33989898501](https://github.com/BeniCheni/kickoff/actions/runs/33989898501)
  passed typecheck, **251 tests in 23 files**, and the production build on Node 24.
- [Sync dry run 33989899563](https://github.com/BeniCheni/kickoff/actions/runs/33989899563)
  succeeded, reached `--check: no files written.`, and passed the workflow's report regex:

  ```
  report: changed=true changes=22 urgent=9 standings=changed rank-moves=66 merge=hold
  ```

  The final step explicitly reported `Nothing written, no commit, no PR. Verdict: merge=hold.`
  The counts differ from the earlier sync-half run because the provider was fetched again;
  neither run refreshed committed data. This second run covers the app half's edits to the
  time module imported by `scripts/window.ts`.

All cited implementation SHAs are reachable on the pushed branch. The PR's own required
`verify` run is checked after opening it; a manual dispatch does not substitute for that run.

## Review resolutions

Recorded by the cold review of PR #23 (`/kickoff-pr-review 23 --no-merge`, Claude Code,
5 Sep 2026), which re-derived this file's claims from the diff at
`9307803fea440193001f261fd480f708d2a37dc4`. The PR comment carries the findings table; the
Codex rebuttal and Beni's adjudication may append below.

- **The bootstrap sits before `<meta charset>`.** `head-prepend` puts the 648-byte classic
  script ahead of the charset declaration, which HTML's encoding prescan must find within the
  first 1024 bytes of the document; both builds end the declaration at byte 763. Accepted as
  built — that order is what "before stylesheets" costs — and pinned: `tests/themeBootstrap.test.ts`
  fails if `index.html`'s prelude plus the injected tag would pass 1024 bytes. If the budget
  ever binds, inject after the charset meta instead of prepending the head.
- **A failed run has no reader.** Under the atomic boundary a standings outage no longer opens
  a held PR carrying `standings=failed`; it is exit 2, a red `sync.yml` run, and nothing in
  the PR list. Accepted as the boundary's cost and named in `CLAUDE.md` ("Scheduled sync") and
  `docs/HONESTY.md` §5, so the betting track's Step 0 knows the app can age with no PR to
  read. Surfacing exit-2 runs where a human reads is `docs/v0.2.6-ideas.md` row 15.
- **Team identities still coerce.** `normalizeEvent` keeps `String(team.id)` for
  `home.sourceId` / `away.sourceId`, which `src/lib/standings.ts` joins against table rows
  whose `teamId` is now validated; an object or a coerced value can only miss that join
  silently. Outside row 8's stated scope (event identity and standings entries);
  `docs/v0.2.6-ideas.md` row 16.
- **Verification re-run, and what was not.** 48/48 matrix cells at 360/375/390/1000 with
  `scrollWidth === innerWidth` on every one; Monthly in every lens × theme cell at all four
  widths; the boundary in six lens × theme cells at 390 and 360 through an uncommitted,
  hash-gated throw in `StalenessBanner` (no data edit, no crash switch), with tab recovery,
  Reload and Reset view, and fallback contrast 6.54:1 light / 7.40:1 dark; the OS-change
  listener in the pane; the pin through three midnights with the clock shim and Back reading
  the new today; `?date=<today>` unpinning; `?only=pl,laliga` normalizing to `%2C` with
  Step 0 still decoding it; both builds' byte order (bootstrap at byte 46, stylesheet link at
  1617). Not re-run: the built preview with the application module held at the network layer
  (the pane refuses `file://` and has no preview-server entry), so production "before React"
  ordering rests on that byte order plus the VM tests.
